# SPEC-904 — Pet hidup E: atlas v02 (`held`/`falling`/`dizzy`) + `wave` yang bisa diulang

> **For agentic workers:** REQUIRED SUB-SKILL: gunakan `superpowers:executing-plans` (atau
> `superpowers:subagent-driven-development`) untuk mengeksekusi plan ini task demi task. Langkah
> memakai checkbox (`- [ ]`) untuk pelacakan.

**Goal:** Menerbitkan atlas sprite Pet Hanoman **v02** berisi 13 baris lama apa adanya + tiga baris
baru di ekor (`held`, `falling`, `dizzy`), dengan baris `wave` diregenerasi supaya bisa diputar
berulang tanpa kedip sambungan — aset & manifest saja, tanpa perubahan perilaku komponen.

**Architecture:** Pipeline aset yang sudah ada (ADR-0140) dipakai apa adanya: `gen.py` (Codex GPT
Image) → `key.py` (chroma key hijau) → `register.py` (deteksi celah + registrasi + pin) → `qa.py`
(gerbang + artefak review) → `atlas.py` (rakit atlas + `pet.json`) → `verify.py`. Yang bertambah di
pustaka `petlib.py` hanya: tiga entri `ROWS`, satu **mode registrasi `float`** untuk baris yang tak
menapak tanah, satu **gerbang kerataan langkah** untuk baris yang diputar berulang, plafon atlas yang
dinaikkan, dan `version` manifest yang naik ke 2. Frontend hanya menambah tiga kunci baris dan nama
berkas atlas.

**Tech Stack:** Python 3 + Pillow + NumPy (skrip aset, **tanpa dependency baru**), Codex CLI
(`codex exec -i <ref> -`) untuk generasi gambar, TypeScript + Vite + Vitest (frontend).

## Global Constraints

- Baris baru **hanya di EKOR** `ROWS`/`PET_ROW_KEYS`. Menyisipkan di tengah menggeser indeks baris
  dan mematahkan `--row` semua pose lama **tanpa satu pun error**.
- `anchor.baseline` = **202** dan `character.h` = **168** **tidak boleh berubah**.
- Sel tetap **192×208**, kolom tetap **8**.
- **Tanpa dependency Python baru.** Pipeline tetap `codex exec -i ref -` + `petlib.py`.
- `src/src/screens/HanomanPet.tsx` dan `src/src/screens/pet-walk.ts` **tidak disentuh** sama sekali
  (tiga baris baru bukan pose; `POSE_ROW` dan `PetPose` tak berubah).
- Ukuran atlas: 16 baris @ `quality=76` terukur 1 191 960 B = **+25,4 %** vs v01 (950 480 B) — di
  dalam batas +40 % brief. **Jangan menurunkan resolusi sel.**
- Test frontend saja di plan ini; `--no-file-parallelism` tak diperlukan (tak ada test server).
- `internal/docs` yang tersentuh diperbarui **dalam commit yang sama** dan ter-link di
  `internal/docs/README.md`.
- Spec acuan: `docs/superpowers/specs/2026-08-22-spec-904-pet-atlas-v02-diseret-design.md`.
- Semua perintah dijalankan dari root worktree
  `/Users/denameidina/Documents/Nafanesia/hanoman/.worktrees/spec-904`.

---

### Task 1: `petlib` — mode `float`, gerbang kerataan langkah, tiga baris, plafon, `version: 2`

**Files:**
- Modify: `internal/scripts/pet/petlib.py`
- Test: `internal/scripts/pet/test-petlib.py`

**Interfaces:**
- Consumes: —
- Produces:
  - `petlib.ROWS` bertambah tiga entri di ekor: `held`/`falling` (`mode: "float"`, `even: True`),
    `dizzy` (`mode: "stand"`). `wave` bertambah `"even": True`.
  - `petlib.RESIDUAL_GATE["float"] -> float` (nilai awal `0.35`).
  - `petlib.STEP_RATIO_GATE: float = 3.5` dan `petlib.STEP_VISIBLE: float = 0.10`.
  - `petlib.frame_steps(strip: Image.Image) -> list[float]` — delapan langkah antar-frame
    (indeks `i` = langkah frame `i+1` → frame `(i+1) % 8 + 1`, jadi elemen terakhir adalah
    sambungan 8→1), masing-masing = jumlah piksel XOR mask ÷ massa mask frame 1.
  - `petlib.manifest()` memancarkan `"version": 2` dan membuang kunci pipeline `mode` **dan** `even`
    dari setiap baris.

- [ ] **Step 1: Tulis test yang gagal untuk `frame_steps`**

Tambahkan di `internal/scripts/pet/test-petlib.py`, sesudah class `FeetTest`:

```python
class FrameStepsTest(unittest.TestCase):
    def strip_of(self, masks: list[np.ndarray]) -> Image.Image:
        strip = Image.new("RGBA", (petlib.CELL_W * 8, petlib.CELL_H), (0, 0, 0, 0))
        for i, m in enumerate(masks):
            cell = Image.fromarray(np.dstack([np.full(m.shape + (3,), 200, np.uint8),
                                              (m * 255).astype(np.uint8)]), "RGBA")
            strip.alpha_composite(cell, (i * petlib.CELL_W, 0))
        return strip

    def block(self, x0: int, w: int) -> np.ndarray:
        m = np.zeros((petlib.CELL_H, petlib.CELL_W), bool)
        m[40:180, x0:x0 + w] = True
        return m

    def test_returns_eight_steps_and_the_last_one_is_the_seam(self) -> None:
        # Frame 1..8 = balok yang bergeser 4 px per frame lalu kembali: langkah 8→1 sama besarnya
        # dengan langkah lain, sehingga rasio kerataannya 1.
        xs = [40, 44, 48, 52, 56, 52, 48, 44]
        steps = petlib.frame_steps(self.strip_of([self.block(x, 60) for x in xs]))
        self.assertEqual(len(steps), 8)
        for s in steps:
            self.assertAlmostEqual(s, steps[0], delta=0.01)

    def test_twin_frames_at_the_seam_make_the_ratio_explode(self) -> None:
        # Frame 7, 8 dan 1 nyaris kembar (langkah 0), frame 3..5 melompat jauh — persis bentuk
        # `wave` v01 (rasio terukur 7,07).
        xs = [40, 40, 90, 90, 40, 40, 40, 40]
        steps = petlib.frame_steps(self.strip_of([self.block(x, 60) for x in xs]))
        self.assertEqual(min(steps), 0.0)
        self.assertGreater(max(steps), petlib.STEP_VISIBLE)
```

- [ ] **Step 2: Jalankan test, pastikan GAGAL**

```bash
python3 internal/scripts/pet/test-petlib.py FrameStepsTest -v
```

Diharapkan: `AttributeError: module 'petlib' has no attribute 'frame_steps'` (dua test error).

- [ ] **Step 3: Implementasikan `frame_steps` + konstanta gerbang di `petlib.py`**

Di `internal/scripts/pet/petlib.py`, sesudah blok komentar `RESIDUAL_GATE` (ganti baris
`RESIDUAL_GATE = {"stand": 0.25, "walk": 0.30, "jump": 0.50}`) menjadi:

```python
RESIDUAL_GATE = {"stand": 0.25, "walk": 0.30, "jump": 0.50, "float": 0.35}
# SPEC-904 · kerataan langkah untuk baris yang DIPUTAR BERULANG (`even: True`). Yang membuat sebuah
# loop tersendat bukan besar-kecilnya gerak, melainkan langkah besar yang bertetangga dengan langkah
# nyaris nol — `wave` v01 mengukur max 0,201 (tangan turun) di sebelah 0,028/0,031 (dua frame
# nyaris kembar di sambungan), rasio 7,07. Gerbang karena itu RASIO, bukan nilai mutlak, dan hanya
# berlaku saat geraknya memang terlihat: baris bernapas halus (`sleep` 21,3 · `blocked` 6,9) punya
# rasio besar tanpa pernah terbaca tersendat karena SELURUH langkahnya kecil. Dikalibrasi atas baris
# nyata yang sudah lolos Gate 2: walk-left 2,06 · walk-right 2,33 · waiting 3,18 · deciding 3,43.
STEP_VISIBLE = 0.10      # di bawah ini gerak baris dianggap terlalu halus untuk dinilai rata
STEP_RATIO_GATE = 3.5    # max(langkah) / min(langkah), termasuk sambungan 8→1
```

Lalu di bagian `# ---------------------------------------------------------------- QA artefak`,
sebelum `def onion_skin(...)`, tambahkan:

```python
def frame_steps(strip: Image.Image) -> list[float]:
    """Delapan langkah antar-frame sebuah strip, dinormalkan ke massa frame 1.

    Elemen TERAKHIR adalah sambungan 8→1 — justru langkah yang tak terlihat di contact sheet
    maupun onion-skin, dan satu-satunya yang menentukan apakah baris ini bisa diputar berulang."""
    masks = [np.asarray(strip.crop((i * CELL_W, 0, (i + 1) * CELL_W, CELL_H)).getchannel("A")) > 64
             for i in range(COLUMNS)]
    base = max(float(masks[0].sum()), 1.0)
    return [round(float(np.count_nonzero(masks[i] ^ masks[(i + 1) % COLUMNS])) / base, 4)
            for i in range(COLUMNS)]
```

- [ ] **Step 4: Jalankan test, pastikan LULUS**

```bash
python3 internal/scripts/pet/test-petlib.py FrameStepsTest -v
```

Diharapkan: `Ran 2 tests` … `OK`.

- [ ] **Step 5: Tulis test yang gagal untuk mode `float`**

Pertama, `draw_character` perlu bisa memanjangkan kaki ke bawah tanpa menggeser kepala. Ubah
tanda tangannya di `internal/scripts/pet/test-petlib.py`:

```python
def draw_character(size: tuple[int, int] = (260, 340), tail_lift: int = 0, lift: int = 0,
                   scale: float = 1.0, leg_drop: int = 0) -> Image.Image:
```

dan ganti baris torso:

```python
    # kaki + torso: kolom 150..230; `leg_drop` memanjangkan kaki KE BAWAH tanpa menyentuh kepala
    d.rectangle((150, base - 220, 230, base + leg_drop), fill=BODY)
```

(`size` tetap `(260, 340)` dan `base = h - 6 - lift`, jadi `leg_drop ≤ 6` saja yang muat; naikkan
tinggi kanvas default menjadi `(260, 360)` supaya `leg_drop` sampai 20 px tetap di dalam kanvas.)

Lalu tambahkan test baru di dalam `class BuildStripTest`:

```python
    def test_float_registers_the_upper_body_and_never_forces_feet_to_the_baseline(self) -> None:
        # Kepala DIAM, kaki menjuntai berbeda panjang tiap frame — bentuk baris `held`/`falling`.
        drops = [0, 8, 16, 20, 16, 8, 0, 4]
        frames = [draw_character(leg_drop=d) for d in drops]
        shifts = [(0, 0)] * 8
        sprites = self.sprites(frames, shifts)
        walk, _ = petlib.build_strip(sprites, "walk")
        float_strip, report = petlib.build_strip(sprites, "float")

        def top(strip, i):
            a = np.asarray(strip.crop((i * petlib.CELL_W, 0, (i + 1) * petlib.CELL_W,
                                       petlib.CELL_H)).getchannel("A")) > 64
            return int(np.where(a.any(axis=1))[0].min())

        def bottom(strip, i):
            a = np.asarray(strip.crop((i * petlib.CELL_W, 0, (i + 1) * petlib.CELL_W,
                                       petlib.CELL_H)).getchannel("A")) > 64
            return int(np.where(a.any(axis=1))[0].max())

        # `walk` memaksa SETIAP dasar ke baseline → kepala ikut naik-turun.
        self.assertEqual({bottom(walk, i) for i in range(8)}, {petlib.BASELINE - 1})
        self.assertGreater(len({top(walk, i) for i in range(8)}), 3)
        # `float` menjangkarkan tubuh ATAS → kepala sejajar, dasar bebas mengikuti kaki.
        self.assertLessEqual(max(top(float_strip, i) for i in range(8))
                             - min(top(float_strip, i) for i in range(8)), 2)
        self.assertGreater(len({bottom(float_strip, i) for i in range(8)}), 3)
        # `float` tak di-pin: tak ada `residual_post` sama sekali.
        self.assertTrue(all("residual_post" not in r for r in report), report)
        self.assertIn("float", petlib.RESIDUAL_GATE)
```

- [ ] **Step 6: Jalankan test, pastikan GAGAL**

```bash
python3 internal/scripts/pet/test-petlib.py BuildStripTest.test_float_registers_the_upper_body_and_never_forces_feet_to_the_baseline -v
```

Diharapkan: GAGAL pada `assertIn("float", petlib.RESIDUAL_GATE)` atau lebih awal pada asersi
kepala/dasar — mode `float` belum dikenal gerbangnya.

- [ ] **Step 7: Dokumentasikan mode `float` di `petlib.py`**

`RESIDUAL_GATE["float"]` sudah ditambahkan di Step 3. Yang tersisa adalah menyatakan modenya di
docstring `build_strip`, karena alur kodenya sudah benar tanpa cabang baru (`region_for`
mengembalikan wilayah ATAS untuk setiap mode non-`stand`, `build_strip` hanya menimpa `dy` di cabang
`walk`/`jump`, dan `pin` default `mode == "stand"`). Ganti docstring `build_strip`:

```python
def build_strip(sprites: list[Sprite], mode: str, pin: bool | None = None) -> tuple[Image.Image, list[dict]]:
    """8 sprite → strip 8 × (CELL_W×CELL_H) terregistrasi. Laporan per frame untuk gerbang QA.

    Empat mode: `stand` (registrasi wilayah BAWAH + pin), `walk` (wilayah ATAS, dasar dipaksa
    menapak baseline), `jump` (wilayah ATAS, ketinggian busur dipertahankan), dan `float` —
    SPEC-904, untuk baris yang tak menapak tanah sama sekali (`held` tergantung dari atas,
    `falling` meringkuk di udara). `float` meminjam wilayah ATAS seperti `walk` karena yang statis
    di sana adalah kepala/torso, tetapi `dy` diambil apa adanya dari registrasi: memaksa piksel
    terendah ke baseline akan mendorong seluruh badan naik-turun mengikuti kaki yang menjuntai.
    Tanpa pin — tak ada wilayah bawah yang statis untuk dibekukan."""
```

- [ ] **Step 8: Jalankan test, pastikan LULUS**

```bash
python3 internal/scripts/pet/test-petlib.py BuildStripTest -v
```

Diharapkan: `Ran 5 tests` … `OK`.

- [ ] **Step 9: Tulis test yang gagal untuk `ROWS`, `version: 2`, dan pembuangan kunci pipeline**

Ganti isi `AtlasTest.test_compose_atlas_and_manifest_from_rows` di
`internal/scripts/pet/test-petlib.py`:

```python
    def test_compose_atlas_and_manifest_from_rows(self) -> None:
        with tempfile.TemporaryDirectory() as tmp:
            rows = Path(tmp)
            for r in petlib.ROWS:
                Image.new("RGBA", (petlib.CELL_W * 8, petlib.CELL_H), (0, 0, 0, 0)).save(rows / f"{r['key']}.png")
            atlas = petlib.compose_atlas(rows)
            self.assertEqual(atlas.size, (1536, petlib.CELL_H * len(petlib.ROWS)))
            m = petlib.manifest(rows)
            self.assertEqual(m["version"], 2)
            self.assertEqual([r["key"] for r in m["rows"]], petlib.ROW_KEYS)
            self.assertNotIn("mode", m["rows"][0])
            self.assertEqual(m["rows"][7], {"key": "shipped", "fps": 10, "loop": False, "then": "idle"})
            self.assertEqual(set(m["sources"]), set(petlib.ROW_KEYS))
            self.assertEqual(m["character"], {"h": petlib.STAND_H})
            # SPEC-904 · tiga baris baru DI EKOR; indeks baris lama tak bergeser.
            self.assertEqual(petlib.ROW_KEYS[:13], [
                "idle", "walk-right", "walk-left", "working", "waiting", "blocked", "review",
                "shipped", "docs-updated", "wave", "deciding", "sleep", "thanks"])
            self.assertEqual(petlib.ROW_KEYS[13:], ["held", "falling", "dizzy"])
            self.assertEqual(m["rows"][13], {"key": "held", "fps": 8, "loop": True})
            self.assertEqual(m["rows"][14], {"key": "falling", "fps": 8, "loop": True})
            self.assertEqual(m["rows"][15], {"key": "dizzy", "fps": 8, "loop": False, "then": "idle"})
            # `mode` dan `even` adalah kunci PIPELINE — keduanya tak boleh bocor ke pet.json.
            self.assertTrue(all("even" not in r for r in m["rows"]), m["rows"])
            self.assertEqual([r["key"] for r in petlib.ROWS if r.get("even")],
                             ["wave", "held", "falling"])
            self.assertEqual(petlib.ATLAS_BUDGET, 1_300_000)
```

- [ ] **Step 10: Jalankan test, pastikan GAGAL**

```bash
python3 internal/scripts/pet/test-petlib.py AtlasTest -v
```

Diharapkan: GAGAL — `m["version"]` masih `1`.

- [ ] **Step 11: Tambahkan tiga baris, `even`, plafon, dan `version: 2`**

Di `internal/scripts/pet/petlib.py`, ganti `ATLAS_BUDGET = 1_000_000` menjadi:

```python
# SPEC-904 · 1 MB tak bisa menampung 16 baris pada kualitas mana pun yang layak: q76 = 1 191 960 B,
# dan `quality` adalah tuas yang LEMAH di sini (q76 → q20 hanya −34 %) karena atlas ini didominasi
# kanal alpha lossless di atas seni datar berkontur tegas. Turun ke bawah 1 MB butuh ±q45 — dan
# atlas dirakit ulang dari rows/, jadi ketigabelas baris yang sudah lolos Gate 2 ikut turun.
# Premis plafon lama ("satu <img> di-decode di setiap halaman") juga tak dilayani `quality`: biaya
# decode adalah PIKSEL (19,5 MiB RGBA pada 16 baris), bukan byte. 1 300 000 = +30 % vs atlas v01
# (950 480 B) — di dalam batas +40 % SPEC-904 — dengan sisa ±108 KB untuk satu regenerasi rutin.
ATLAS_BUDGET = 1_300_000
```

Lalu di akhir array `ROWS`, tambahkan `"even": True` pada baris `wave` dan tiga entri baru:

```python
    {"key": "wave",         "fps": 10, "loop": False, "mode": "stand", "then": "idle", "even": True},
```

```python
    # SPEC-898 · reaksi saat pet dielus (STK-007). Sekali-putar seperti `wave`, bukan pose mesin.
    {"key": "thanks",       "fps": 10, "loop": False, "mode": "stand", "then": "idle"},
    # SPEC-904 · tiga baris untuk pet yang DISERET, tetap di EKOR seperti preseden SPEC-897/898.
    # `held` & `falling` tak menapak tanah → mode `float`; keduanya diputar berulang selama seretan
    # berlangsung → `even: True`. `dizzy` berdiri (kaki tertanam, torso sempoyongan) dan sekali-putar
    # supaya pet BERDIRI LAGI sesudah mendarat, jadi frame 8-nya pose istirahat seperti `wave`.
    {"key": "held",         "fps": 8,  "loop": True,  "mode": "float", "even": True},
    {"key": "falling",      "fps": 8,  "loop": True,  "mode": "float", "even": True},
    {"key": "dizzy",        "fps": 8,  "loop": False, "mode": "stand", "then": "idle"},
]
```

Terakhir, di `manifest()`, naikkan versi dan buang kunci pipeline `even`:

```python
def manifest(rows_dir: Path) -> dict:
    return {
        "id": "PET-001", "version": 2,
        "cell": {"w": CELL_W, "h": CELL_H}, "columns": COLUMNS,
        "anchor": {"x": ANCHOR_X, "baseline": BASELINE},
        "character": {"h": STAND_H},
        # `mode` dan `even` adalah kunci PIPELINE (registrasi & gerbang QA); frontend tak
        # memakainya, jadi keduanya tak pernah ikut ke pet.json.
        "rows": [{k: v for k, v in r.items() if k not in ("mode", "even")} for r in ROWS],
        "sources": {r["key"]: sha256(rows_dir / f"{r['key']}.png") for r in ROWS},
    }
```

- [ ] **Step 12: Jalankan seluruh test petlib, pastikan LULUS**

```bash
python3 internal/scripts/pet/test-petlib.py -v
```

Diharapkan: `Ran 13 tests` … `OK` — 10 test lama (`ChromaKeyTest` 1 · `DetectTest` 2 · `FeetTest` 1
· `BuildStripTest` 4 · `AtlasTest` 2) + `FrameStepsTest` 2 + mode `float` 1. Tanpa satu pun
`FAIL`/`ERROR`.

- [ ] **Step 13: Commit**

```bash
git add internal/scripts/pet/petlib.py internal/scripts/pet/test-petlib.py
git commit -m "feat(pet): mode registrasi float, gerbang kerataan langkah, tiga baris SPEC-904 (petlib)"
```

---

### Task 2: Gerbang kerataan langkah di `qa.py`

**Files:**
- Modify: `internal/scripts/pet/qa.py`

**Interfaces:**
- Consumes: `petlib.frame_steps`, `petlib.STEP_RATIO_GATE`, `petlib.STEP_VISIBLE`, `petlib.row_def`
- Produces: `qa.py <key>` mencetak kedelapan langkah antar-frame untuk **setiap** baris, dan
  **menggagalkan** baris ber-`even: True` yang `max/min > STEP_RATIO_GATE` saat `max ≥ STEP_VISIBLE`.

- [ ] **Step 1: Buktikan gerbangnya menangkap `wave` v01 (baseline terukur)**

```bash
python3 - <<'PY'
import importlib.util, sys
from pathlib import Path
from PIL import Image
s = importlib.util.spec_from_file_location("petlib", "internal/scripts/pet/petlib.py")
petlib = importlib.util.module_from_spec(s); sys.modules["petlib"] = petlib; s.loader.exec_module(petlib)
for k in ("wave", "walk-left", "walk-right", "waiting", "deciding"):
    st = petlib.frame_steps(Image.open(f"internal/assets/pet/rows/{k}.png").convert("RGBA"))
    print(f"{k:12s} max={max(st):.3f} min={min(st):.3f} rasio={max(st)/max(min(st),1e-6):.2f}")
PY
```

Diharapkan persis: `wave` rasio **7,07** (max 0,201 / min 0,028), `walk-left` 2,06, `walk-right`
2,33, `waiting` 3,18, `deciding` 3,43. Itu membuktikan gerbang 3,5 memisahkan `wave` dari semua
baris loop bergerak yang sudah lolos Gate 2 — catat angkanya di pesan commit Task 6.

- [ ] **Step 2: Tambahkan gerbang di `qa.py`**

Di `internal/scripts/pet/qa.py`, sesudah blok `for fr in report["frames"]: …` dan **sebelum**
`qa = ASSETS / "qa"`, sisipkan:

```python
    steps = petlib.frame_steps(strip.convert("RGBA"))
    ratio = max(steps) / max(min(steps), 1e-6)
    if row.get("even") and max(steps) >= petlib.STEP_VISIBLE and ratio > petlib.STEP_RATIO_GATE:
        problems.append(f"langkah tak rata: max {max(steps):.3f} / min {min(steps):.3f} "
                        f"= {ratio:.2f} > {petlib.STEP_RATIO_GATE} — baris ini diputar berulang")
```

dan sesudah baris `print(f"qa/{key}-contact.png …")`, tambahkan cetakan langkah untuk **semua**
baris (sambungan 8→1 adalah elemen terakhir, dan ia tak terlihat di contact sheet maupun onion-skin):

```python
    print("  langkah " + " ".join(f"{i + 1}→{(i + 1) % 8 + 1}:{s:.3f}" for i, s in enumerate(steps))
          + f"  rasio={ratio:.2f}")
```

- [ ] **Step 3: Jalankan `qa.py` atas baris lama, pastikan tak ada yang jadi merah**

`qa.py` menulis ulang artefak `qa/` setiap kali dipanggil, sedangkan di sini yang dicari hanya
kode keluarnya — jadi artefak yang sudah dikomit dikembalikan sesudahnya.

```bash
for k in idle walk-right walk-left working waiting blocked review shipped docs-updated deciding sleep thanks; do
  python3 internal/scripts/pet/qa.py "$k" >/dev/null || echo "REGRESI: $k"
done
git checkout -- internal/assets/pet/qa
git status --porcelain internal/assets/pet/qa
echo "selesai"
```

Diharapkan: `selesai` tanpa satu pun baris `REGRESI`, dan `git status` kosong — hanya `wave` (yang
`even: True`) boleh berubah statusnya, dan itu diuji di langkah berikutnya.

- [ ] **Step 4: Jalankan `qa.py wave`, pastikan GAGAL dengan alasan yang benar**

```bash
python3 internal/scripts/pet/qa.py wave; echo "exit=$?"
```

Diharapkan: `exit=1` dan pesan `FAIL: wave: langkah tak rata: max 0.201 / min 0.028 = 7.07 > 3.5 —
baris ini diputar berulang`. Inilah gerbang yang akan dijawab regenerasi di Task 6.

- [ ] **Step 5: Commit**

```bash
git add internal/scripts/pet/qa.py
git commit -m "feat(pet): gerbang kerataan langkah antar-frame untuk baris yang diputar berulang"
```

---

### Task 3: Naskah generasi empat baris

**Files:**
- Create: `internal/assets/pet/prompts/held.md`, `internal/assets/pet/prompts/falling.md`,
  `internal/assets/pet/prompts/dizzy.md`
- Modify: `internal/assets/pet/prompts/wave.md`

**Interfaces:**
- Consumes: `internal/assets/pet/prompts/common.md` (subjek/gaya/layout/registrasi/latar — tak
  disentuh).
- Produces: empat naskah yang dibaca `gen.py` (`prompts/<key>.md` + `common.md`).

- [ ] **Step 1: Tulis `prompts/held.md`**

```bash
cat > internal/assets/pet/prompts/held.md <<'EOF'
ROW "held" (8 frames, LOOPS): he is being picked up and held in the air by the viewer, dangling from an invisible grip at the back of his neck/collar — dignified and startled, never slapstick. He stays in profile facing right. This row is an exception to the fixed-feet rule: his feet are OFF the ground and touch nothing. Do NOT draw a hand, a rope, a hook or any other prop — only the character. The head, shoulders and upper chest are the FIXED part: draw them at EXACTLY the same position and size in all 8 frames, as if pinned. The single visible eye is WIDE OPEN throughout — a large round eye with the eyebrow raised high and a small open mouth; his expression does not change between frames. The arms hang loosely down from the shoulders and sway only slightly. The short legs dangle straight down and swing gently left-right like a pendulum: frame 1 centred, frames 2-3 swinging forward (to the right), frame 4 back at centre, frames 5-6 swinging back (to the left), frames 7-8 returning toward centre so that frame 8 leads into frame 1 with a step no larger than the others. The long tail hangs down and sways in the OPPOSITE direction to the legs, its golden tip trailing last. Keep the swing small — no more than about a tenth of his body width in either direction — and make every step the same size, because this row is played on repeat.
EOF
```

- [ ] **Step 2: Tulis `prompts/falling.md`**

```bash
cat > internal/assets/pet/prompts/falling.md <<'EOF'
ROW "falling" (8 frames, LOOPS): he has been let go and is drifting downward slowly, bracing himself — calm and dignified, not panicking. He stays in profile facing right and touches nothing; there is no ground, no speed lines, no dust cloud, no motion blur, no props. This row is an exception to the fixed-feet rule: the feet are off the ground. He is TUCKED but still drawn TALL, not curled into a small ball: knees drawn up toward the chest, both arms wrapped around the shins, back rounded, chin tucked, and the long tail RAISED HIGH, curving up and over his back with the golden tip well above the head. The overall height of the drawing, from the raised tail tip down to the tucked feet, must be about the same as the standing character in the attached idle strip — do not draw him smaller and do not draw him larger. The single visible eye is open and steady with the brow slightly drawn, mouth a small firm line; the expression does not change between frames. The head, torso, arms and knees are the FIXED part: draw them at EXACTLY the same position and size in all 8 frames. The only motion is a slow, even sway of the raised tail tip and a gentle lift-and-settle of the loose hem of the sarong: frames 1-4 the tail tip drifts a little to the left and the hem lifts, frames 5-8 they return, with frame 8 leading into frame 1 in a step no larger than the others. Every step must be the same size, because this row is played on repeat.
EOF
```

- [ ] **Step 3: Tulis `prompts/dizzy.md`**

```bash
cat > internal/assets/pet/prompts/dizzy.md <<'EOF'
ROW "dizzy" (8 frames, plays ONCE then returns to idle): he has just landed after being dropped and is briefly dizzy before pulling himself together. He stays in profile facing right and stands on the ground the whole time: the feet, ankles and the hem of the sarong are PLANTED — draw them at EXACTLY the same position and size in all 8 frames, no stepping, no hopping, no drifting. Only the torso above the waist, the shoulders, the arms and the head move. Frame 1: the rest pose, exactly like idle frame 1. Frames 2-6: he is dizzy — the head lolls and rolls through one full slow circle (frame 2 tipped forward, frame 3 tipped down-back, frame 4 tipped back, frame 5 tipped up-forward, frame 6 coming level), the single visible eye is drawn as a SPIRAL (a simple flat two-turn spiral, no extra pupil, no cross-eyes, no emoji face), the mouth a small wavy line, the arms hanging loose and swinging opposite to the head, and the torso swaying left-right above the fixed hips. Across frames 2-6 draw TWO small flat gold five-pointed stars circling the tall golden jamang/crown: they must TOUCH or OVERLAP the crown silhouette at all times and must stay inside the horizontal width of the head — never floating away as separate detached shapes, never drawn to the side of the head with a gap. Frame 7: the stars are gone, the spiral eye has become a normal eye again, the head is level, he is straightening up. Frame 8: back to the rest pose, equal to frame 1, so the row leads cleanly into idle. Keep all the steps the same size; nothing snaps.
EOF
```

- [ ] **Step 4: Ganti `prompts/wave.md`**

```bash
cat > internal/assets/pet/prompts/wave.md <<'EOF'
ROW "wave" (8 frames, plays ONCE and is also played REPEATEDLY while the viewer hovers): a friendly greeting to the viewer. He stays in profile facing right; feet, legs and sarong are fixed and identical in all 8 frames, no stepping and no drifting. This row is REPLAYED BACK TO BACK, so the step from frame 8 into frame 1 matters as much as every other step, and NO TWO NEIGHBOURING FRAMES MAY LOOK NEARLY IDENTICAL — a repeated frame reads as a stutter. Move the far arm by the SAME small amount every single frame. Frame 1: rest pose, exactly like idle frame 1, the far hand down at his side. Frame 2: the far hand has risen to hip height, elbow starting to bend. Frame 3: the hand is at shoulder height, palm beginning to turn toward the viewer. Frame 4: the hand is up beside the head, palm open toward the viewer, tilted to the LEFT; the head turns a little toward the viewer with a warm expression. Frame 5: the same raised hand tilted to the RIGHT. Frame 6: tilted to the LEFT again. Frame 7: the hand has come back down to shoulder height, palm turning inward. Frame 8: the hand is back at hip height — the SAME position as frame 2, and NOT the rest pose, so that the step from frame 8 into frame 1 is exactly as large as the step from frame 1 into frame 2. The hand keeps the same size, the same fingers and the same shape in every frame; only its position and angle change, rotating around the elbow and wrist. The tail gives one small happy flick on frames 4-5 and settles by frame 7.
EOF
```

- [ ] **Step 5: Verifikasi `gen.py` bisa merakit keempat prompt**

```bash
for k in held falling dizzy wave; do
  python3 internal/scripts/pet/gen.py "$k" --print | head -1
done
```

Diharapkan: empat baris perintah `codex exec --skip-git-repo-check -s workspace-write -C /var/...
--add-dir ... -i .../ref/anoman-pet-model.png -i .../rows/idle.png -` (tanpa `FAIL:`).

- [ ] **Step 6: Commit**

```bash
git add internal/assets/pet/prompts
git commit -m "feat(pet): naskah held/falling/dizzy + wave yang menyambung 8→1 (SPEC-904)"
```

---

### Task 4: Baris `held`

**Files:**
- Create: `internal/assets/pet/rows/held.png`, `internal/assets/pet/rows/held.report.json`,
  `internal/assets/pet/qa/held.gif`, `internal/assets/pet/qa/held-contact.png`,
  `internal/assets/pet/qa/held-onion.png`

**Interfaces:**
- Consumes: `petlib.ROWS` entri `held` (`mode: "float"`, `even: True`) dari Task 1; naskah dari Task 3.
- Produces: strip `rows/held.png` berukuran 1536×208 RGBA yang lolos `qa.py`.

- [ ] **Step 1: Generasi**

```bash
python3 internal/scripts/pet/gen.py held
```

Diharapkan (±3 menit): `[held] /Users/.../exec-*.png → raw/held.png (<N> B, <t>s)`. Bila
`FAIL: [held] codex keluar …`, baca `internal/assets/pet/raw/held.log` dan ulangi perintah yang sama.

- [ ] **Step 2: Chroma key**

```bash
python3 internal/scripts/pet/key.py held
```

Diharapkan: `held.keyed.png: transparan=<besar> opak=<besar> parsial=<kecil>`. Bila `transparan`
mendekati nol, latar yang digambar Codex bukan hijau polos — ulangi Step 1.

- [ ] **Step 3: Registrasi**

```bash
python3 internal/scripts/pet/register.py held
```

Diharapkan: delapan baris `f1..f8` dengan `residu=` dan **tanpa** `TUMPAH=`, lalu
`ditulis rows/held.png + rows/held.report.json`. Bila `terdeteksi <n> sprite, butuh 8`, generasinya
rusak — ulangi Step 1 dengan `--note` yang mengulang **seluruh** batasan (pelajaran `thanks`:
koreksi parsial membuat model melupakan batasan yang sudah dipenuhi), misalnya:

```bash
python3 internal/scripts/pet/gen.py held --note "Exactly 8 separate poses in a 4x2 grid with clear empty space between cells; never let a tail, hand or ornament cross into a neighbouring cell; head and shoulders identical in every frame; feet off the ground; legs swinging only a little; pure flat #00FF00 background over the whole canvas."
```

- [ ] **Step 4: Gerbang QA**

```bash
python3 internal/scripts/pet/qa.py held
```

Diharapkan: `qa/held-contact.png qa/held-onion.png qa/held.gif ditulis`, baris `langkah …
rasio=<r>` dengan `r ≤ 3.5`, lalu `OK held: 8 frame, residu maks <x> ≤ 0.35`.

Bila gagal karena `residu … > 0.35`: catat angkanya. Gerbang `float` **belum pernah dikalibrasi atas
baris nyata** — kalau residu pra-pin di bawah 0,5 sementara `qa/held.gif` terlihat mantap di mata,
naikkan `RESIDUAL_GATE["float"]` ke angka terukur + margin dan **catat pengukurannya di komentar
`RESIDUAL_GATE`**, mengikuti preseden komentar yang sudah ada di sana. Kalau residunya ≥ 0,5,
generasinya memang rusak — ulangi Step 1.

Bila gagal karena `langkah tak rata`: ulangi Step 1 dengan `--note` yang mengulang seluruh batasan
plus penekanan irama, misalnya:

```bash
python3 internal/scripts/pet/gen.py held --note "This row is played on repeat: move the legs and tail by the SAME small amount in every single frame, and make frame 8 exactly one step before frame 1 — no two neighbouring frames may look nearly identical. Keep head and shoulders pinned, feet off the ground, no hand/rope/prop, pure flat #00FF00 background over the whole canvas, 8 poses in a 4x2 grid with clear gaps."
```

- [ ] **Step 5: Review mata (Gate 2)**

Buka `internal/assets/pet/qa/held.gif` dan `internal/assets/pet/qa/held-contact.png`. Periksa
secara sadar — gerbang numerik **tidak** menangkap semua ini (memori SPEC-897: `sleep` lolos
`qa.py` sambil memunculkan ornamen ekor kedua yang berkedip):

- siluet profil satu mata, jamang, kain, ekor besar; **tak ada mirror**;
- kepala & bahu benar-benar diam di kedelapan frame;
- mata membelalak di **semua** frame (bukan hanya sebagian);
- kaki & ekor mengayun berlawanan fase, langkahnya rata, dan frame 8 menyambung ke frame 1;
- tak ada tangan/tali/prop yang ikut tergambar;
- tak ada ornamen ekor kedua yang muncul-hilang antar-frame.

Bila ada yang meleset, ulangi Step 1 dengan `--note` yang mengulang seluruh batasan + koreksi baru.

- [ ] **Step 6: Commit**

```bash
git add internal/assets/pet/rows/held.png internal/assets/pet/rows/held.report.json internal/assets/pet/qa/held.gif internal/assets/pet/qa/held-contact.png internal/assets/pet/qa/held-onion.png
git commit -m "feat(pet): baris held — tergantung dari atas, kaki & ekor menjuntai (SPEC-904)"
```

---

### Task 5: Baris `falling`

**Files:**
- Create: `internal/assets/pet/rows/falling.png`, `internal/assets/pet/rows/falling.report.json`,
  `internal/assets/pet/qa/falling.gif`, `internal/assets/pet/qa/falling-contact.png`,
  `internal/assets/pet/qa/falling-onion.png`

**Interfaces:**
- Consumes: `petlib.ROWS` entri `falling` (`mode: "float"`, `even: True`) dari Task 1; naskah dari
  Task 3.
- Produces: strip `rows/falling.png` 1536×208 RGBA yang lolos `qa.py`.

- [ ] **Step 1: Generasi**

```bash
python3 internal/scripts/pet/gen.py falling
```

Diharapkan: `[falling] … → raw/falling.png (<N> B, <t>s)`.

- [ ] **Step 2: Chroma key**

```bash
python3 internal/scripts/pet/key.py falling
```

Diharapkan: `falling.keyed.png: transparan=<besar> opak=<besar> parsial=<kecil>`.

- [ ] **Step 3: Registrasi**

```bash
python3 internal/scripts/pet/register.py falling
```

Diharapkan: delapan baris `f1..f8` tanpa `TUMPAH=`, lalu `ditulis rows/falling.png + …`.

- [ ] **Step 4: Periksa skala — jebakan khusus baris ini**

`build_strip` menormalkan tinggi bbox frame 1 menjadi `STAND_H` = 168 px **untuk setiap baris**
(terukur: ketigabelas strip yang dikomit semuanya `h=168`, `bottom=202`). Badan yang digambar
meringkuk kecil karena itu akan **diperbesar** sampai setinggi pet berdiri. Bandingkan lebarnya
dengan baris berdiri:

```bash
python3 - <<'PY'
import numpy as np
from PIL import Image
for k in ("idle", "falling"):
    a = np.asarray(Image.open(f"internal/assets/pet/rows/{k}.png").convert("RGBA")
                   .crop((0, 0, 192, 208)).getchannel("A")) > 64
    ys, xs = np.where(a)
    print(f"{k:8s} w={xs.max()-xs.min()+1:3d} h={ys.max()-ys.min()+1:3d}")
PY
```

Diharapkan: `falling` `h=168` (selalu) dan `w` di dalam rentang baris lain (116–173 px). Bila
`w > 200` atau siluetnya jelas jauh lebih gemuk daripada `idle`, badannya digambar terlalu meringkuk
— ulangi Step 1 dengan `--note`:

```bash
python3 internal/scripts/pet/gen.py falling --note "Draw him TUCKED BUT TALL, not curled into a ball: the total height from the raised tail tip down to the tucked feet must match the standing character in the attached idle strip. Keep the tail raised high above the head. Head, torso, arms and knees identical in all 8 frames; only the tail tip and the sarong hem move, in equal small steps, with frame 8 one step before frame 1. No ground, no speed lines, no props. Pure flat #00FF00 background, 8 poses in a 4x2 grid with clear gaps."
```

- [ ] **Step 5: Gerbang QA**

```bash
python3 internal/scripts/pet/qa.py falling
```

Diharapkan: artefak `qa/` tertulis, `rasio ≤ 3.5`, lalu `OK falling: 8 frame, residu maks <x> ≤ 0.35`
(atau ≤ gerbang `float` yang dikalibrasi di Task 4 Step 4).

- [ ] **Step 6: Review mata (Gate 2)**

Buka `qa/falling.gif` dan `qa/falling-contact.png`:

- badan meringkuk **tegak**, ekor terangkat tinggi di atas punggung, ujung emas terlihat;
- kepala/torso/lengan/lutut benar-benar diam; hanya ujung ekor & kain yang bergerak;
- ukurannya sebanding dengan `idle`, tidak menggelembung;
- tak ada tanah, garis kecepatan, awan debu, atau prop;
- tak ada mirror, tak ada dua mata.

- [ ] **Step 7: Commit**

```bash
git add internal/assets/pet/rows/falling.png internal/assets/pet/rows/falling.report.json internal/assets/pet/qa/falling.gif internal/assets/pet/qa/falling-contact.png internal/assets/pet/qa/falling-onion.png
git commit -m "feat(pet): baris falling — meringkuk, ekor terangkat, jatuh perlahan (SPEC-904)"
```

---

### Task 6: Baris `dizzy`

**Files:**
- Create: `internal/assets/pet/rows/dizzy.png`, `internal/assets/pet/rows/dizzy.report.json`,
  `internal/assets/pet/qa/dizzy.gif`, `internal/assets/pet/qa/dizzy-contact.png`,
  `internal/assets/pet/qa/dizzy-onion.png`

**Interfaces:**
- Consumes: `petlib.ROWS` entri `dizzy` (`mode: "stand"`, `loop: False`, `then: "idle"`) dari Task 1;
  naskah dari Task 3.
- Produces: strip `rows/dizzy.png` 1536×208 RGBA yang lolos `qa.py`.

- [ ] **Step 1: Generasi**

```bash
python3 internal/scripts/pet/gen.py dizzy
```

Diharapkan: `[dizzy] … → raw/dizzy.png (<N> B, <t>s)`.

- [ ] **Step 2: Chroma key**

```bash
python3 internal/scripts/pet/key.py dizzy
```

- [ ] **Step 3: Registrasi — jebakan bintang yang terpisah**

```bash
python3 internal/scripts/pet/register.py dizzy
```

`detect_sprites` memisahkan sprite pada celah transparan **≥ 8 kolom** dan **membuang** bbox yang
lebih kecil dari 40×80 px **tanpa suara**. Bintang pusing yang melayang terpisah dari kepala karena
itu punya dua nasib buruk: hilang dari strip, atau (bila cukup besar) membuat jumlah sprite ≠ 8.

- Bila muncul `terdeteksi <n> sprite, butuh 8` dengan `n > 8`: bintangnya terpisah. Ulangi Step 1
  dengan `--note` di bawah.
- Bila `n == 8` tetapi bintangnya tak terlihat di `qa/dizzy-contact.png` nanti: bintangnya juga
  terpisah, hanya kebetulan terlalu kecil untuk dihitung. Ulangi Step 1 dengan `--note` yang sama.

```bash
python3 internal/scripts/pet/gen.py dizzy --note "The two dizzy stars must TOUCH or OVERLAP the golden crown silhouette in every frame they appear, and stay inside the horizontal width of the head — never a detached shape with a gap around it. Feet, ankles and sarong hem planted and identical in all 8 frames; only torso, shoulders, arms and head move. Frame 1 and frame 8 are the rest pose equal to idle frame 1. Spiral eye only in frames 2-6. Pure flat #00FF00 background over the whole canvas, 8 poses in a 4x2 grid with clear gaps between cells."
```

Diharapkan pada akhirnya: delapan baris `f1..f8` tanpa `TUMPAH=`, lalu `ditulis rows/dizzy.png + …`.

- [ ] **Step 4: Gerbang QA**

```bash
python3 internal/scripts/pet/qa.py dizzy
```

Diharapkan: `OK dizzy: 8 frame, residu maks <x> ≤ 0.25`. `dizzy` **tidak** ber-`even`, jadi gerbang
kerataan langkah tak berlaku — busurnya memang sengaja tak rata (pusing lalu pulih).

Bila gagal `residu … > 0.25`: torsonya bergoyang lebih jauh daripada gerbang `stand`. Bandingkan
dengan `waiting` (0,162, baris berdiri paling ekstrem yang ada). Bila di atas 0,25 tetapi kakinya
memang tertanam di `qa/dizzy.gif`, ulangi Step 1 dengan `--note` yang menegaskan goyangan lebih
kecil; **jangan** melonggarkan `RESIDUAL_GATE["stand"]` — itu gerbang bersama dengan sembilan baris
berdiri lain.

- [ ] **Step 5: Review mata (Gate 2)**

Buka `qa/dizzy.gif` dan `qa/dizzy-contact.png`:

- kaki & kain benar-benar tertanam, tak ada langkah maupun geser;
- kepala berputar satu lingkaran penuh di frame 2–6, bukan sekadar mengangguk;
- mata spiral hanya di frame 2–6, kembali normal di frame 7–8;
- dua bintang emas **menempel** pada jamang, tak pernah terpisah, dan hilang di frame 7;
- frame 8 benar-benar sama dengan frame 1 (sambungan ke `idle` bersih) — periksa berdampingan di
  contact sheet;
- pin tidak memakan tangan (memori SPEC-896): tak ada telapak yang terpotong garis lurus.

- [ ] **Step 6: Commit**

```bash
git add internal/assets/pet/rows/dizzy.png internal/assets/pet/rows/dizzy.report.json internal/assets/pet/qa/dizzy.gif internal/assets/pet/qa/dizzy-contact.png internal/assets/pet/qa/dizzy-onion.png
git commit -m "feat(pet): baris dizzy — kepala berputar, bintang di jamang, badan sempoyongan (SPEC-904)"
```

---

### Task 7: Regenerasi `wave` sampai sambungan 8→1 rata

**Files:**
- Modify: `internal/assets/pet/rows/wave.png`, `internal/assets/pet/rows/wave.report.json`,
  `internal/assets/pet/qa/wave.gif`, `internal/assets/pet/qa/wave-contact.png`,
  `internal/assets/pet/qa/wave-onion.png`

**Interfaces:**
- Consumes: gerbang kerataan dari Task 2; naskah `wave` dari Task 3.
- Produces: `rows/wave.png` baru dengan `max/min langkah ≤ 3.5`; manifest `wave` **tak berubah**
  (`fps 10, loop false, then idle`), hanya hash `sources.wave` yang bergeser.

- [ ] **Step 1: Simpan baseline lama sebagai pembanding**

```bash
cp internal/assets/pet/rows/wave.png /tmp/wave-v01.png
python3 internal/scripts/pet/qa.py wave 2>&1 | grep -E "langkah|FAIL"
```

Diharapkan: `langkah 1→2:0.163 2→3:0.077 3→4:0.098 4→5:0.091 5→6:0.067 6→7:0.201 7→8:0.028
8→1:0.031  rasio=7.07` diikuti `FAIL: wave: langkah tak rata …`.

- [ ] **Step 2: Generasi**

```bash
python3 internal/scripts/pet/gen.py wave
```

- [ ] **Step 3: Chroma key + registrasi**

```bash
python3 internal/scripts/pet/key.py wave && python3 internal/scripts/pet/register.py wave
```

Diharapkan: delapan baris `f1..f8` tanpa `TUMPAH=`.

- [ ] **Step 4: Gerbang QA**

```bash
python3 internal/scripts/pet/qa.py wave
```

Diharapkan: `rasio ≤ 3.5` dan `OK wave: 8 frame, residu maks <x> ≤ 0.25`.

Bila `rasio` masih > 3,5, ulangi Step 2 dengan `--note` yang mengulang **seluruh** batasan:

```bash
python3 internal/scripts/pet/gen.py wave --note "This row is replayed back to back, so the step from frame 8 into frame 1 matters as much as every other step. Frame 8 must show the hand at HIP HEIGHT — the same position as frame 2 — and must NOT be the rest pose. No two neighbouring frames may look nearly identical. Move the arm by the same small amount every frame: f1 hand at the side, f2 hip, f3 shoulder, f4 beside the head tilted left, f5 tilted right, f6 tilted left, f7 shoulder, f8 hip. Feet, legs and sarong fixed and identical in all 8 frames, profile facing right, one visible eye, no mirroring. Pure flat #00FF00 background over the whole canvas, 8 poses in a 4x2 grid with clear gaps."
```

- [ ] **Step 5: Buktikan frame 1 masih menyambung ke `idle`**

`wave` tetap `loop: false, then: "idle"`, jadi frame 1-nya wajib tetap pose istirahat supaya
pergantian `idle → wave → idle` tak melompat:

```bash
python3 - <<'PY'
import numpy as np
from PIL import Image
def f1(k):
    return np.asarray(Image.open(f"internal/assets/pet/rows/{k}.png").convert("RGBA")
                      .crop((0, 0, 192, 208)).getchannel("A")) > 64
a, b = f1("idle"), f1("wave")
print("beda f1 wave vs f1 idle =", round(np.count_nonzero(a ^ b) / a.sum(), 3))
PY
```

Diharapkan: ≤ **0,15**. `wave` v01 sebagai pembanding: jalankan potongan yang sama atas
`/tmp/wave-v01.png` bila perlu. Bila jauh di atas 0,15, frame 1-nya bukan pose istirahat — ulangi
Step 2 dengan `--note` yang menegaskan "frame 1 is EXACTLY the rest pose of idle frame 1".

- [ ] **Step 6: Review mata (Gate 2)**

Buka `qa/wave.gif` dan `qa/wave-contact.png`:

- tangan naik & turun dalam langkah yang **terlihat sama besarnya**, tak ada frame yang melompat;
- frame 8 ≠ frame 1 (tangan di pinggul vs di sisi badan) — periksa berdampingan;
- lambaian di frame 4–6 kiri-kanan-kiri, telapak menghadap penonton;
- satu tangan saja yang naik (kalau dua, itu `thanks`, bukan `wave`);
- kaki & kain tak bergeser sama sekali.

- [ ] **Step 7: Commit**

```bash
git add internal/assets/pet/rows/wave.png internal/assets/pet/rows/wave.report.json internal/assets/pet/qa/wave.gif internal/assets/pet/qa/wave-contact.png internal/assets/pet/qa/wave-onion.png
git commit -m "fix(pet): wave bisa diputar berulang — frame 8 satu langkah sebelum frame 1 (SPEC-904)"
```

---

### Task 8: Rakit atlas v02, buang v01

**Files:**
- Modify: `internal/scripts/pet/atlas.py`, `internal/scripts/pet/verify.py`
- Create: `internal/assets/pet/hnm-pet-anoman-atlas-v02.webp`
- Delete: `internal/assets/pet/hnm-pet-anoman-atlas-v01.webp`
- Modify: `internal/assets/pet/pet.json`

**Interfaces:**
- Consumes: keenam belas strip di `internal/assets/pet/rows/`; `petlib.ATLAS_BUDGET` = 1 300 000.
- Produces: `internal/assets/pet/hnm-pet-anoman-atlas-v02.webp` (1536×3328) +
  `internal/assets/pet/pet.json` ber-`version: 2` dan 16 baris — dikonsumsi Task 9.

- [ ] **Step 1: Ganti nama atlas di `atlas.py`**

Di `internal/scripts/pet/atlas.py`, ubah docstring baris pertama dan konstanta:

```python
"""rows/*.png (urutan petlib.ROWS) → hnm-pet-anoman-atlas-v02.webp + pet.json.
`--check`: tulis nihil; keluar 1 bila baris kurang, hash rows/ tak cocok manifest, atau atlas
melampaui anggaran."""
```

```python
ATLAS = ASSETS / "hnm-pet-anoman-atlas-v02.webp"
```

dan ganti komentar di `encode()`:

```python
def encode(atlas: Image.Image) -> bytes:
    buf = io.BytesIO()
    # SPEC-904 · `quality` DIPERTAHANKAN di 76 dan plafonnya yang naik (petlib.ATLAS_BUDGET).
    # Terukur atas 16 baris: q76 = 1 191 960 B, q60 = 1 081 404 B, q40 = 946 588 B, q20 = 784 084 B
    # — q76 → q20 hanya −34 %, karena atlas ini didominasi kanal alpha lossless di atas seni datar
    # berkontur tegas. Menurunkan quality demi plafon lama berarti menurunkan kualitas ketigabelas
    # baris yang sudah lolos Gate 2, untuk menghemat byte yang tak menyentuh biaya decode.
    atlas.save(buf, format="WEBP", quality=76, method=6, exact=False)
    return buf.getvalue()
```

- [ ] **Step 2: Ganti nama atlas di `verify.py`**

Di `internal/scripts/pet/verify.py`:

```python
    atlas_path = ASSETS / "hnm-pet-anoman-atlas-v02.webp"
```

- [ ] **Step 3: Rakit**

```bash
python3 internal/scripts/pet/atlas.py
```

Diharapkan: `ditulis hnm-pet-anoman-atlas-v02.webp (<N> B, 1536×3328) + pet.json` dengan
`<N> ≤ 1 300 000` (perkiraan terukur ±1 190 000 B).

Bila `FAIL: atlas <N> B > anggaran 1300000 B`: **jangan** menurunkan resolusi sel. Turunkan
`quality` di `encode()` selangkah (76 → 72 → 68) sampai muat, dan catat angka terukurnya di
`internal/assets/pet/README.md` pada Task 10.

- [ ] **Step 4: Hapus v01 dan periksa manifest**

```bash
git rm internal/assets/pet/hnm-pet-anoman-atlas-v01.webp
python3 -c "import json;m=json.load(open('internal/assets/pet/pet.json'));print(m['version'], len(m['rows']), [r['key'] for r in m['rows'][13:]], m['anchor'], m['character'])"
```

Diharapkan: `2 16 ['held', 'falling', 'dizzy'] {'x': 0.62, 'baseline': 202} {'h': 168}` — `anchor`
dan `character` **wajib** persis seperti v01.

- [ ] **Step 5: Verifikasi struktural**

```bash
python3 internal/scripts/pet/atlas.py --check && python3 internal/scripts/pet/verify.py
```

Diharapkan: `OK atlas segar, <N> B, 16 baris` lalu `OK PET-001: 16 baris, 1536×3328, <N> B`.

- [ ] **Step 6: Commit**

```bash
git add internal/scripts/pet/atlas.py internal/scripts/pet/verify.py internal/assets/pet/hnm-pet-anoman-atlas-v02.webp internal/assets/pet/pet.json
git commit -m "feat(pet): atlas v02 16 baris + pet.json version 2; v01 dibuang (SPEC-904)"
```

---

### Task 9: Frontend — `PET_ROW_KEYS` dan nama atlas

**Files:**
- Modify: `src/src/screens/pet-sprite.ts`
- Test: `src/test/pet-sprite.test.ts`, `src/test/pet-mount.test.tsx`

**Interfaces:**
- Consumes: `internal/assets/pet/pet.json` (`version: 2`, 16 baris) dan
  `internal/assets/pet/hnm-pet-anoman-atlas-v02.webp` dari Task 8.
- Produces: `PET_ROW_KEYS` berisi 16 kunci (`… "thanks", "held", "falling", "dizzy"`);
  `PET_ATLAS_URL` menunjuk berkas v02. `POSE_ROW`, `PetPose`, `parsePetManifest`, `HanomanPet.tsx`,
  dan `pet-walk.ts` **tidak berubah**.

- [ ] **Step 1: Tulis test yang gagal**

Di `src/test/pet-sprite.test.ts`, ganti judul dan isi blok `it("memuat dua baris SPEC-897 …")`
menjadi:

```typescript
  it("memuat baris SPEC-897/898/904 di ekor, indeks baris lama tak bergeser", () => {
    expect(PET_MANIFEST.version).toBe(2);
    expect(PET_MANIFEST.rows.length).toBe(16);
    expect(PET_MANIFEST.rows.map((r) => r.key).slice(10)).toEqual(
      ["deciding", "sleep", "thanks", "held", "falling", "dizzy"]);
    expect(rowIndex("wave")).toBe(9);
    expect(rowIndex("deciding")).toBe(10);
    expect(rowIndex("sleep")).toBe(11);
    // SPEC-898 · baris reaksi, bukan pose: ia hanya bisa dipilih oleh `oneShot`.
    expect(rowIndex("thanks")).toBe(12);
    expect(rowOf("thanks").loop).toBe(false);
    expect(thenOf("thanks")).toBe("idle");
    expect(Object.values(POSE_ROW)).not.toContain("thanks");
    expect(durationMs("deciding")).toBe(Math.round((8 / 6) * 1000));
    expect(durationMs("sleep")).toBe(2000);   // 8 frame @ 4 fps
    expect(rowOf("sleep").loop).toBe(true);
    expect(thenOf("sleep")).toBeNull();
  });

  it("tiga baris SPEC-904 duduk di ekor dan bukan pose", () => {
    expect(rowIndex("held")).toBe(13);
    expect(rowIndex("falling")).toBe(14);
    expect(rowIndex("dizzy")).toBe(15);
    // `held`/`falling` diputar selama seretan berlangsung → loop tanpa `then`.
    for (const key of ["held", "falling"] as const) {
      expect(rowOf(key).loop).toBe(true);
      expect(thenOf(key)).toBeNull();
      expect(durationMs(key)).toBe(1000);     // 8 frame @ 8 fps
    }
    // `dizzy` sekali putar lalu pet BERDIRI LAGI.
    expect(rowOf("dizzy").loop).toBe(false);
    expect(thenOf("dizzy")).toBe("idle");
    expect(durationMs("dizzy")).toBe(1000);
    // Ketiganya baris interaksi, bukan pose mesin: `POSE_ROW` tak menyentuhnya.
    for (const key of ["held", "falling", "dizzy"] as const) {
      expect(Object.values(POSE_ROW)).not.toContain(key);
    }
  });
```

Dan di `src/test/pet-mount.test.tsx` baris 50, ganti asersi nama berkas:

```typescript
    expect(sprite).toContain('from "../../../internal/assets/pet/hnm-pet-anoman-atlas-v02.webp?url"');
```

- [ ] **Step 2: Jalankan test, pastikan GAGAL**

```bash
pnpm vitest --run src/test/pet-sprite.test.ts src/test/pet-mount.test.tsx
```

Diharapkan: GAGAL. `pet-sprite.test.ts` melempar `pet.json tidak sah: rows: butuh 13 baris` saat
modul dimuat (manifest sudah 16 baris, `PET_ROW_KEYS` masih 13 — inilah gerbang keras butir (d)),
dan `pet-mount.test.tsx` gagal pada asersi `v02`.

- [ ] **Step 3: Tambahkan tiga kunci dan ganti import atlas**

Di `src/src/screens/pet-sprite.ts`, ganti import atlas:

```typescript
import atlasUrl from "../../../internal/assets/pet/hnm-pet-anoman-atlas-v02.webp?url";
```

dan tambahkan tiga kunci di **ekor** `PET_ROW_KEYS`:

```typescript
export const PET_ROW_KEYS = [
  "idle", "walk-right", "walk-left", "working", "waiting", "blocked", "review", "shipped",
  // SPEC-897 · dua baris baru di EKOR: indeks baris lama tak bergeser, diff atlas minimal.
  "docs-updated", "wave", "deciding", "sleep",
  // SPEC-898 · reaksi elus. BUKAN pose — `POSE_ROW` tak menyentuhnya.
  "thanks",
  // SPEC-904 · pet yang diseret: terangkat, jatuh perlahan, pusing sesaat. Di EKOR seperti dua
  // preseden di atas. Juga BUKAN pose — interaksinya dibangun backlog penerus, bukan `POSE_ROW`.
  "held", "falling", "dizzy",
] as const;
```

- [ ] **Step 4: Jalankan test, pastikan LULUS**

```bash
pnpm vitest --run src/test/pet-sprite.test.ts src/test/pet-mount.test.tsx
```

Diharapkan: dua berkas test, **semua** test lulus, dan jumlah test yang berjalan **bukan nol**
(`--changed` menyalakan `passWithNoTests`, tapi di sini path-nya disebut langsung, jadi "no test
files" berarti path-nya salah).

- [ ] **Step 5: Jalankan test pet yang lain — mereka membaca manifest yang sama**

```bash
pnpm vitest --run src/test/hanoman-pet.test.tsx src/test/pet-state.test.ts src/test/pet-walk.test.ts src/test/pet-speech.test.ts
```

Diharapkan: semua lulus. `hanoman-pet.test.tsx` mengimpor `PET_MANIFEST`/`rowIndex`/`durationMs`,
jadi ia ikut membuktikan tinggi atlas dan indeks baris tak bergeser.

- [ ] **Step 6: Typecheck paket frontend**

```bash
pnpm --filter ./src typecheck
```

Diharapkan: selesai tanpa error.

- [ ] **Step 7: Commit**

```bash
git add src/src/screens/pet-sprite.ts src/test/pet-sprite.test.ts src/test/pet-mount.test.tsx
git commit -m "feat(pet): PET_ROW_KEYS +held/falling/dizzy, atlas v02 (SPEC-904)"
```

---

### Task 10: Docs — ADR-0140 diamandemen, README aset, kontrak frontend, index

**Files:**
- Modify: `internal/docs/adr/0140-pet-sprite-codex-pipeline-berkeliaran.md`
- Modify: `internal/assets/pet/README.md`
- Modify: `internal/docs/frontend/frontend-implementation.md:373-385`
- Modify: `internal/docs/design-system/design-system.md:99`
- Modify: `internal/docs/README.md`

**Interfaces:**
- Consumes: angka terukur dari Task 1–9.
- Produces: docs yang tersentuh diperbarui + ter-link di index.

- [ ] **Step 1: Amandemen ADR-0140**

Di `internal/docs/adr/0140-pet-sprite-codex-pipeline-berkeliaran.md`, tambahkan di akhir berkas:

```markdown
## Amandemen 2026-08-22 — SPEC-904 (Pet hidup E): mode `float`, plafon 1,3 MB, atlas v02

Keputusan 1, 3, dan bagian pertama Konsekuensi diamandemen; sisanya ditegakkan.

- **Atlas naik ke `hnm-pet-anoman-atlas-v02.webp`, 16 baris** (`… thanks, held, falling, dizzy`).
  v01 **dihapus, tidak disimpan berdampingan**: ia turunan murni dari `rows/` + `petlib.ROWS` dan
  tak bisa dirakit ulang dari state repo mana pun sesudah `ROWS` bertambah, jadi menyimpannya
  berarti menyimpan 950 KB yang tak punya pembaca dan tak bisa diverifikasi `verify.py`. Namanya
  ikut naik karena `pet.json` naik ke `version: 2` dan atlas di-`import` bundler dengan `?url` —
  nama berbeda membuat mustahil ada build yang memuat manifest v2 di atas piksel v1.
- **Mode registrasi keempat: `float`**, untuk baris yang tak menapak tanah (`held`, `falling`).
  Registrasi di wilayah ATAS seperti `walk`/`jump` (yang statis adalah kepala/torso), tetapi `dy`
  diambil apa adanya dari registrasi seperti `stand`, dan tanpa pin. Memaksa piksel terendah ke
  baseline (`walk`) akan mendorong seluruh badan naik-turun mengikuti kaki yang menjuntai; busur
  `jump` diturunkan dari "tanah per baris lembar" yang di sini tak bermakna. Modenya lahir tanpa
  cabang baru di alur: `region_for` sudah mengembalikan wilayah atas untuk setiap mode non-`stand`,
  `build_strip` hanya menimpa `dy` di cabang `walk`/`jump`, dan `pin` default `mode == "stand"`.
- **Gerbang kerataan langkah** untuk baris yang diputar berulang (`even: True`): `max/min` dari
  kedelapan langkah antar-frame — **termasuk sambungan 8→1, satu-satunya langkah yang tak terlihat
  di contact sheet maupun onion-skin** — harus ≤ 3,5 saat langkah terbesarnya ≥ 0,10. Gerbangnya
  RASIO, bukan nilai mutlak: yang membuat loop tersendat adalah langkah besar yang bertetangga
  dengan langkah nyaris nol, bukan besar-kecilnya gerak. Dikalibrasi atas baris nyata yang sudah
  lolos Gate 2 — walk-left 2,06 · walk-right 2,33 · waiting 3,18 · deciding 3,43 — dan menangkap
  `wave` v01 pada **7,07** (max 0,201 tangan turun di sebelah 0,028/0,031 dua frame nyaris kembar).
  Baris bernapas halus (`sleep` 21,3 · `blocked` 6,9) dikecualikan oleh ambang 0,10.
- **`ATLAS_BUDGET` 1 000 000 → 1 300 000 B**, `quality` tetap 76. Konsekuensi ADR ini dulu berbunyi
  "+1 atlas ≤ 1 MB di bundle"; 16 baris tak muat di sana pada kualitas mana pun yang layak. Terukur:
  q76 = 1 191 960 B · q60 = 1 081 404 B · q40 = 946 588 B · q20 = 784 084 B — **`quality` adalah
  tuas yang lemah** (q76 → q20 hanya −34 %) karena atlas ini didominasi kanal alpha lossless di atas
  seni datar berkontur tegas, dan atlas dirakit ulang dari `rows/` sehingga menurunkannya menurunkan
  ketigabelas baris yang sudah lolos Gate 2. Premis plafon lama ("satu `<img>` di-decode di setiap
  halaman") juga tak dilayani `quality`: biaya decode adalah **piksel** — 19,5 MiB RGBA pada 16
  baris vs 15,8 MiB pada 13 — bukan byte. 1,3 MB = +30 % vs v01 (950 480 B), di dalam batas +40 %
  yang ditetapkan SPEC-904, dengan sisa untuk satu regenerasi baris rutin.
- **Tak berubah:** sel 192×208, 8 kolom, `anchor.baseline` 202, `character.h` 168, renderer
  (`--row` + `steps(8)`), grammar SPEC-648, dan mesin berkeliaran. `held`/`falling`/`dizzy` **bukan
  pose**: `POSE_ROW`, `PetPose`, `HanomanPet.tsx`, dan `pet-walk.ts` tak disentuh SPEC-904 — baris
  itu dipakai backlog penerus yang membangun interaksi seretnya.
```

- [ ] **Step 2: Perbarui `internal/assets/pet/README.md`**

Ganti judul, kalimat pembuka, baris tabel atlas, dan blok "Yang dikunci pengukuran":

- judul + pembuka: `13 baris` → `16 baris`, tambahkan kalimat spec E:
  "Spec E `docs/superpowers/specs/2026-08-22-spec-904-pet-atlas-v02-diseret-design.md` (SPEC-904,
  amandemen ADR-0140) menambahkan `held`, `falling`, dan `dizzy` di ekor, meregenerasi `wave` supaya
  bisa diputar berulang, dan menaikkan atlas ke **v02** (v01 dihapus, bukan disimpan berdampingan)."
- baris tabel: `hnm-pet-anoman-atlas-v01.webp` → `hnm-pet-anoman-atlas-v02.webp`.
- **ganti** butir plafon yang berbunyi "Jangan menaikkan plafon — satu `<img>` yang di-decode di
  setiap halaman adalah anggaran, bukan preferensi." dengan:

```markdown
- Plafon `ATLAS_BUDGET` **naik 1 MB → 1,3 MB pada SPEC-904**, `quality` tetap 76. Alasan lama
  ("satu `<img>` yang di-decode di setiap halaman adalah anggaran") ternyata tak dilayani
  `quality`: biaya decode adalah **piksel** (19,5 MiB RGBA pada 16 baris vs 15,8 MiB pada 13),
  bukan byte. Dan `quality` adalah tuas yang lemah di sini — 16 baris terukur q76 = 1 191 960 B ·
  q60 = 1 081 404 B · q40 = 946 588 B · q20 = 784 084 B (q76 → q20 hanya −34 %), karena atlas ini
  didominasi kanal alpha lossless di atas seni datar berkontur tegas. Bertahan di 1 MB berarti
  ±q45, dan karena atlas dirakit ulang dari `rows/`, itu menurunkan **ketigabelas** baris yang
  sudah lolos Gate 2. `alpha_quality=60` (976 678 B) ditolak karena menyisakan 24 KB — persis
  situasi "satu regenerasi rutin menembus plafon" yang dipakai menolak q78 dulu.
- **Setiap baris diskalakan sehingga bbox frame 1 = `STAND_H` (168 px)**, termasuk baris yang tak
  berdiri — terukur, ketigabelas strip v01 semuanya `h=168`, `bottom=202`. Baris yang badannya
  meringkuk karena itu akan DIPERBESAR sampai setinggi pet berdiri; naskah `falling` meminta badan
  meringkuk **tegak** dengan ekor terangkat tinggi supaya rentang vertikalnya sebanding.
- **Sambungan 8→1 tak terlihat di contact sheet maupun onion-skin.** `qa.py` karena itu mencetak
  kedelapan langkah antar-frame untuk semua baris dan menggerbangi rasio `max/min ≤ 3,5` untuk
  baris ber-`even: True` (`wave`, `held`, `falling`) saat langkah terbesarnya ≥ 0,10. `wave` v01
  mengukur 7,07 dan diregenerasi SPEC-904: frame 8 kini satu langkah sebelum frame 1 (tangan di
  pinggul), bukan salinan pose istirahat.
- **`detect_sprites` membuang blob kecil yang terpisah tanpa suara** (celah ≥ 8 kolom memisahkan,
  bbox < 40×80 px dibuang). Ornamen yang melayang lepas — bintang pusing `dizzy` — karena itu wajib
  MENYENTUH siluet karakter; kalau tidak ia hilang dari strip, atau membuat jumlah sprite ≠ 8.
```

- tambahkan di bagian "Review manusia (Gate 2 brand)" apa yang harus dibedakan secara sadar:
  `held` tergantung dengan kepala & bahu benar-benar diam dan mata membelalak di **semua** frame;
  `falling` meringkuk tegak dengan ekor terangkat (bukan bola kecil); `dizzy` berdiri dengan kaki
  tertanam dan bintang **menempel** pada jamang (bukan melayang lepas).

- [ ] **Step 3: Perbarui kontrak frontend**

Di `internal/docs/frontend/frontend-implementation.md`, blok **Atlas & manifest** (mulai baris 373):
`hnm-pet-anoman-atlas-v01.webp` → `hnm-pet-anoman-atlas-v02.webp`; **13 baris** → **16 baris** dengan
daftar kunci lengkap (`… deciding, sleep, thanks, held, falling, dizzy`); ganti kalimat plafon
("Baris ke-13 memaksa `quality` WebP turun 82 → 76 … plafon `ATLAS_BUDGET` 1 MB tak dinaikkan") dengan:

```markdown
Baris ke-13 memaksa `quality` WebP turun 82 → 76 untuk **seluruh** atlas; baris ke-14–16 (SPEC-904)
memaksa **plafon**-nya naik 1 MB → 1,3 MB dengan `quality` tetap 76, karena `quality` adalah tuas
yang lemah di atlas ini (q76 → q20 hanya −34 %) dan biaya decode mengikuti piksel, bukan byte.
Angka pengukurannya di `internal/assets/pet/README.md`.
```

dan tambahkan sesudah kalimat tentang `thanks`:

```markdown
`held`, `falling`, dan `dizzy` (SPEC-904) juga **bukan pose**: ketiganya baris interaksi untuk pet
yang diseret — terangkat, turun perlahan, pusing sesaat lalu berdiri lagi — dan dipakai backlog
penerus yang membangun interaksinya. `POSE_ROW` tetap memetakan **sepuluh** pose, kini ke enam belas
baris.
```

- [ ] **Step 4: Perbarui design system**

Di `internal/docs/design-system/design-system.md` baris 99, `10 baris` → `16 baris` (angka itu sudah
basi sejak SPEC-897/898) dan sebut atlas v02.

- [ ] **Step 5: Perbarui index Source of Truth**

Di `internal/docs/README.md`:
- entri ADR-0140 (baris 94): tambahkan `· diamandemen SPEC-904 (mode float, plafon 1,3 MB, atlas v02)`;
- entri `frontend-implementation` (baris 246): tambahkan `· Pet hidup E SPEC-904 (atlas v02)`;
- entri `design-system` (baris 243) dan `brand/illustration` (baris 18): sesuaikan bila angka baris
  atlas disebut.

Lalu periksa integritas index:

```bash
node cli/dist/index.js docs index --check 2>/dev/null || pnpm --filter ./cli exec tsx src/index.ts docs index --check
```

Diharapkan: index utuh (`OK`). Bila kedua perintah gagal karena CLI belum di-build, lewati — tak ada
berkas doc **baru** yang ditambahkan plan ini, jadi tak ada entri index baru yang wajib lahir.

- [ ] **Step 6: Commit**

```bash
git add internal/docs internal/assets/pet/README.md
git commit -m "docs(pet): ADR-0140 diamandemen SPEC-904 — mode float, plafon 1,3 MB, atlas v02"
```

---

### Task 11: Verifikasi akhir + bukti visual 16 baris

**Files:**
- Create: `internal/assets/pet/qa/atlas-v02-contact.png` (contact sheet seluruh 16 baris)

**Interfaces:**
- Consumes: seluruh keluaran Task 1–10.
- Produces: bukti visual yang dilampirkan di ringkasan sesi (butir (e) brief).

- [ ] **Step 1: Seluruh test petlib**

```bash
python3 internal/scripts/pet/test-petlib.py -v
```

Diharapkan: `OK`, tanpa satu pun `FAIL`/`ERROR`.

- [ ] **Step 2: Gerbang QA keenam belas baris**

```bash
for k in idle walk-right walk-left working waiting blocked review shipped docs-updated wave deciding sleep thanks held falling dizzy; do
  python3 internal/scripts/pet/qa.py "$k" >/dev/null 2>&1 || echo "GAGAL: $k"
done
git status --porcelain internal/assets/pet/qa
echo "selesai"
```

Diharapkan: `selesai` tanpa satu pun `GAGAL:`, dan `git status` kosong — `qa.py` menulis ulang
artefaknya tiap kali dipanggil, jadi berkas yang berubah byte-nya di sini berarti keluarannya tak
deterministik dan perlu ditelusuri, bukan diabaikan. Bila kosong, lanjut.

- [ ] **Step 3: Verifikasi struktural atlas**

```bash
python3 internal/scripts/pet/atlas.py --check && python3 internal/scripts/pet/verify.py
ls -la internal/assets/pet/hnm-pet-anoman-atlas-v0*.webp
```

Diharapkan: `OK atlas segar, <N> B, 16 baris`, `OK PET-001: 16 baris, 1536×3328, <N> B`, dan
`ls` hanya menemukan **v02** (v01 sudah dihapus).

- [ ] **Step 4: Render contact sheet seluruh 16 baris untuk review mata**

```bash
python3 - <<'PY'
from pathlib import Path
from PIL import Image, ImageDraw
import json
A = Path("internal/assets/pet")
m = json.loads((A / "pet.json").read_text())
keys = [r["key"] for r in m["rows"]]
CW, CH = m["cell"]["w"], m["cell"]["h"]
atlas = Image.open(A / "hnm-pet-anoman-atlas-v02.webp").convert("RGBA")
pad = 90
sheet = Image.new("RGBA", (pad + CW * 8, CH * len(keys)), (250, 246, 236, 255))
d = ImageDraw.Draw(sheet)
for i, k in enumerate(keys):
    sheet.alpha_composite(atlas.crop((0, i * CH, CW * 8, (i + 1) * CH)), (pad, i * CH))
    d.text((8, i * CH + CH // 2), f"{i:2d} {k}", fill=(40, 36, 28, 255))
    d.line((0, i * CH, sheet.size[0], i * CH), fill=(200, 192, 176, 255))
out = A / "qa" / "atlas-v02-contact.png"
sheet.convert("RGB").save(out)
print(out, sheet.size)
PY
```

Diharapkan: `internal/assets/pet/qa/atlas-v02-contact.png (1626, 3328)`.

- [ ] **Step 5: Review mata atas contact sheet — tiap baris berdampingan**

Buka `internal/assets/pet/qa/atlas-v02-contact.png` dan periksa:

- **indeks tak bergeser**: baris 0–12 persis seperti v01 kecuali baris 9 (`wave`) yang memang
  diregenerasi; baris 13/14/15 = `held`/`falling`/`dizzy`;
- **baseline & skala seragam**: kaki/tubuh setiap baris berdiri duduk di garis yang sama, tak ada
  baris yang jelas lebih besar/kecil;
- **karakter & palet identik**: satu mata, jamang emas, kain merah-emas, ekor besar berujung emas —
  di semua 16 baris;
- tak ada tumpahan ke sel tetangga, tak ada frame kosong.

Lampirkan berkas ini beserta `qa/{held,falling,dizzy,wave}.gif` di ringkasan sesi.

- [ ] **Step 6: Test frontend yang tersentuh**

```bash
pnpm vitest --run src/test/pet-sprite.test.ts src/test/pet-mount.test.tsx src/test/hanoman-pet.test.tsx src/test/pet-state.test.ts src/test/pet-walk.test.ts src/test/pet-speech.test.ts
```

Diharapkan: enam berkas test, semua lulus, jumlah test **bukan nol**.

- [ ] **Step 7: Typecheck**

```bash
pnpm --filter ./src typecheck
```

Diharapkan: selesai tanpa error.

- [ ] **Step 8: Diff bersih**

```bash
git status --porcelain
```

Diharapkan: kosong (semua sudah tercommit). Bila ada `internal/assets/pet/raw/…`, itu memang
di-`.gitignore` dan tak akan muncul.
