EDIT the attached sprite sheet. Keep EVERYTHING identical — same 3x2 grid, same cells, same poses, same character, same colours, same painterly style, same pure green #00FF00 background — with ONE change only:

REMOVE ALL HANGING GOLDEN CHAINS / STRINGS / THREADS from every hand. Each hand must grip ONLY its slender golden puppet control bar, with NOTHING dangling from the bar or the hand. No loops, no strands, no threads anywhere. The strings will be drawn by the app itself.

Cell contents stay: (1) body core with golden shoulder pivot discs and lap arms, (2) upper-left arm with control bar, (3) upper-right arm with control bar, (4) mid-left arm with control bar, (5) mid-right arm with control bar, (6) empty pure green.

BACKGROUND: flat uniform green #00FF00 everywhere. Do NOT remove the background.

OUTPUT: exactly ONE image via your image generation tool. No code, no files. Final message = ONLY the absolute PNG path (under ~/.codex/generated_images/) and pixel size.
