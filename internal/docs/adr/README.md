# ADR — index & riwayat keputusan

Sub-index dari [internal/docs/README.md](../README.md). Index utama menyimpan daftar **satu baris per
ADR**; berkas ini menyimpan **narasinya** — apa yang diperluas, dicabut, atau diamandemen tiap
keputusan, berikut gotcha yang harus diingat. Pemisahan ini dilakukan di SPEC-386: index utama dibaca
setiap sesi agen, riwayat ADR hanya saat seseorang butuh alasannya.
> Nomor unik & imutable. ADR usang tidak dihapus — ditandai statusnya di bawah dan di header masing-masing.
> **Satu pengecualian:** ADR-0060/0063/0070/0075 **dihapus berkasnya** di SPEC-384 atas perintah manusia
> yang eksplisit (lihat narasi 0092). Nomornya tetap tak dipakai ulang.
> **Amandemen terbaru ADR-0128 (2026-08-20, menggantikan bypass sementara yang dinarasikan di
> entri 0128):** job `test` dicabut dari seluruh CI setelah hang terus memblokir rilis;
> `validate.yml` kini hanya menjalankan `typecheck`, sedangkan `pnpm test`/`pnpm validate` tetap
> menjadi verifikasi local. `publish` tetap menunggu `validate`; pagar rilis non-test tetap utuh.
- [0160 — Navigasi dashboard lewat URL (react-router) + layar dimuat malas](0160-navigasi-dashboard-router-url.md) — **mengamandemen 0115** dan **0071**, menegakkan 0039/0145, 0110, 0117 (audit 2026-09-05). Halaman = `pathname`; `app.section` di storage tinggal dibaca saat URL tak menunjuk halaman (`/`, key mati). Hash lama dialihkan `replace`, builder deep-link memancarkan path. Dua belas layar malas di balik Suspense `gate()`; Overview/Projects/Backlog/PRD eager. Gotcha: URL diwariskan antar-test dalam satu jsdom → `setup.ts` mereset ke `/`; refresh di `/review/session/<id>` menampilkan id sebagai judul; chunk malas yang gagal diunduh sesudah deploy (SPEC-868) hanya punya `ReloadBadge` sebagai penawar
- [0159 — Custom agent native, terukur, dan terisolasi](0159-custom-agent-native-terukur-terisolasi.md) — **mencabut materialisasi Codex inline 0094**, mengamandemen default/profile builtin 0136, memperluas 0108, dan menegakkan 0002/0037 (SPEC-950). Kedua runtime kini memakai child native dari config temp sesi; Codex mensyaratkan >=0.151 dan tak pernah fallback inline. Config/hook di-mount ro ke sandbox, telemetry event memakai spool per-sesi rw yang direlay ke route bertoken. Lima execution field tersync dengan effort berkatalog runtime/model; builtin aktif tinggal tiga read-only smart, QA lama yang belum disunting dimatikan tepat sekali, dan hook read-only menolak opsi mutasi/eksekusi tersembunyi termasuk config `rg`. Lifecycle masuk `AgentInvocation` LOCAL-only; disposition admin menghasilkan precision operasional tanpa mengarang token/durasi nol. Eval 16 fixture + output beku deterministik terpisah dari live harness opt-in `pnpm agent:eval`: parent terisolasi dan skor hanya dari lifecycle child target; report dilarang di seluruh checkout; recall eval tidak dicampur dengan precision manusia. Gotcha: hook telemetry fail-open, `workspaceChanged` bukan attribution eksklusif, temp hook Codex perlu trust, dan `AgentInvocation` dilarang masuk sync
- [0158 — Urutan backlog diturunkan dari NOMOR spec, bukan dari string id-nya](0158-urutan-backlog-numerik.md) — **menegakkan 0107 & 0090**. `Spec.id` adalah string, jadi `orderBy: { id: "desc" }` di `liveSpecs` membandingkannya leksikografis: `"SPEC-999" > "SPEC-140" > "SPEC-1015"`. Begitu backlog tembus empat digit, item TERBARU jatuh ke EKOR daftar — dan karena list view dipaginasi 20/halaman (0107), ia terbaca sebagai **"spec 1000 ke atas tidak tampil"**, bukan sebagai salah urut. Terukur saat ditemukan: 16 spec (`SPEC-1000`…`SPEC-1015`) di project berisi 376 spec → halaman **19 dari 19**. Nomor berikutnya tak pernah ikut salah (`nextSpecId` sudah memakai `parseInt`), jadi ini murni urutan BACA — nol id tercetak ulang, nol data hilang. Urutan kini diturunkan dari **nomornya** dan disortir di JS, bukan di SQL: SQLite tak bisa mengurut numerik pada kolom itu tanpa kolom turunan, dan biayanya nol karena `findMany` memang memuat set penuh (filter & paginasi 0107 berjalan di layer response). Id tanpa angka jatuh ke **0, bukan NaN** — komparator ber-NaN membuat urutan tak terdefinisi, yaitu cara yang sama untuk menghilangkan item dengan sebab berbeda. Diperbaiki di `liveSpecs` (dibagi GET /specs & grup siar WS `specs`, SPEC-199) dan bukan di klien, sebab yang memotong halaman adalah server. Nol endpoint, nol migration, nol kolom
- [0157 — Papan Tim & status instance terjangkau agen: domain capability `team`, dan tool untuk route `GLOBAL_READ`](0157-mcp-papan-tim-dan-global-read.md) — **mengamandemen 0150 & 0065**, melanjutkan 0155. Dua kelompok route tanpa tool MCP, dengan sebab berbeda: papan Tim (`/api/tasks*`, `/api/members*`) jatuh ke `null` di `capabilityForRoute` sehingga tertutup bagi agen, dan empat route `GLOBAL_READ` (`/limits`, `/limits/codex`, `/update`, `/fs/browse`) yang justru sudah terjangkau SETIAP token sah tanpa satu pun capability — yang hilang cuma skemanya. Domain BARU `team:read`/`team:write` mencakup kedua permukaan sekaligus (kartu tanpa nama penanggung jawab hanyalah judul), TERSENDIRI dari `backlog` karena `status` kartu milik manusia sementara `Spec.stage` diturunkan dari fase sesi. `POST /tasks/:id/escalate` melahirkan `Spec` tapi tetap `team:write` — preseden `/tickets/:id/accept` (0062) & issue GitHub (0095): permukaan MASUK memegang capability permukaannya sendiri, dan yang menahan peluncuran tetap `launchPrincipal` (tanpa `sessions:write`, Spec-nya lahir TANPA `launchApprovedAt`). Sepuluh tool `team` + empat tool `GLOBAL_READ` → katalog **151 → 165**, capability **28 → 30**, 13 domain. `capability: null` kini punya DUA arti (tak memanggil `/api`, ATAU route `GLOBAL_READ`), jadi assert baru menuntut setiap tool null menyentuh route `GLOBAL_READ` — tanpanya sebuah tool bisa mengaku bebas capability sambil menyentuh route bergerbang, dan yang menemukannya 403 di lapangan. Gerbang cakupan diperketat: `GLOBAL_READ` tak lagi di-`continue`, dan `GET /events/ws` masuk daftar kecuali (kanal siar, bukan request-response). `POST /update/apply` tetap tanpa tool di tingkat MANA PUN. Nol endpoint, nol migration, aditif bagi token yang sudah terbit
- [0156 — Entri tersembunyi di Explorer: toggle, direktori terabaikan diruntuhkan, isi dimuat per tingkat](0156-entri-tersembunyi-explorer.md) — **memperluas 0121 & 0034**; menegakkan 0115. `git ls-files --exclude-standard` membuat `.env`, DB lokal, keluaran build, dan `.worktrees/**` — tempat kerja agen hanoman sendiri — tak pernah terlihat dari Explorer. Melepas flag itu saja bukan jawabannya: terukur di repo ini entri terabaikan berjumlah **22 686 berkas / 2,0 MB path**, hampir seluruhnya `node_modules`, dan `maxBuffer` git yang terlampaui dijawab `catch` sebagai pohon **KOSONG** — gagal senyap, bukan galat. Karena itu `--directory --no-empty-directory` meruntuhkan direktori yang seluruhnya diabaikan menjadi satu nama di `dirs` (**8 entri / 111 KB** menggantikan 2,0 MB), dan isinya dibuka **satu tingkat per klik** lewat `under=<dir>` — `readdir`, bukan git, sebab isi direktori terabaikan juga terabaikan. Penjaga path & capability `ide:read` dipakai apa adanya; bersama `ref` flag-nya diabaikan karena sebuah commit tak pernah memuat berkas terabaikan, dan togglenya mati di UI alih-alih menjawab daftar yang sama diam-diam.
- [0154 — Linimasa lintas project: amplop yang tak berbohong tentang okupansi](0154-linimasa-lintas-project.md) — **memperluas 0150 & 0153**, menutup rantai A→B→{C,D}→E; **mengamandemen 0151** (paragraf item E "hampir pasti butuh permukaan sendiri" **terbantah** — nol topik & nol fetch baru, dan plafon 200/kolom justru MENGIKAT karena task yang terpotong memendekkan amplop project); menegakkan 0115, 0131, SPEC-146 & SPEC-255. Nol kolom, nol route, nol entri sync. Penyaring project **dinonaktifkan & terlihat mati** alih-alih diabaikan diam-diam — penyaring yang tampak aktif tapi tak berlaku sekelas dengan plafon yang memotong senyap (0151) — dan nilainya **tak ditulis** sebab App pemilik tunggalnya; akibatnya mode ini SATU-SATUNYA yang memuat ulang saat berpindah, dan kedua arahnya diuji supaya tak berubah senyap. Baris project membawa **amplop + segmen per task**, bukan amplop saja: rentang polos berbohong tentang okupansi (Januari + Desember = batang selebar setahun yang tampak bertabrakan dengan segalanya) tepat pada pertanyaan yang mode ini ada untuk menjawabnya, dan urutan lukisnya diuji karena amplop yang dilukis belakangan menutupi segmennya tanpa satu pun galat. `projectSpan` → **`null`** menutup batang selebar nol & NaN di sumbernya, dengan akhir inklusif yang **tak ditambahkan dua kali** dan `invalid` yang **menular** supaya baris berisi tanggal terbalik mengaku. `barGeometry` dipisah jadi `spanGeometry` sebab rentang project tak punya `startDate` — buktinya pemisahan dan bukan penulisan ulang adalah seluruh test lama yang hijau tanpa disentuh. `TimelineCanvas` bertambah `testId` BUKAN sebagai kosmetik: dua mode yang berbagi komponen akan **lolos** cermin `SURFACES` sambil melanggar persis apa yang dijaganya. Jendela lahir dari SELURUH task, bukan baris yang terlihat, supaya membuka satu baris tak menggeser sumbu. Kunci 'tanpa project' `Symbol`, bukan string sentinel (SPEC-255)
- [0153 — Linimasa papan tim: geometri sebagai fungsi murni, dan yang tak muat tetap didaftar](0153-linimasa-gantt-papan-tim.md) — **memperluas 0150**, **mengamandemen 0151** (konsekuensi "item D tak bisa menumpang topik `tasks`" terbantah: nol topik baru, nol fetch baru, nol langganan baru — linimasa membaca `board` yang sudah dilanggan per kolom, jadi plafon 200/kolom berlaku dan tetap dirender); menegakkan 0115, 0131, dan SPEC-879/906 (SPEC-948). Nol perubahan skema, route, maupun kontrak sync — seluruh keputusannya soal hal yang gagal **senyap**. Aritmetika tanggal gagal tanpa satu pun galat sementara layout gagal terlihat, jadi `taskSpan`/`timelineWindow`/`barGeometry`/`todayOffset`/`timelineRows` hidup sebagai fungsi **murni** yang diuji tanpa jsdom, dengan `today` **selalu argumen** (fungsi yang membaca jamnya sendiri hanya bisa diuji dengan membekukan waktu global, yang di repo ini sudah pernah menjatuhkan test lain). Empat aturan yang masing-masing menutup satu kebohongan: akhir tanggal **inklusif** — tanpa `+1 hari` task sehari berlebar NOL dan kartunya seolah tak bertanggal padahal bertanggal; "belum dijadwalkan" berarti **kedua** tanggal null, sebab kartu ber-`dueDate` saja adalah **tenggat** dan tenggat justru yang dicari linimasa (menyembunyikannya membuat kanvas dan kartu papan berselisih tentang apa artinya "punya tanggal"); tenggat yang mendahului mulai **digambar + ditandai** alih-alih ditukar diam-diam, karena `zCreateTask` tak memaksa urutannya sehingga keadaan itu ADA di DB dan menukarnya menampilkan rencana yang tak pernah diketik siapa pun; dan tick berplafon **120** dengan task di luar jendela **DIDAFTAR** — plafon yang menghilangkan baris tanpa jejak adalah persis kelas bug yang sudah dijawab 0151 dengan "menampilkan N dari M", ditegakkan invarian `rows + unscheduled + outside === tasks`. **Gotcha yang terbukti mahal, ketiganya hijau palsu sampai diprobe:** `clippedStart`/`clippedEnd` yang TERTUKAR lolos dari assertion simetris `expect(a && b).toBe(true)` — yang menangkapnya dua test terpisah yang sengaja mematikan sisi lawannya; `min-width: 0` yang hanya hidup di `app.css` dijaga **nol test** karena jsdom tak memuat stylesheet, jadi properti yang menahan kelas bug SPEC-879 dipindah ke gaya **inline**; dan batangnya `<button>`, sehingga `min-height` **wajib** dinyatakan inline — `app.css` menaikkan tiap `button` ke `var(--touch-target)` 44 px di `pointer: coarse` dan di bawah 768 px, dan batang 44 px di baris 34 px meluber menimpa baris berikutnya tanpa satu pun test merah (pola yang sama sudah dibawa `SessionHistoryModal`, `TerminalScreen`, `GitGraph`, `VpsScreen`). Tick bulan satuan **kalender** sehingga tak sama lebar dalam hari — konsekuensinya dinyatakan alih-alih disembunyikan: batang diposisikan oleh **waktu** (persen), gridline oleh **piksel**, dan di zoom bulan garis grid adalah penanda bulan, bukan koordinat presisi. Aritmetika & label **UTC** di kedua sisi (`dateInputToIso` menulis tengah hari UTC). `TimelineCanvas` sengaja **tak menyebut `Task`** dan menerima `bars` **jamak** supaya item E memakainya ulang dengan baris per project, dan cermin `TEAM_VIEWS` ↔ cabang render dijaga test kontrak sejak entri kedua — entri tanpa cabang merender permukaan mode LAIN di bawah pilnya sendiri, 200 dan nol error
- [0152 — Eskalasi kartu papan tim ke backlog](0152-eskalasi-kartu-tim-ke-backlog.md) — **memperluas 0150**; menegakkan 0062/0095, 0109, 0110 & 0065, 0090 (SPEC-947). `Task.specId` sudah ada sejak 0150 tapi **tak ada satu jalur pun yang bisa mengisinya** — ia sengaja absen dari `zCreateTask`/`zPatchTask` supaya kartu tak bisa mengaku tertaut pada `Spec` yang tak pernah menyetujuinya. ADR ini adalah operasi yang ditunggu kolom itu, dan bentuknya diambil dari preseden yang sudah ada (`/tickets/:id/accept` + `/unlink`), bukan diciptakan. Tiga keputusan yang **membalik atau menyimpang** dari cermin lamanya, masing-masing dengan sebabnya: (1) teks kartu **tidak** dibungkus `UNTRUSTED_TICKET_DATA_*` — pembungkus itu ada karena tiket datang dari **publik**, sementara kartu ditulis anggota tim di dashboard ber-auth, dan memperlakukannya sebagai racun **melatih agen mengabaikan konteks yang justru sengaja diberikan**; (2) `severity` payload qa **diturunkan** dari prioritas lewat `severityFromPriority` alih-alih dihardcode `"major"` seperti `acceptTicket`/`acceptGithubIssue` — dua call site itu memang tak punya nilai prioritas di tangan, sedangkan di sini operator baru saja memilihnya di dialog yang sama, jadi menuliskan `major` untuk kartu berprioritas rendah membuang satu-satunya informasi baru; (3) tautan putus (`specId` terisi, `Spec`-nya terhapus) **disembuhkan** dengan membuat `Spec` baru — cermin `acceptGithubIssue` yang menulis `if (spec) return`, **bukan** `acceptTicket` yang menulis `spec!` dan karena itu akan mengembalikan `undefined` sebagai `Spec` lalu meledak di pemanggil, jauh dari sebabnya. Keputusan yang paling mudah terlewat justru yang paling murah: `projectId` di body **memindahkan kartunya**, karena kartu yang mengaku "tanpa project" sambil menunjuk `Spec` di dalam sebuah project takkan pernah muncul di papan project yang backlog item-nya sedang dikerjakan — satu kebenaran kedua yang drift sejak detik pertama. Penolakan kartu tanpa project **menyebut sebabnya** di server DAN di dialog: tombol mati tanpa penjelasan adalah bentuk lain dari menolak dengan diam (SPEC-546). Nol kolom, nol migration, nol entri `capabilityForRoute`/`clientRouteAllowed` — yang terakhir adalah **keputusan**, bukan kelalaian: keduanya deny-by-default, jadi menambahkan entri justru MEMBUKA route ini bagi agent token dan role `client`
- [0151 — Papan Tim berlangganan PER KOLOM, dengan plafonnya terlihat](0151-papan-tim-langganan-per-kolom.md) — **memperluas 0150**; menegakkan 0145/0039, 0107, 0115, 0127 & 0094 (SPEC-946). Ia lahir dari satu tabrakan yang tak diantisipasi dokumen desain induk: **papan tak dipaginasi** — kolom yang terpotong halaman bukan board — sementara topik `tasks` **mewajibkan** `page`/`limit` dan `zSubLimit` menjepit `limit` ke **200**. Satu langganan untuk seluruh papan bukan sekadar kurang optimal, ia bisa **dibuktikan salah**: `buildTasksPage` mengurutkan `order asc` sementara `order` bermakna **DI DALAM** kolom, jadi urutan lintas-status tak punya arti dan potongan 200 memotong himpunan gabungan empat kolom di titik yang **sewenang-wenang** — kolom mana yang terpotong, dan seberapa, tak bisa dijelaskan kepada operator maupun oleh kodenya sendiri. Karena itu **empat langganan, satu per kolom**, tiap kolom ber-`total` sendiri, dan plafonnya **dirender** ("menampilkan N dari M — persempit penyaring"): papan yang diam-diam memotong terbaca sebagai papan yang **lengkap**, dan keputusan "siapa mengerjakan apa" diambil dari apa yang terlihat — penerapan prinsip yang sama dengan lencana "basi" 0131. Muat awal HTTP wajib berparameter **identik** dengan langganannya; godaan yang ditolak adalah muat tanpa `limit` (route memang mengizinkannya) lalu menyerahkan pembaruan ke langganan berplafon — akibatnya kartu **hilang** tiga detik sesudah muncul, tanpa tindakan operator dan tanpa satu pun error. `canDropTask` adalah **pembalikan** `canDrop` board Backlog, bukan penyempitan: `Spec.stage` diturunkan dari fase sesi (0008/0024) sehingga hampir semua drop dilarang, sedangkan `Task.status` milik manusia sehingga keempat kolom saling menerima dan yang tersisa satu larangan (`from !== to`, karena drop ke kolom asal cuma melahirkan `PATCH` bernilai sama + satu baris `SyncLog` tanpa pembaca). **Gotcha yang terbukti mahal:** aturan murni saja tak cukup — `from`/`to` yang tertukar **lolos** dari unit test aturannya sendiri, jadi papan ini ikut pola `backlog-board.test.tsx` yang men-drag kartu sungguhan di jsdom; dan `TaskModal` sempat tak mengirim `order` sama sekali sehingga tiap kartu lahir `order: 0` dan "buat" menaruh kartu di ATAS sementara "drag" menaruhnya di BAWAH — dua aturan untuk satu tindakan, nol error, padahal ADR ini sudah menjanjikan `order` naik monoton per kolom. **Konsekuensi yang harus dibaca item D & E — SEPARUHNYA TERBANTAH oleh 0153:** dugaan bahwa Linimasa tak bisa menumpang topik ini "karena sumbunya tanggal, bukan `order`" **salah**; item D memakai `board` yang sudah dilanggan apa adanya, nol topik baru dan nol fetch baru, sebab yang menumpang bukan sumbunya melainkan **himpunan task**-nya — sumbu waktu lahir di klien dari tanggal yang sudah ikut di tiap baris. Yang tersisa berlaku hanya untuk item E (agregat per project bukan halaman per kolom), dan itu pun harus dibuktikan dulu sebelum topik baru dibangun.
- [0148 — Status hidup per-device disimpan di MEMORI, tak pernah menyentuh DB](0148-status-hidup-tidak-disync.md) — **menegakkan 0131, 0045, 0086 & 0024** (SPEC-919). Pertanyaan yang tersisa sesudah 0147 memutuskan transportnya: di mana keadaan itu disimpan, dan apakah ia entitas. Pola default repo untuk "keadaan milik satu mesin" adalah **tabel LOCAL-only** (`LocalBinding`, `SyncState`, `SyncOutbox`, `SyncConflict`, `SessionHistory`, `User.terminalWorkspace`); yang membuat presence berbeda adalah **kadensnya**. ADR-0131 mengukur `pollHealth()` menulis `notifySynced("vps", …)` sekali per 5 menit per VPS menjadi **121.222 baris / 213,6 MB = 83 % isi DB hub**, sampai hub tercekik `P1008 Socket timeout` berulang — dan presence berdenyut **tiap 30 detik per device**, dua orde lebih sering daripada sumber yang sudah pernah merobohkan hub produksi. Keputusannya: `Map` di memori yang **tak menyentuh Prisma sama sekali**. Ini bukan "tabel yang kebetulan tak masuk `FIELDS`" — barisnya tak pernah lahir, dan perbedaannya bukan retoris: `FIELDS`/`SYNCED`/`PARENTS`/`DATE_FIELDS`/`JSON_FIELDS`/`BOOTSTRAP_ORDER`/`PG_ORDER` adalah **tujuh daftar literal yang harus bergerak bersama** dan repo ini sudah membayar kelas gagal-senyapnya berkali-kali (0090/0093/0105/0135), jadi keadaan tanpa baris **tak bisa** masuk `SyncLog` walau kelak ada yang menambah entitas ke `SYNCED` tanpa membaca ADR ini — jaminannya struktural, bukan disiplin. **Model `DevicePresence` LOCAL-only ditolak**: nilai yang ditawarkannya ("bertahan restart") tidak ada, karena restart hub memutus socket sehingga barisnya basi sampai reconnect toh, sementara yang dibayar persis harga yang dilarang 0131. **Kolom `lastSeenAt` baru juga ditolak** — `DeviceToken.lastSeenAt` sudah ditulis best-effort `verifyDeviceToken` pada tiap request sync ter-auth, dan kanal presence sengaja **tidak** menulisnya per denyut supaya tulisan berkadens tinggi tak masuk lewat pintu belakang. `statusAt` dicap **di registry**, bukan oleh klien: satu-satunya bahan di sisi klien adalah aktivitas pane yang bergerak tiap detik, jadi signature berubah tiap denyut dan pengirim yang seharusnya diam berubah jadi banjir frame — dan karena sesi mesin hub masuk lewat pintu yang sama (0147 §8), rumus itu berlaku seragam. Keadaan basi punah lewat dua jalur tanpa timer: socket putus → `dropPresence` seketika, denyut berhenti > `PRESENCE_OFFLINE_MS` 90 dtk (3× denyut) → disapu **saat dibaca**. Registry kosong sesudah restart adalah **fitur** (terisi lagi ≤ 30 dtk). Batasnya: tak ada riwayat (`SessionResult`/`SessionHistory` yang memegang jejak eksekusi), dan keputusan ini **mengasumsikan satu proses server** (0086) — membelah server jadi beberapa proses akan membelah registry ini bersamanya dan menuntut ADR baru. Presence murni informasional: tak menggerbangi start sesi, worktree, auto-merge, scheduler, maupun lead (batas yang sama dengan 0135 §6, dengan alasan tambahan bahwa keadaan yang punah sendiri tak boleh jadi prasyarat keputusan yang harus deterministik)
- [0147 — Presence sesi lintas device: arah naik di socket sync yang sudah ada](0147-kanal-presence-di-socket-sync.md) — **mengamandemen 0046** (`GET /api/sync/ws` tak lagi satu arah); menegakkan 0043, 0044/0117, 0024, 0039/0145, dan kontras dengan 0135 (SPEC-919). Sync memindahkan **record**, tak pernah sesi — dan itu bukan kelalaian: komentar `SessionHistory` di schema menuliskan alasannya ("sesi hidup di tmux mesin ini dan transkripnya berkas di disk mesin ini; menyiarkannya akan mengirim baris yang menunjuk berkas yang tak ada di sana"). Akibatnya hub buta: `GET /terminal/sessions` membaca tmux mesinnya sendiri, dan tak ada kolom yang menyimpan "sesi ini hidup di device mana", sehingga spec ber-stage `executing` bisa sedang dikerjakan di laptop lain tanpa satu jejak pun. Tiga bahan sudah ada dan belum pernah disambung: `DeviceToken{name,lastSeenAt}`, bit `decision` SPEC-903, dan — yang menentukan — **socket persisten klien→hub yang SUDAH dipegang klien** (`sync-client.ts` → `ws://<hub>/api/sync/ws`, Bearer, lengkap dengan reconnect); yang kurang hanya arah naiknya. Keputusannya **satu jenis frame masuk** `{t:"presence", v:1, sessions[]}` bersemantik **ganti-penuh** (tak ada frame "hapus" yang bisa hilang; re-kirim saat reconnect identik dengan pengiriman pertama), cermin apa yang 0145 lakukan pada `/events/ws`. **Alasan yang menentukan bukan hemat koneksi melainkan kompatibilitas mundur yang tak perlu ditulis:** hub versi lama **tak memasang `socket.on("message")` sama sekali**, jadi frame klien baru jatuh ke lantai tanpa satu error dan sync jalan terus — terpenuhi *by construction*, bukan oleh kode versi yang harus dirawat. **Kanal terpisah ditolak**: 0046 memisahkan `/sync/ws` dari `/events/ws` karena **otorisasinya berbeda** (token vs cookie), sedangkan di sini otorisasinya identik (device token, principal, `revalidateWsPrincipal` 60 dtk yang sama) — yang dipisahkan hanya jumlah socket, yang ditambahkan adalah penanganan `404` pada upgrade, backoff kedua, kuota koneksi kedua, jalur auth kedua. **`POST` berkala ditolak** (polling keluar dari tiap klien selamanya). Muatannya RINGKAS dan zod **`.strict()`** — kunci asing DITOLAK, jadi "tak ada isi terminal" adalah properti yang dijaga test; **`cwd` dibuang** (persis alasan `SessionHistory` LOCAL-only), dan judul/nama/stage tak dikirim karena hub sudah memegang `Spec`/`Project` tersync — **kebalikan sadar dari `HandledByEntry` (0135)** yang HARUS membawa snapshot `name` justru karena penerimanya client tanpa katalog device. Kosakata status memakai bit yang **sudah ada** (`waiting` = `SessionInfo.decision` apa adanya, `exited` = `pane_dead`), presedensi `exited > waiting > working`. `startedAt` menuntut sumber baru → `#{session_created}` ditambahkan di **ujung** `FMT` `pty.ts` (nol invokasi tmux tambahan, posisi kolom lama tak bergeser — persis cara `#{window_activity}` masuk di SPEC-903) dan mendarat di `Pane`, **bukan** `SessionInfo` (cermin `activityAt`/`eventHook`: bahan presence, bukan DTO yang disiarkan). **Empat pagar supaya kanal status tak menjatuhkan sync**, karena socket ini mengangkut changefeed: handler `message` seluruhnya dalam `try/catch`; frame rusak/tak lolos skema **diabaikan**; verdict `WsMessageGuard` (60/menit) **diabaikan**, bukan diterjemahkan jadi `close 1008/1009` seperti di `/events/ws` yang socket-nya milik dashboard; dan `MAX_PRESENCE_SESSIONS` = 100 dipotong **di sisi pengirim**, karena `maxPayload` 64 KiB ditegakkan `ws` **sebelum** handler kita sempat mengabaikan apa pun (100 × ±200 B ≈ 20 KB, margin 3×). Pengirim di klien menerima `send` sebagai **argumen** — modul itu tak memegang socket dan karena itu tak bisa menutupnya, properti yang terbaca dari tipenya. `deviceId` **selalu** dari `req.wsPrincipal.id`, tak pernah dari payload (klien memang tak tahu `deviceId`-nya sendiri, dan itu sifat yang benar). Reconnect ikut naik jadi **backoff 1→30 dtk ber-jitter ±20 %** menggantikan `setTimeout(…, 3000)` datar yang mengetuk hub mati 20×/menit — sekaligus menambal cacat yang sudah ada: **timernya tak pernah dibatalkan**, sehingga `applySyncConfig()` (stop lalu start) meninggalkan socket yatim yang menyambung memakai token **lama**. Ke layar lewat **grup siar GLOBAL ke-10 `presence`** (3 dtk, dedup signature yang sudah ada) — grup, bukan topik 0145, karena muatannya tak berparameter; `GET /api/presence` **COOKIE_ONLY** (non-delegatable: ia memaparkan peta pekerjaan lintas mesin) hanya muat awal + fallback, dan ia dipanggil dari `ClientsScreen` **bukan** `load()` App — menambah satu panggilan `api` di `load()` mematahkan **20 test ber-mock parsial sekaligus** (terukur; kelas jebakan SPEC-884). Gerbang **`enabled`** = hub punya ≥1 `DeviceToken` belum dicabut, sehingga instalasi satu mesin **nol perubahan tampilan**; gerbang nav-nya `NavItem.gate` + context `NavGate` (bukan prop — `Shell` dirender di belasan cabang `App` dan prop akan meninggalkan cabang yang terlewat), dan ujung terakhirnya `PresenceChip` yang merender **nol elemen** untuk daftar nama kosong. Sesi mesin hub masuk lewat pintu `recordPresence` yang **sama** supaya `statusAt` punya satu rumus; `activeSpecs` di App tetap berarti hal **lain** ("ada sesi di MESIN INI yang bisa dibuka" — menggerbangi tombol, bukan penanda). Plafon yang diketahui: data mengalir **satu arah klien→hub** (client tak bisa menampilkan sesi hub maupun client lain; fan-out balik menggandakan permukaan untuk kebutuhan yang tak diminta), **menempel terminal sesi klien dari hub ada DI LUAR** keputusan ini (butuh relay WS lintas instance + gerbang auth sendiri), presence **murni informasional** dan tak boleh dibaca jalur eksekusi tanpa ADR baru, dan `WsMessageGuard` memakai jendela **fixed** sehingga 60 frame/menit adalah polisi tidur — yang benar-benar mengikat adalah `MAX_PRESENCE_SESSIONS` dan `maxPayload`
- [0146 — Pintu deteksi hanoman-lead dipicu event hook, bukan denyut](0146-lead-dipicu-event-hook.md) — **mengamandemen 0091 §5** (dua irama → satu irama rumah tangga + satu jalur event) dan §2 pintu #2 (lead tak lagi menyimpulkan pertanyaan claude dari layar); **menegakkan 0024** (jumlah timer BERKURANG — `TICK_MS` 5 dtk dicabut, sisanya `HOUSEKEEPING_MS` 60 dtk; tanpa queue/worker/cron), **0039** (frame `leadAsks` menumpang kanal siar yang sudah ada, tanpa WS kedua), **0102** (`LeadFlow` & rantai utuh), **0142** (`screenHash`/`deciding`/`answering`), **0143** (arti & pemilik marker tak disentuh), **0037**, **0065**, **0087** (SPEC-909). Yang selama ini dianggap "setengah tick" ternyata punya lapis kedua yang lebih besar dan lebih tua: hook `Notification` claude bukan hook `AskUserQuestion` — ia menembak dari pengait **idle 6 detik** yang dipasang tiap dialog, terukur **6 071 ms** & **6 023 ms** SESUDAH `PreToolUse(AskUserQuestion)`, dan setengah tick menumpuk di ATASNYA. Hook `PreToolUse` ber-matcher `AskUserQuestion` (claude) dan perintah `Stop` kedua (codex) kini MENGIRIM payloadnya apa adanya ke `POST /api/session-events`; terukur end-to-end **32/46/164 ms** (median 46) dari `AskUserQuestion` sampai lead mulai menyusun, dan **nol** `capture-pane` saat tak ada yang bertanya. Kredensialnya **token turunan HMAC per sesi** (stateless lintas restart, id sesi saja tak pernah cukup), prefix-nya SENDIRI karena `/api/lead` dipetakan `rw("lead")` — agent token pemegang `lead:write` di sana bisa MEMALSUKAN pertanyaan atas nama sesi mana pun. Payload membuat panjang rantai DIKETAHUI (satu panggilan = satu event, berapa pun pertanyaannya), jadi `CHAIN_END_TRIES`/`settledPane` dicabut; yang tersisa dari layar cuma tiga langkah mekanis di dalam rantai yang sudah dipicu event. Codex justru menguat: gerbang `ASK_SIGNALS` kini menilai `last_assistant_message` yang TIDAK dipotong lebar pane. Pet menampilkan pertanyaan aslinya seketika lewat grup siar `leadAsks`, dan operator bisa MENGAMBIL ALIH — pemenang perebutannya ditentukan `beginAnswer()`, `Set` yang sama yang mencegah dua POST manusia menyilangkan keystroke, dan yang kini juga dilewati jalur lead. Gotcha termahal: `PreToolUse` menembak **sebelum** tool-nya jalan (dialognya belum tergambar → `decide()` dulu, tunggu layar belakangan, dan `waitDialog` yang habis berarti BATAL bukan jatuh ke prosa); hook berkode keluar 2 MEMBLOKIR tool-nya (`exit 0` itu syarat, bukan kerapian); dan `submitPaneDialog` fail-closed untuk layar yang bukan rekap sementara dialog SATU pertanyaan claude tak pernah punya layar rekap — menekan Submit tanpa syarat membuat kasus PALING UMUM dilaporkan `gagal` sampai `failCapped`, dan panggung fixture yang tak pernah BERAKHIR menyembunyikannya. Sesi yang lahir sebelum pembaruan **tidak** dilayani dan dikatakan lewat satu notifikasi seumur sesi (penandanya opsi window tmux `@hanoman_event_hook`).
- [0145 — Data realtime berparameter lewat langganan di `/events/ws`, bukan polling klien](0145-langganan-berparameter-events-ws.md) — **mengamandemen 0039 pada dua sifatnya (kanal tak lagi read-only; snapshot global bukan lagi satu-satunya bentuk frame); menegakkan 0024, 0087, 0107, 0115, 0134 & 0014/0016** (SPEC-908). ADR-0039 memindahkan polling ke server untuk **delapan grup snapshot GLOBAL tanpa parameter** (`sessions`, `specs`, `notifications`, `cleanups`, `vps`, `limits`, `codexLimits`, `update`); empat layar tak pernah ikut dan sampai sini masih men-poll sendiri di tiap tab — Scheduler 5 dtk (plus empat seksi antrean lewat penanda `nonce` SPEC-523), Triage 5 dtk, Lead 5 dtk (tiga panggilan), GitGraph 4 dtk (tiga panggilan). Akarnya batas desain, bukan kelalaian, dan ada tiga: muatan keempatnya **fungsi dari parameter klien** sementara `GROUPS` menyiarkan **string yang sama** ke semua klien; kanalnya read-only secara harfiah (`routes/events.ts` tak pernah memasang `socket.on("message")`, `Client` hub cuma `{send, close}` — **nol state per-koneksi**); dan skip "tak ada yang butuh" bekerja di level loop (`stopLoop()`), bukan per-grup. Keputusannya: **satu** jenis frame masuk `{t:"sub", subs:[{topic, params}]}` bersemantik **ganti-penuh** — karena itu tak ada frame `unsubscribe` yang bisa hilang dan re-kirim saat reconnect identik dengan pemasangan pertama (idempoten by construction) — dengan **lima topik** yang masing-masing cermin satu `load()` layar: `schedulerState`, `schedulerQueue`, `tickets`, `lead`, `git`. Nama topik **identik** dengan `t` frame keluarnya (satu-ke-satu, tak ada peta kedua yang bisa berselisih), dan `lead`/`git` sengaja **dibundel** karena keduanya hari ini satu `Promise.all` yang menyetel tiga state sekaligus — tiga frame terpisah akan menampilkan campuran dua generasi data yang hari ini tak mungkin terjadi. `key` dihitung **kedua sisi** lewat `subKey(topic, params)` di `@hanoman/shared`: kalau id datang dari klien, dua tab berparameter identik menerima frame **berbeda byte** dan dedup signature 0039 hilang. **Invalidasi/versi ditolak** walau lebih murah dan menjaga kanal read-only — ia menyisakan satu round-trip HTTP tiap perubahan dan **menggandakan sumber kebenaran bentuk muatan**, dan untuk domain termahal (git per project) versinya praktis **sama mahalnya dengan datanya** (menentukan "apakah graph berubah" = membaca HEAD + seluruh ref + status kotor, hampir seluruh `ideStatus`); varian mtime `.git/**` ditolak karena biayanya tumbuh mengikuti **jumlah project terdaftar** bukan yang ditonton, `git status` yang kita jalankan sendiri mengubah mtime `.git/index` (risiko **loop muat-ulang**), dan kotor/bersih working tree tak tertangkap mtime ref sama sekali. **Tujuh pagar biaya**, dua terakhir lahir dari review keamanan yang mengukur jalurnya: entri hanya lahir dari frame `sub` dan **mati saat pelanggan terakhirnya lepas**; dedup signature `Group.last` dipertahankan per entri; satu hitungan untuk N klien; kadens per-topik semuanya ≤ yang digantikan (2/3/3/4/4 dtk vs 5/5/5/5/4 dtk); **tak pernah memblokir event loop** — `runEntry` di-tick tanpa `await`, semua `build` async, dan `listSessions()` ber-`execFileSync` (terukur 6,28–916 ms saat mesin sibuk) diganti `listSessionsAsync` di jalur ini sekaligus di `GET /scheduler/state` & `GET /lead/status`; **`MAX_INFLIGHT` = 4** build serentak se-hub karena `e.inflight` hanya men-dedup DI DALAM satu entri dan entri baru selalu lahir `inflight:false` — terukur **32 `buildGitLive` serentak menahan event loop 505 ms**, event loop yang sama yang melayani PTY terminal (kelas regresi SPEC-812/878); dan **jatah 30 build-di-luar-jadwal per menit per klien**, karena semantik ganti-penuh membuat tiap frame `sub` bisa memperkenalkan 16 kunci "baru" lagi sehingga kuota 120 frame/menit terukur menjadi **1 920 build/menit dari satu socket** (≈160 fork `git`/dtk) — entri yang kehabisan jatah **tidak hilang**, ia menunggu slot `everyTicks`-nya. Permukaan masuknya digerbangi: **hanya principal cookie** yang boleh berlangganan (`canSubscribeTopics`, `kind === "user"`, plus `"test"` di bawah `NODE_ENV=test`) — bukan kehati-hatian umum, sebab `/events/ws` dipetakan ke capability **`GLOBAL_READ`** untuk agent token (0065) sementara topiknya menyentuh domain `support`, `lead`, dan `ide`, jadi tanpa gerbang ini satu agent token ber-read global memperoleh baca ke tiga domain yang tak diberikan kepadanya (dashboard satu-satunya konsumen → nol fungsi hilang; kedelapan grup lama tetap dilayani principal mana pun); `MAX_WS_MESSAGE_BYTES` 64 KiB, `subs` ≤ 16, parameter zod `.strict()` (kunci asing **ditolak**, bukan diabaikan), `limit` dijepit ke plafon yang sama dengan route HTTP-nya (0107 — untuk `git` plafon topik justru lebih ketat daripada route HTTP yang tak punya batas atas sama sekali), `WsMessageGuard` 120 frame/menit; **topik tak dikenal dilewati PER-ENTRI** bukan menjatuhkan frame, karena dashboard boleh lebih baru daripada server (0087); `admitBrowserWs` + `revalidateWsPrincipal` 60 dtk apa adanya. Gotcha yang dicatat di komentar fungsinya: portal klien tertutup **di lapisan lain** (allowlist `clientRouteAllowed` menolak `/api/events/ws` **dan** `/api/ws-tickets` sebelum upgrade) — `canSubscribeTopics` sendiri **tidak** membedakan role, jadi kalau allowlist itu suatu hari membuka `events`, satu akun klien langsung memperoleh baca `tickets`/`lead`/`git` lintas project. Degradasinya bersandar pada **ketiadaan** sinyal: server mengirim `{t:"hello", topics}` saat attach dan **server lama tak mengirimnya sama sekali** (0087), jadi `setInterval` fallback menyala hanya pada dua keadaan yang bisa **dibuktikan** — `hello` tiba tanpa topik itu di dalamnya, atau socket belum mengantar satu frame pun selama 15 dtk (WS terhalang proxy padahal HTTP hidup). Selama WS sehat: **nol `setInterval`, nol request HTTP berkala**; saat putus sesaat tak ada polling sama sekali — reconnect ber-backoff memulihkan lewat muatan-pertama-seketika dan operator melihat `LiveConnectionBadge` sesudah grace 6 dtk, sementara `paused` (tab tersembunyi, socket ditutup **atas permintaan kita**) tak pernah memunculkan indikator. Konsekuensinya: body enam route diekstrak ke service (`scheduler/state.ts`, `scheduler/queue.ts`, `tickets-list.ts`, `lead/views.ts`, `git-ide.ts`, `repo-dir.ts`) lalu dipanggil route **dan** hub — menyalin serializer ke call site kedua adalah kelas bug SPEC-431/448/475; `GraphCommit`/`RepoStatus`/`Stash` pindah ke `@hanoman/shared` (dulu KEMBAR di `services/git-ide.ts` dan `api/client.ts` tanpa ikatan tipe apa pun); `WireMsg` disempitkan pada `t`-nya, yang sebelumnya `{t: string; …}` sehingga `t` salah ketik lolos typecheck server lalu jatuh senyap di klien; penanda `nonce` SPEC-523 **dicabut** (tiap `QueueSection` berlangganan (status, halaman)-nya sendiri); **tak satu pun endpoint HTTP dihapus** (MCP, agent token, portal klien, dashboard lama memakainya, dan ia tetap jalur muat awal + fallback); kuota koneksi tak naik karena semua langganan multipleks di socket yang sudah ada. Plafon yang diketahui dan sengaja ditinggalkan: `buildTicketsPage` masih memuat **seluruh** tabel `Ticket` lalu menyaring `q` di memori (`findMany` tanpa `take`) — amplifikasi terburuknya ditutup dengan debounce 400 ms pada `q` yang menyuapi kunci langganan (mengetik tak lagi melahirkan satu scan per huruf), tetapi steady-state tetap satu scan per 3 dtk selama layar Triase terbuka dan skalanya ikut jumlah tiket; memindahkan `q` ke `where` **mengubah semantik pencarian** (`LIKE` SQLite case-insensitive hanya untuk ASCII, wildcard `%`/`_` di dalam `q` jadi bermakna, pencarian tak lagi menjangkau batas judul↔email) jadi ia butuh keputusannya sendiri. Satu layar Scheduler membuka **lima** langganan (plafon 16 memberi ruang; frame `sub` ter-coalesce di microtask menjaga jumlah frame masuk tetap satu), dan `WsMessageGuard` memakai jendela **fixed** bukan sliding sementara `POST /api/ws-tickets` tak dibatasi laju — jadi 120 frame/menit adalah polisi tidur, bukan plafon keras; yang benar-benar mengikat kerja adalah `MAX_INFLIGHT` dan jatah per klien
- [0144 — Pet yang diseret: sumbu Y di jalur pet, tiga mode fisika di mesin murni, jalur yang melebar hanya selagi terangkat](0144-pet-diseret-sumbu-y-jalur-melebar.md) — **mengamandemen 0140 pada mesin berkeliaran & bentuk jalur; menegakkan 0039, 0024, 0037 dan grammar SPEC-648** (SPEC-905). Pet hidup di jalur `fixed` setinggi SATU sel dan satu-satunya penulis posisinya `stepWalk`, pada SATU sumbu; masukan manusia yang ada hanya bisa MENJEDA. Baris `held`/`falling`/`dizzy` sudah ada di atlas v02 sejak SPEC-904 dan sengaja tanpa pemakai. Sumbu Y + tiga mode karena itu masuk ke mesin MURNI, bukan ke komponen: keadaan seret masuk sebagai MASUKAN (`dragging`/`pointerX`/`pointerY`/`laneHeight`/`petHeight`), `PetMove` menjadi `{x,y,durationMs,ease}`, dan ketiga cabangnya duduk di KEPALA `stepWalk` di atas `anchored()` — sebuah `waiting` yang muncul di tengah jatuh tak boleh menelan jatuhnya. `parkedX` adalah TITIK jangkarnya; predikat `anchored()` tak berubah satu karakter pun, dan **seret nyala di SEMUA tier termasuk mobile** karena `anchored()` melarang gerak OTONOM, bukan manipulasi langsung yang diminta manusia — tanpa `parkedX`, justru pengguna "Diam di pojok", `prefers-reduced-motion`, dan mobile yang pet-nya melompat balik ke pojok begitu dilepas, sehingga seret sia-sia persis di sana. Rasio berkeliaran dibalik (`STAND_MS` [4000,12000]→[1200,4500], `WALK_MS` [2000,6000]→[5000,14000]: ±33 % → **±72 % berjalan**) dengan `WALK_PX_PER_S` TETAP 40 — yang kurang adalah durasinya, bukan lajunya, dan menaikkan laju mengubah karakter berjalan yang sudah lolos Gate 2. Jalur melebar ke `top: max(0px, var(--safe-top))` HANYA selagi `held`/`falling` dan tepi BAWAHnya tak bergerak, jadi sprite/panel/gelembung tak bergeser satu piksel pun; **alasannya BUKAN clipping** — terukur, `pet-root` ber-`overflow: visible` dan sprite 300 px di atas jalur tetap tergambar DAN tetap dijawab `elementFromPoint` sebagai `pet-hit`, jadi premis brief terbantah dan dicatat supaya pembaca berikutnya tak mencari clipping yang tak pernah ada. Yang dibeli pelebaran itu adalah **plafon angkat yang bisa DIUKUR dari CSS-nya sendiri**: `getComputedStyle(...).getPropertyValue("--safe-top")` mengembalikan token `env(...)` yang belum di-resolve, jadi satu-satunya cara menghormatinya adalah mengukur elemen yang memakainya. Satu properti `transform` untuk kedua sumbu — juga saat `y = 0`, karena daftar properti yang di-transisi tak boleh berganti di tengah rantai berjalan→diangkat→jatuh→mendarat; `ease: "fall"` = `cubic-bezier(0.55,0.085,0.68,0.53)` (easeInQuad, percepatan). Dua konsekuensi yang tak terbaca dari kodenya: `cut()` menyempit ke `walk`/`home` (memotong transisi pet yang sedang jatuh = MENGHAPUS jatuhnya) dan `land()` SELALU memancarkan `y → 0` termasuk di jalur `reduced` yang tak pernah melewati `falling` (komponen menyimpan `move` terakhir, jadi `move: null` meninggalkan sprite DI UDARA). `dizzy` kembali ke pose lewat `until` mesin, BUKAN lewat `thenOf` — manifest menulis `then: "idle"` dan rantai itu berbohong saat pose mesinnya `working`; durasinya tetap dari manifest (1000 ms). Lambaian menetap dengan keputusan lanjut/berhenti HANYA di `animationend`, yaitu tepat di batas putaran, sehingga lepas hover tak pernah memotongnya; `oneShot.id` wajib penghitung naik karena `key` React `${row}:${id}` yang identik membuat animasinya tak restart dan lambaian berhenti diam-diam. Terukur di Chrome headless lewat CDP di DUA viewport: 1280×800 rantai `walk-left`→`held`→`falling`→`dizzy`→`idle` tanpa baris menyelip, jalur 139→800 px lalu kembali 139 px tepat saat mendarat, plafon berhenti persis di 661 px = 800−139, jatuh 300 px = `transform 1.25s` dengan Y 298,7→280,9→0 monoton, dan pet berdiri di x 888,0 (tempat ia dilepas) sementara pojoknya di 1136; 390×844 identik dengan jalur 119→844, plafon 725, dan pet yang dijatuhkan di x 64 TETAP di 64 alih-alih kembali ke rumahnya di 264. `elementFromPoint` di tengah layar mengembalikan konten dashboard di SETIAP langkah, termasuk selagi jalur melebar. **Dua jebakan jsdom yang membuat test seret hijau palsu:** `globalThis.PointerEvent` TAK ADA di jsdom 24 sehingga `fireEvent.pointerDown(el,{clientX:10})` jatuh ke `Event` polos dan handler menerima `clientX === null` (penawarnya `new MouseEvent("pointerdown",{clientX,clientY})` + `pointerId` lewat `defineProperty`), dan `setPointerCapture`/`releasePointerCapture` juga tak ada sehingga komponen wajib memanggilnya opsional. Konsekuensi ketiga yang menyelamatkan matematikanya: koordinat seret dihitung sebagai SELISIH (`dx = clientX − currentX()`, `dy = clientY + y`), bukan dari `getBoundingClientRect()` — rect nol jsdom hilang dengan sendirinya di aritmetika selisih. `touch-action`/`user-select` pindah ke kelas `.hn-pet-hit` karena jsdom MENJATUHKAN keduanya dari inline style secara senyap; permukaan tap tetap 44×44 px (SPEC-763), dan konsekuensi yang diterima adalah sapuan vertikal yang dimulai tepat di atasnya menjadi seret, bukan gulir. Tanpa persistensi posisi antar-muat halaman; `held`/`falling`/`dizzy` tetap BUKAN pose
- [0143 — "Menunggu keputusan" adalah keadaan TURUNAN dari keadaan pane, bukan latch marker](0143-menunggu-keputusan-keadaan-turunan.md) — **mengamandemen SPEC-184 pada arti marker & 0141 pada turunan `decisionAt`; menegakkan 0039, 0024, 0091 & 0037** (SPEC-903). `SessionInfo.decision` mengaku keadaan ("sesi ini sedang menunggu manusia") padahal membaca pemberitahuan ("pernah minta masukan"): satu bit dari ukuran marker `.worktrees/.decisions/<id>`. Marker dipasang hook agen (claude `Notification`, codex `Stop` — codex tak punya `Notification`) dan dilepas HANYA `UserPromptSubmit` + rantai lead, jadi empat jalur keluar tetap terbuka: dialog `AskUserQuestion`/izin dijawab di TUI (tool result, bukan prompt), `POST /terminal/sessions/:id/dialog/answer` (0142) yang tak menyentuh marker, Esc, dan codex yang melanjutkan sendiri. Terukur 2026-08-22: dua pane memutar `✢ Creating… (28m 3s · ↓ 112.3k tokens)` dan `✶ Manifesting… (25m 12s)` dengan marker keduanya terisi. Menambal per-jalur tak akan selesai — daftarnya bertambah tiap ada permukaan baru — jadi `decision` menjadi TURUNAN: `markerFilled && paneQuiet(#{window_activity})`, `PANE_QUIET_MS = 3000`, fail-open (aktivitas tak terbaca → dibaca diam → perilaku lama). Sumbernya menumpang `FMT` milik `tmux list-panes -a` yang sudah dipanggil tiap poll (pola `#{alternate_on}`, 0133) → **nol invokasi tmux tambahan**; biaya terukur 4,12 → 4,33 ms/panggilan bahkan dengan sebuah pencarian isi pane `#{C/ri:}` ikut serta. Pemisahannya terukur **0 dtk vs 317 dtk** (22 sampel 1 Hz: pane bekerja `window_activity == now` 22/22 karena timer giliran berdetak tiap detik; pane diam di prompt beku 317 dtk), dan `window_activity` **berdetak tanpa klien tmux terpasang** — penting karena hanoman baru memasang klien saat ada yang menonton. Gerbang berbasis ISI pane ditolak walau `#{C/ri:}` tersedia di tmux 3.7b: lebar pane 52 kolom sudah memotong `esc to interrupt`, bentuknya kontrak tampilan agen yang berganti tiap rilis, dan footer dialog sebagai gerbang positif justru MENAHAN pil menyala di jalur yang paling sering — sesudah opsi dipilih footer bisa masih terlihat sementara agen sudah bekerja. Mengosongkan marker karena pane terbaca sibuk juga ditolak, dan ini yang paling penting: `Notification` mengisi marker SEKALI per dialog dan tak pernah menembak lagi (terukur 0 B selama 120 dtk dengan dialog terbuka), jadi satu keluaran latar belakang akan menghilangkan pertanyaan itu PERMANEN. Marker tetap sinyal masuk durable; penulis dari sisi server tetap dua dan keduanya bukti positif manusia menjawab — rantai lead (SPEC-452) dan route `dialog/answer` yang ditambahkan di sini, satu definisi `clearMarker()`. `decisionAt` ikut diturunkan (`max(onset marker, window_activity)`) karena satu episode marker kini bisa memuat beberapa episode menunggu dan `PET_URGENT_MS` akan menjerit palsu; ISI marker tak disentuh. `liveDecisions().waiting` menyeragamkan notifikasi & panel lead, sementara dedup notifikasi tetap dikunci pada MARKER supaya kedipan (manusia mengetik jawabannya) tak melahirkan notif kedua. Pintu deteksi lead tak digerbangi ulang: `AGENT_TURN_LINE` (SPEC-487, 6/6 vs 0/16) sudah lebih kuat. Bentuk DTO tak berubah → nol migrasi, nol dampak sync; `deciding` (0091) & gerbang SPEC-433 tetap menang di atasnya; sesi yang sudah berjalan ikut terperbaiki tanpa dilahirkan ulang karena tak satu pun hook agen berubah
- [0142 — Inbox keputusan: dialog sesi dibaca & dijawab lewat HTTP bergerbang `screenHash`, di luar katalog MCP](0142-inbox-keputusan-dialog-sesi.md) — **menegakkan 0039, 0037, 0065, 0091, 0099 & 0102; tak mencabut apa pun** (SPEC-899). `tui-dialog.ts` sudah memiliki SELURUH mekanisme menjawab dialog `AskUserQuestion` — parse `ChoiceDialog` + primitif `PaneIO` + empat jebakan terukur (burst > 1 karakter ditelan, digit memilih seketika, kolom bebas di nomor `opsi+1`, `Submit` multiSelect tanpa nomor) — tetapi hanya `hanoman-lead` yang memakainya; panel pet menunjuk sesi `waiting` dan cuma bisa menawarkan "Buka Terminal". `steer` tak cukup: ia tak pernah mengembalikan bentuk layar, tak punya gerbang kesegaran untuk aksi yang lahir dari sebuah DAFTAR, dan `resolveChoices`-nya fuzzy — benar untuk agen yang menalar dengan bahasa, mubazir untuk manusia yang menunjuk baris nomor sekian. Karena itu dua endpoint di bawah prefix `/terminal` yang sudah dipetakan `rw("sessions")` (nol perubahan peta capability, method yang menurunkan read/write — pola anti-SPEC-405), dengan `screenHash = sha256(dialogKey(paneText))` sebagai gerbang kesegaran. Hash itu SENGAJA bukan hash teks pane mentah: pane memuat kursor berkedip & spinner, jadi hash atasnya berbeda antar dua capture berturut-turut dan setiap jawaban akan ditolak 409 — sementara `dialogKey` sudah membuang label kolom bebas (SPEC-474) dan `☐/☒` tab strip layar multi (gotcha 0102 #1), tepat dua sifat yang dibutuhkan. Dispatch-nya CERMIN `sendToPane` tanpa cabang baru, dan `pty.ts` hanya MENGEKSPOR primitif pane-nya (`dialogIO` → `paneIO`) — dua titik tulis yang tak sepakat adalah pola kegagalan SPEC-431/448. Layar tanpa kolom bebas & tanpa kolom catatan (dialog trust, prompt izin) dan layar rekap rantai **tak pernah** dilaporkan bisa dijawab: tombol dashboard yang menjawab prompt izin adalah kebalikan penuh dari batas 0037. Dua gerbang tulis: `isDeciding` → 409 (0091 apa adanya) dan kunci in-flight per sesi → 409 (dua POST berbarengan menyilangkan keystroke jadi sampah). **Di luar katalog MCP** karena ia menjawab pertanyaan yang secara desain ditujukan kepada manusia — agen yang bisa memanggilnya bisa menjawab pertanyaannya sendiri (memperluas 0099 & 0112). Harga yang diterima sadar: jawaban single-select tiba di agen sebagai TEKS kolom bebas berisi label opsi (menekan digit lebih harfiah tetapi tak punya titik pembatalan — begitu byte-nya keluar sesi sudah bergerak), dan dua pertanyaan berjudul sama persis dalam satu rantai punya hash yang sama. Gelembung pose `waiting` berhenti `aria-hidden` karena ia kini punya tombol, dan yang disembunyikan pindah ke TEKS-nya — keputusan SPEC-898 #3 (region `role="status"` tetap satu-satunya yang membacakan) tetap dipegang.
- [0141 — Onset "menunggu manusia" hidup di ISI marker keputusan, dan menyeberang sebagai `decisionAt`](0141-onset-menunggu-di-marker-keputusan.md) — **mengamandemen SPEC-184 pada isi marker, menegakkan 0039, 0024 & 0091** (SPEC-898). `TerminalSession` tak punya satu pun stempel waktu, jadi sesi yang baru bertanya dan sesi yang sudah 40 menit tak dijawab tak bisa dibedakan konsumen mana pun. Jawaban naifnya — `mtime` marker keputusan — **salah dan terukur salah**: hook `Notification` menjalankan `echo waiting >> f` SETIAP kali Claude menandai dirinya idle, dan hanya `UserPromptSubmit` yang mengosongkannya, jadi tiap baris mencap ulang mtime; marker nyata `prd-orchestrator-hanoman` berisi **13 baris** tanpa satu pun truncate di antaranya, dan umur yang diturunkan dari mtime karena itu selalu lebih muda dari satu putaran idle — gerbang 10 menit tak akan pernah menyala. Karena itu hook berubah dari "tambahkan satu baris" menjadi "tulis stempel bila masih kosong" (`[ -s F ] || date +%s > F`, cermin di hook `Stop` codex): **semantik `size > 0` tidak berubah** sehingga `markerFilled()`, `scanDecisions()`, dan `GET /lead/status` tak berubah satu baris pun — yang diamandemen hanya ISI-nya, yang dulu tak bermakna. `decisionAt` (ISO) lalu ditumbuhkan additif & opsional di `SessionInfo`/`SessionDTO`/`TerminalSession`, ada HANYA saat `decision` true dan isinya bisa diparse; **absen berarti "tak diketahui"** dan konsumen tak boleh mengeskalasi tanpa stempel — itulah nasib sesi yang sudah berjalan saat versi ini dipasang. Berkasnya dibaca hanya untuk marker yang sudah terbukti terisi, jadi sesi yang tak menunggu membayar nol I/O tambahan dan `pty.ts` tetap nol dependensi DB. Dua alternatif ditolak dengan alasannya: peta onset di memori server hilang saat restart DAN saat dashboard ditutup lama (onset baru tercatat ketika `listSessions()` dipanggil), dan `createdAt` notifikasi `decision` — persis benar, tahan restart — berarti satu query DB per detik di grup siar `sessions` untuk stempel yang sudah ada di disk. Gotcha yang diwarisi, bukan diperkenalkan: dialog TUI yang dijawab tanpa `UserPromptSubmit` (SPEC-452) meninggalkan marker terisi, jadi akibat lamanya "`decision` lengket" kini bertambah "dan umurnya terus tumbuh". **Diamandemen ADR-0143 (SPEC-903):** gotcha itu ditutup di sana, dan dua klausa di atas pensiun — `decisionAt` kini `max(onset marker, window_activity)` sehingga marker pra-ADR-0141 (isi `waiting`) TIDAK lagi absen, dan `markerFilled()`/`scanDecisions()`/`GET /lead/status` TIDAK lagi tak-berubah: ketiganya kini membaca bit turunan. Isi marker sendiri tetap apa adanya
- [0140 — Pet dashboard sebagai sprite beranimasi: aset dibuat AI lewat Codex, pipeline registrasi, renderer frame, berkeliaran di tepi bawah](0140-pet-sprite-codex-pipeline-berkeliaran.md) — **menegakkan 0039, mengamandemen konvensi SPEC-585/648 dan sistem maskot brand** (SPEC-896). Pet SPEC-585/648 hanya bisa MENGGOYANG satu sticker raster: tujuh pose = tujuh shorthand `animation` di atas artwork yang sama, jadi "identitas gerak per status" sebenarnya identitas *transform*, bukan identitas *karakter*. Keluarga aset baru `internal/assets/pet/` (atlas WebP 8 kolom × 10 baris saat lahir, kini **16 baris** — lihat amandemen SPEC-904 di bawah; sel 192×208, karakter berdiri 168 px, + `pet.json`) dibuat **Codex (GPT Image)** lewat prompt yang dikomit — Codex diperlakukan sebagai alat pengembang seperti `cwebp`: turunannya dikomit, runner CI tak pernah memanggilnya. Empat keputusan pipeline semuanya lahir dari pengukuran, bukan dari desain di atas kertas: latar generasi **hijau** karena despill magenta MEMOTONG merah (emas jadi olive) dan karena itu key dikerjakan pipeline, bukan Codex; sprite **dideteksi dari celah transparan** karena model tak menaati sel grid mana pun yang diminta; frame **diregistrasi** offset+skala di wilayah statis lalu wilayah itu **di-pin** ke frame 1 (jangkar kaki saja menyisakan jitter ±15 px); dan kaki dikenali lewat **massa** run di pita terbawah, bukan lewat sisi — ekor yang menggores baris bawah hanya seutas garis (82–98 px vs 289–594 px), sedangkan `walk-left` digambar dengan ekor di sisi KANAN sehingga aturan "run paling kanan" akan menjangkar seluruh baris itu pada ekornya. Renderer memakai satu `<img>`: baris lewat `--row`, frame lewat `steps(8)` atas `translateX(-100%)` — grammar SPEC-648 (hanya transform/opacity, tanpa rAF/interval) ditegakkan, katalog `hn-pet-idle-*`/`hn-pet-pose-*`/`--dur-pet-*` dicabut karena gerak pose kini DIGAMBAR. Perilaku baru (berkeliaran 40 px/dtk di jalur tepi bawah, pulang ke pojok saat pose perhatian, diam saat hover/panel/tab tersembunyi) hidup di `pet-walk.ts` yang MURNI, jadi tabelnya bisa diuji tanpa DOM; mobile, reduced-motion, dan `hanoman.pet.roam=0` menjangkar ke pojok. Jalur selebar viewport memaksa gerbang tap SPEC-763 diperluas: `pointer-events: none` di seluruh pembungkus, `auto` hanya di tombol 44 px yang IKUT BERPINDAH bersama pet. A11y berubah bentuk karena satu gambar yang isinya berganti-frame tak punya alt yang jujur — kalimat status pindah ke `span` visually-hidden di dalam `role="status"`, atlas `alt=""`. Brand membayar satu pengecualian eksplisit: band **pet** 80–128 px (±2,5 head unit) melanggar "don't use chibi inflation", ditebus review Gate 2 lewat `internal/assets/pet/qa/` (GIF + contact sheet per baris). Gotcha untuk pembaca berikutnya: gerbang residu pra-pin menilai wilayah statis, jadi pose yang prompt-nya MEMANG menggerakkan tangan setinggi pinggang (`waiting`, `docs-updated`) mengukur lebih tinggi dari `idle` tanpa cacat apa pun — kalibrasinya harus dibaca bersama `residual_post`, yang tetap ≤ 0,004 justru pada baris-baris itu. **Diamandemen SPEC-904** (Pet hidup E): atlas naik ke **v02, 16 baris** — `held`, `falling`, `dizzy` ditambahkan di ekor untuk pet yang DISERET dan `wave` diregenerasi supaya bisa diputar berulang; v01 **dibuang**, bukan disimpan berdampingan, karena ia turunan murni dari `rows/` + `petlib.ROWS` yang tak bisa dirakit ulang sesudah `ROWS` bertambah. Tiga perubahan pipeline, semuanya lahir dari pengukuran: mode registrasi keempat **`float`** untuk baris yang tak menapak tanah (registrasi wilayah ATAS seperti `walk`, tetapi `dy` apa adanya dari registrasi seperti `stand`, tanpa pin — memaksa piksel terendah ke baseline akan mendorong badan naik-turun mengikuti kaki yang menjuntai); gerbang **kerataan langkah** `max/min ≤ 3,5` untuk baris yang diputar berulang, menilai juga **sambungan 8→1** yang tak terlihat di contact sheet maupun onion-skin (`wave` v01 mengukur 7,08 — dua frame nyaris kembar mengapit dua hentakan — dan turun ke 2,27 sesudah regenerasi); dan gerbang **skala karakter** `body_ratio ≥ 0,80`, yang lahir karena `held` percobaan 1 LOLOS setiap gerbang yang ada (8 sprite, tumpahan 0, residu 0,021, kerataan 2,43) sambil menggambar kepala 63 px vs 79 px milik `idle` — `build_strip` menskalakan **bbox** frame 1 ke `STAND_H`, jadi ekor yang menjuntai lurus ke bawah mengecilkan badannya diam-diam. Plafon `ATLAS_BUDGET` naik 1 MB → **1,3 MB** dengan `quality` tetap 76: 16 baris terukur q76 = 1 165 556 B · q60 = 1 058 832 · q40 = 929 558 · q20 = 768 780, jadi `quality` adalah tuas yang LEMAH (−34 % dari q76 ke q20) sementara premis plafon lama ("satu `<img>` di-decode di setiap halaman") ternyata tak dilayaninya sama sekali — biaya decode adalah **piksel** (19,5 MiB RGBA pada 16 baris vs 15,8 MiB pada 13), bukan byte. Sel, kolom, `anchor.baseline` 202, `character.h` 168, renderer, dan mesin berkeliaran tak berubah; ketiga baris baru **bukan pose** — `POSE_ROW`, `PetPose`, `HanomanPet.tsx`, dan `pet-walk.ts` tak disentuh, dan interaksi seretnya dikerjakan backlog penerus. **Diamandemen ADR-0144 (SPEC-905):** interaksi seret itu SELESAI — `pet-walk.ts` kini memegang sumbu Y + mode `held`/`falling`/`dizzy`, angka berkeliaran dibalik jadi ±72 % berjalan, dan jangkar `anchored()` menunjuk `parkedX ?? pojok`; ketiga baris tetap bukan pose
- [0139 — Hardening ADR-0117 jadi opt-in (`HANOMAN_HARDENING`), setup awal dipandu wizard di browser](0139-hardening-opsional-dan-wizard-setup.md) — **mengamandemen 0117 & 0087, mengikuti 0088** (SPEC-884). `npm i -g hanoman && hanoman` TAK BISA BOOT: dengan env yang persis dihasilkan `serverEnv()` untuk instalasi polos, `assertRuntimeBoundary` menjawab `HANOMAN_SESSION_SANDBOX=podman wajib di production` (uid 1000) dan `harus berjalan sebagai user non-root` (uid 0) — dan env hardening TAK PERNAH lahir dari instalasi npm, hanya dari `EnvironmentFile` systemd. Akarnya `NODE_ENV` yang merangkap TIGA peran (runtime terpaket, cookie `Secure`, gerbang 0117), yang juga membuat profil `lab` ADR-0137 mustahil lahir: `serverEnv()` menuliskannya hardcoded dan ditumpuk belakangan, mengalahkan `EnvironmentFile`. Karena itu dua nilai eksplisit: `HANOMAN_DEPLOYMENT` (local|public, tak memaksa apa pun) dan `HANOMAN_HARDENING` (satu-satunya yang menyalakan 0117), resolvernya MURNI di `@hanoman/runner` karena `cli` tak bergantung `server` dan `doctor` wajib menjawab sama dengan wizard. `assertRuntimeBoundary` hanya dapat satu baris di depan — badannya tak disentuh, jadi begitu hardening menyala perilakunya identik. Env 0117 lama (`HANOMAN_SESSION_SANDBOX=podman`/`PUBLIC_ORIGINS`/`TRUST_PROXY`) dibaca sebagai hardening MENYALA supaya hub produksi tak turun senyap saat upgrade. Jawaban wizard di `$HANOMAN_HOME/config.env` yang digabung PALING LEMAH (`{...file, ...process.env, ...serverEnv()}`) — bukan `RuntimeConfig`, yang presedensinya DB→env dan karena itu membiarkan siapa pun yang bisa menulis config mematikan hardening (pelajaran 0088). `hardeningLocked` butuh DUA bacaan (process.env vs berkas) karena sesudah merge keduanya tak bisa dibedakan. Restart pakai sentinel sendiri `CONFIG_RESTART_EXIT = 76` (75 akan ikut menjalankan `npm i -g`), efeknya dipisah ke `services/restart.ts` supaya bisa di-mock — `process.exit` dari handler membuat tiap test apply menjadwalkan exit di worker vitest. Wizard DUA langkah; akun pertama tetap di `AuthScreen` supaya hanya satu jalur melahirkan akun, dan `needed` bersyarat GANDA (`user.count()===0` DAN `HANOMAN_SETUP_DONE`) karena tanpa syarat kedua wizard muncul lagi tiap restart. Toggle hardening tak bisa menyala saat prasyarat merah — menulisnya melahirkan instance yang menolak boot, kegagalan yang sama cuma dipindah ke tombol. Cookie `Secure` pindah ke skema request dengan `x-forwarded-proto` dibaca LANGSUNG (bukan `req.protocol`, yang `false` tanpa `HANOMAN_TRUST_PROXY` → instance di balik Cloudflare Tunnel akan KEHILANGAN `Secure`); memercayai header itu aman karena arahnya satu — menyuntiknya hanya memperketat. Harga yang diterima sadar: instance publik tanpa hardening dijaga satu password, dan wizard yang tak terlindungi bisa DIKLAIM orang pertama yang membukanya bila instance sudah terjangkau internet sebelum selesai
- [0137 — Provisioning VPS berbasis katalog: probe sebagai sumber kebenaran, biner ≠ login](0137-provisioning-vps-berbasis-katalog.md) — **menegakkan 0117, 0042 & 0088, mengamandemen 0087** (SPEC-883). Memasang hanoman di VPS adalah prosedur manual enam bagian; modul VPS sudah punya separuh mesinnya sejak SPEC-164/220 tapi seluruhnya diarahkan ke *hardening* mesin yang sudah dipakai, bukan *menyiapkan* mesin kosong. Komponen jadi **data** (sembilan entri, `requires` per profil) dan dependensinya ditutup + diurutkan topologis **di server** — skrip menerima daftar lengkap dan tak pernah menebak. Satu `provision.sh`, dua mode: `COMP <id> <ok|partial|absent>` (probe) dan `STEP <id> <would|ok|fail|skip>` (apply), cermin `remediate.sh` plus `skip` — komponen yang prasyaratnya gagal WAJIB tetap menerbitkan baris, karena daftar langkah yang lebih pendek dari yang dicentang terbaca seperti "berhasil". **Probe satu-satunya penulis `Vps.components`**: apply sukses pun diakhiri probe ulang, jadi penandaan tetap jujur saat mesin disentuh di luar hanoman (SPEC-487). Komponen ber-login TAK PERNAH `ok` — paling jauh `partial not-logged-in`, lalu Console (0042) untuk login sekali; nol rahasia menyeberang. Dua profil: `lab` (sesi di host) dan `production` (gerbang 0117 utuh, agen di dalam image Podman, login mendarat di credential dir) — 0117 ditegakkan, tak dilonggarkan; konsekuensi jujur profil `lab` adalah cookie sesi **tanpa `Secure`** karena `NODE_ENV` bukan production. Gerbang DNS mendahului Caddy supaya rate-limit ACME tak terbakar. Ketiga kolom baru LOCAL-ONLY (kelas SPEC-880). Temuan yang memaksa masuk scope: `copyPlan()` tak pernah menyalin `server/scripts/vps/` sementara `scriptPath()` jatuh ke `process.cwd()` — audit/harden/remediate kemungkinan besar sudah mati senyap di setiap instalasi npm, direproduksi sebagai test
- [0136 — Agen bawaan sistem: katalog konstanta yang di-SEED sebagai baris, diperbarui hanya bila belum disunting](0136-agen-bawaan-sistem-seed-idempoten.md) — **memperluas 0094 & 0101, mengikuti 0113 & 0119**, tanpa mencabut apa pun (SPEC-881). Katalog custom agent lengkap sejak SPEC-450/484 tapi KOSONG di setiap instalasi — "berbiaya nol saat tak dipakai" terbaca sebagai "tak dipakai", karena operator harus menulis prosa persona dari nol sebelum tahu apakah ia berguna. Delapan persona (dua per domain: dev · QA · audit · security) dikirim sebagai TABEL KONSTANTA di shared — pola METHODS, data murni tanpa `node:*` karena paket itu ikut dibundel browser — lalu di-SEED sebagai baris `CustomAgent` global saat boot; empat menyala (`scout`, `qa-verifier`, `blast-radius`, `security-reviewer`), empat lahir mati. `mentions`/`model`/`runtime` sengaja BUKAN field entri: tanpa `mentions`, `Task` dicabut dari argv dan lapis-1 anti-loop 0094 tetap nol risiko. Seed menghormati TOMBSTONE (0119) — tanpa itu penghapusan operator dibatalkan tiap boot — dan jalur perbaruannya menuntut DUA syarat: isi baris masih cocok stempel terakhir seed DAN versi terpasang berbeda; tanpa syarat pertama upgrade menimpa kerja operator, tanpa syarat kedua `updatedAt` bergerak tanpa sebab dan menyeberang sync sebagai mutasi palsu. `enabled` tak pernah ikut diperbarui. Stempel menumpang `Setting.data` → nol migration dan LOKAL per mesin (`setting` tak ada di `FIELDS`), jadi dua mesin beda versi tak saling menimpa bolak-balik. Status "bawaan" DITURUNKAN di response (`builtin`/`builtinEdited`, pola `inherited`), bukan kolom — kolom baru ditolak hub versi lama (kelas SPEC-880). Satu klausa prompt mendorong jalur claude memakainya, menyebut hanya agen yang BENAR-BENAR ada di roster sesi itu
- [0135 — Penanda "ditangani oleh" pada Project: kolom `Json` yang MASUK sync, berisi snapshot device](0135-penanda-project-ditangani-hanoman-client.md) — **menegakkan 0043, 0044, 0045**, kontras dengan 0072/0091/0103 yang semuanya LOCAL-only (SPEC-880). `Project.handledBy` (`Json?`, nol backfill) adalah kolom project PERTAMA sesudah `gitRemote` yang sengaja MASUK `FIELDS.project`: `repoDir`/`schedulerOptIn`/`leadOptIn`/`autoMerge` lokal karena masing-masing properti MESIN ini, sedangkan "project ini dipegang mesin yang mana" adalah pernyataan tentang dunia — justru menyeberangnya nilai itu yang jadi seluruh gunanya. Tiap entri SNAPSHOT `{deviceId,name}`, bukan FK: `DeviceToken` tak ikut `SYNCED`, jadi client penerima tak punya baris device untuk di-join dan chip akan tampil KOSONG TANPA SATU PUN ERROR bila `name` tak ikut tersimpan (kelas gagal-senyap 0090/0093/0105). `revoked` TIDAK disimpan — ia diturunkan `toProjectView` dari baris device lokal, dan revoke tak pernah menghapus penanda. Gerbang tulis menghakimi `deviceId` HANYA bila instance ini punya katalog device; editor jatuh baca-saja di client dan `handledBy` dihilangkan dari body PATCH di sana (mengirim `[]` akan MENGHAPUS nilai hub, dan penghapusan itu menyeberang). Kompatibilitas menuntut rilis HUB DULU: `snapshot()` menyusun `data` dari SELURUH `FIELDS`, jadi client baru mengirim `handledBy: null` di SETIAP push project dan hub lama menolak semuanya — `POST /sync/push` karena itu kini menilai per-record (`{id,ok:false,error}`) alih-alih 500 seluruh batch, menutup kelasnya untuk penambahan field berikutnya. Murni informasional: tak menggerbangi sesi, worktree, auto-merge, scheduler, maupun lead
- [0134 — Prediksi input dihakimi oleh pengakuan pengiriman, bukan oleh `readyState`](0134-pengakuan-pengiriman-input-terminal.md) — **menegakkan 0014, 0016, 0117, dan 0133**, tanpa mencabut apa pun (SPEC-878). TTL 500 ms + suspend 30 dtk milik SPEC-856 dimulai saat karakter DIKETIK, dan gerbangnya membaca `ws.readyState === OPEN` — dua fakta transport dipakai sebagai fakta pengiriman. Terukur di jalur nyata: socket `OPEN` tanpa byte mengalir (bentuk normal pindah sel) membuat 9 glyph muncul lalu **dihapus** (`\x1b[9D\x1b[K`) dan prediksi mati 30 dtk dengan `link` tetap `"open"` dan **nol banner** — satu kedip 500 ms membeli **30,5 dtk layar bisu**; socket tertutup memberi **0 tulis lokal untuk 14 keystroke** meski bytenya aman diantre. `ws.bufferedAmount` bukan penggantinya (payload satu keystroke lolos ke buffer kernel dan terbaca `0` justru pada kasus yang perlu dideteksi). Kontrak frame karena itu bertambah satu pasang: `{t:"in", d, seq}` → `{t:"ack", seq}`, dibalas sesudah `writeTo`; `View.connected` jadi `View.deliverable`; `onInput` menghentikan jam (`since = null`) dan `onDelivered` menyalakannya hanya saat semua frame sudah diakui. `TTL_MS`/`SUSPEND_MS` tak berubah — yang diperbaiki kapan hukumannya berhak dijatuhkan. Merosot aman di kedua rilis campuran, tanpa denyut periodik apa pun (arah keluar SPEC-812 utuh). Diukur ulang sesudah perbaikan: glyph **bertahan** selama pemadaman (9 tulis lokal, nol rollback) dan prediksi tetap hidup sesudahnya **Amandemen 2026-08-22:** suspend menyembuhkan diri — teks yang diketik selama suspend dicatat dan suspend dicabut begitu frame server yang sudah tergambar (callback `term.write`) memperlihatkan ekornya di kiri kursor; pemicu palsu yang tersisa sesudah jalur server sinkron adalah TUI agen yang repaint >500 ms saat mesin sibuk; `SUSPEND_MS` 30 → 5 dtk tinggal fallback konteks bungkam, `TTL_MS` tetap.
- [0133 — Keadaan pane datang dari tmux, bukan dari aliran byte klien](0133-alternate-screen-pane-dari-tmux.md) — **menegakkan 0014 dan 0016**, tanpa mencabut apa pun (SPEC-863). Gerbang alt-screen SPEC-856 memindai `?1049h/l` dari aliran byte klien; premis itu **salah di bawah tmux** dan mematikan echo prediktif **total** tanpa satu pun galat, log, atau tanda di UI — terukur di jalur nyata (tmux + node-pty + xterm 6 asli + Chrome headless, RTT disuntik di jembatan): `altScreen: true` sejak frame pertama, nol tulis lokal, `predict=1` **tak bisa dibedakan** dari `predict=0` (225,5 vs 225,8 ms). Dua ukuran yang menentukan bentuk keputusannya, keduanya membantah tebakan yang paling wajar: (1) ini **bukan** regresi tmux 3.7b — tmux **3.4** dan **3.5a** yang dibangun dari sumber berperilaku identik sampai ke `head` aliran yang sama persis, karena `?1049h` itu `smcup` **terminfo klien** dan pasangan `rmcup`-nya memang dikirim, hanya saat **detach** yakni ketika klien sudah dibunuh (`vt100`/`ansi` tanpa `smcup` → 0 kejadian); dan (2) tmux **tak pernah meneruskan** `?1049`/`?47` milik program di dalam pane — kontrol positif memperlihatkan `#{alternate_on}` bergerak `0 → 1 → 0` sementara hitungan di klien tak bergerak sama sekali. Karena itu gerbang berbasis aliran hanya bisa **salah-positif, tak pernah benar-positif**, dan "cabut saja `?1049` dari daftar" bukan perbaikan melainkan pemindahan kematian ke sisi lain: gerbangnya lalu tak pernah menyala dan prediksi hidup juga di `vim`. Keputusannya menaikkan pertanyaannya satu lapis — **aliran byte klien tmux adalah gambar, bukan laporan keadaan** — sehingga `#{alternate_on}` ikut di `FMT` `list-panes` yang **sudah** dipoll 500 ms (nol invokasi tmux tambahan) dan lahir sebagai varian frame baru `{t:"alt"}`, disiarkan saat berubah **dan** sekali ke tiap klien baru di `attach()` (tanpa yang kedua, klien yang mendarat di tengah keadaan yang tak berubah lagi takkan pernah menerimanya — pelajaran pil "Selesai" SPEC-433). Hasil diukur ulang, bukan disalin dari SPEC-856: **225,3 → 0,6 ms** (RTT 200 ms), **21,0 → 0,4 ms** (localhost), sementara alternate screen sungguhan tetap menolak segalanya (0 tulis lokal, latensi sama dengan prediksi mati) dan layar klien identik `tmux capture-pane` di setiap lengan. Gotcha yang dibayar sadar: transisi alt-screen terlambat **≤ 500 ms** (periode poll) — satu huruf bergaris bawah bisa sempat tergambar di TUI layar penuh, lalu dilepas byte server pertama atau TTL 500 ms; mempercepat poll akan membebani seluruh sesi terbuka demi jendela yang sudah pendek. Gotcha kedua untuk pembaca berikutnya: angka `?47h` 1× di kontrol negatif audit SPEC-856 **tak bisa direproduksi** pada satu pun dari tiga versi tmux maupun tujuh TERM — jangan bangun apa pun di atasnya
- [0128 — Gerbang validasi sebelum publish, dan bootstrap Prisma Client di postinstall](0128-gerbang-validasi-sebelum-publish.md) — **memperluas 0087** (amandemen 2026-07-30 yang memindahkan publish ke CI) dan **menegakkan 0080**, tanpa mencabut apa pun (SPEC-852, issue #1): sampai keputusan ini `.github/workflows/` berisi **tepat satu berkas** — `release.yml` — dan jalurnya `install → pnpm release → smoke --version → publish`. Tak satu pun langkah memanggil `vitest` maupun `tsc`, jadi paket bisa terbit dari commit yang test dan kontrak TypeScript-nya merah. Dua sebab saling menutupi: **`pnpm release` = build, bukan verifikasi** (server & CLI dibundel esbuild dengan `--external:@prisma/client` — esbuild tak melakukan type checking dan tak pernah menyentuh Prisma Client, jadi build hijau adalah pernyataan tentang bundling saja); dan **checkout bersih tak punya Prisma Client** — skema hanoman ada di `server/prisma/schema.prisma`, sementara postinstall `@prisma/client` hanya mencari lokasi default, tak menemukannya, lalu menyerah dengan peringatan. Terukur pada `da3967af`: sesudah `pnpm install --frozen-lockfile` bersih, `cli/test/migrate-pg.test.ts` gagal **saat collect** (`Cannot read properties of undefined (reading 'datamodel')`, vitest melaporkan `0 test`), `pnpm --filter ./cli typecheck` exit 2, `pnpm --filter ./server typecheck` exit 2 dengan 12 error — dan **satu** `prisma generate --schema prisma/schema.prisma` membuat ketiganya hijau, membuktikan tak ada regresi kode di baliknya. **Keputusannya dua lapis, bukan satu:** `postinstall` paket `server` men-generate client dengan skema **eksplisit** (mekanisme yang gagal justru mekanisme "cari sendiri di lokasi default"), dan **fatal bila gagal** — berbeda dari paket terbit yang sengaja `|| true` karena di sana ada jaring kedua `ensurePrismaClient` (0087), sementara di workspace dev tak ada jaring apa pun; urutan di dalam script karena itu mengikat, `chmod` non-fatal **lebih dulu** dan `prisma generate` **terakhir** supaya exit code script adalah exit code generate. Lapis kedua **`pnpm validate`** = `db:generate` → `pnpm -r typecheck` → `vitest run --no-file-parallelism`, dengan `db:generate` **diulang** walau postinstall sudah melakukannya: postinstall bisa dilewati (`--ignore-scripts`, sebagian CI), dan jalur validasi yang mengandaikan langkah tersembunyi bukan jalur validasi (pola dua lapis yang sama dengan 0087). **`validate.yml` punya tiga pemicu** — `pull_request`, `push: main`, dan **`workflow_call`** — dan `release.yml` **memanggilnya** alih-alih menyalin langkahnya, dengan job `publish` ber-`needs: validate`; menyalin akan menghasilkan dua definisi yang menyimpang tanpa ada yang merah, kelas kegagalan yang sama dengan yang ADR ini perbaiki, dan `needs:` lintas-workflow memang tak ada sehingga `workflow_call` satu-satunya cara memakai definisi yang sama pada **commit yang sama**. Gerbangnya sendiri **dipagari test** (`cli/test/release-gate.test.ts` membaca kedua workflow + kedua `package.json`), karena berkas konfigurasi rusak **tanpa suara**: langkah yang terhapus tak menggagalkan apa pun, ia hanya membuat publish lewat begitu saja — pola `RUNTIME_DEPS`/`REQUIRED_ARTIFACTS` (0087) dan naskah agen (SPEC-489). **Gotcha yang melahirkan poin tersendiri:** prasyarat DMMF kini dibaca `Prisma.dmmf?.datamodel?.models ?? []` dengan **assertion bernama** yang menyebut `pnpm db:generate`, karena gagal-collect melaporkan `no tests` dan `--changed` menyalakan `passWithNoTests` (0080) — berkas yang meledak saat collect punya **dua** cara terlihat hijau. **Konsekuensi diterima sadar:** rilis kini bisa diblokir CI (mitigasi lingkungan dipasang eksplisit di `validate.yml` — `tmux` untuk test sesi terminal 0016, identitas git untuk test worktree, `HANOMAN_HOME` ke dalam workspace runner karena berkas DB test diturunkan dari situ dan bukan dari checkout SPEC-479, `HANOMAN_UPDATE_FETCH=0` supaya validasi tak menyentuh jaringan); waktu CI naik jadi suite serial penuh, yang **tak bisa ditawar** (181 gagal palsu paralel vs 736 lulus serial, SPEC-397); dan `pnpm -r typecheck` **sah di CI tetapi tetap terlarang sebagai rutinitas sesi** — larangan 0080 adalah tentang mesin dev yang menjalankan beberapa sesi sekaligus, bukan tentang runner yang berdiri sendiri. **Alternatif ditolak:** menyalin langkah validasi ke `release.yml`; menggerbangi publish lewat status check commit dari run lain (bukan run yang sama); membatasi gerbang pada test CLI + typecheck saja (batas minimum acceptance criteria issue — melewatkan permukaan terbesar repo); `prisma generate` hanya sebagai langkah CI (memperbaiki CI, meninggalkan checkout bersih tetap merah); dan `postinstall` di root alih-alih `server` (skema milik `server`, dan postinstall paket workspace sudah terbukti berjalan lewat perbaikan `spawn-helper`). · **Amandemen 2026-08-19 (SPEC-853):** `validate.yml` dipecah jadi **dua job tanpa `needs`** — `typecheck` dan `test` — karena suite serial memang tak bisa diparalelkan (SPEC-397 tak berubah) tetapi kedua LAPISAN itu tak saling bergantung; `workflow_call` baru selesai setelah semua job selesai, jadi `needs: validate` di `release.yml` tetap menunggu keduanya dan cakupan gerbang tak berkurang. `pnpm validate` tetap jalur local bernama; yang bergeser adalah klaim "CI menjalankan perintah yang sama persis" — karena itu `release-gate.test.ts` berhenti mencocokkan string `pnpm validate` dan mulai menuntut **cakupan** (kedua job ada, menyebut lapisannya, tak ber-`needs` ke tetangga). Setup bersama di composite action `.github/actions/ci-setup` supaya dua job tak jadi dua salinan yang menyimpang; `tmux` + identitas git tetap hanya di job `test`. · **Bypass sementara 2026-08-19:** job `test` **menggantung** (tiga run mati di plafon 6 jam GitHub, semuanya di `pnpm validate`) — gerbang yang menggantung tak menahan commit buruk, ia memblokir SEMUA rilis; `release.yml` karena itu mengirim `skip_test: true` (default input `false`, dan pemicu PR/push tak punya input sama sekali, jadi jalur sehari-hari tetap menjalankan test). `typecheck` + ancestry + tag==version + smoke `--version` tetap menggerbangi publish; suite test hilang sementara, diterima sadar. Pagar kedua `timeout-minutes` (15/30) supaya hang lapor merah dalam menit, bukan menyamar sebagai "CI lambat". **Cabut** bypass begitu hang diperbaiki; timeout tetap.
- [0127 — Satu kontrak konfirmasi destruktif: `useConfirm()`, `window.confirm` nol di produk](0127-satu-kontrak-konfirmasi-destruktif.md) — **memperluas 0121** (yang menambahkan `requireText` ke `ConfirmDialog`), **menegakkan** kontrak a11y `Modal` tanpa menyentuhnya (SPEC-847, dari GitHub issue #6): lima belas aksi destruktif memakai `window.confirm()` native sementara `ConfirmDialog` sudah ada dan dipakai lima permukaan lain — telanjang di satu berkas yang sama, `TriageScreen` memakai dialog aplikasi untuk *hapus tiket* dan native untuk *tolak tiket*. Akarnya **bentuk pemanggilan**, bukan kelalaian: `window.confirm` sinkron sehingga muat satu baris di tengah fungsi async, `ConfirmDialog` deklaratif sehingga menuntut state + callback + elemen (±15 baris per aksi) — jalur yang benar adalah jalur yang mahal, jadi memigrasikan 15 call site tanpa menutup akarnya hanya menunda call site ke-16. Efek terukurnya pada test: jsdom memulangkan `undefined` (falsy) sehingga tiap flow berhenti di baris pertamanya kecuali test-nya mem-mock, dan hanya **dua** berkas yang mem-mock — `deleteProject`/`deleteUser`/`revokeDeviceToken`/`purgeSessionResults`/`deleteVps`/`hardenVps`/`deleteChangelog` **tak punya satu pun test yang menekan tombolnya**. Bentuknya **`useConfirm()` → `{ confirm, dialog }`**, **lokal per komponen bukan Provider** (layar dirender berdiri sendiri di test; nilai default context gagal SENYAP ke dua arah — "batal" mematikan flow, "ya" menghapus konfirmasi), opsi **`run`** menahan dialog terbuka + `busy` selama mutasi, dan `confirm()` **melempar** bila `run` melempar sementara `false` hanya berarti pembatalan (kalau kegagalan diterjemahkan jadi `false`, call site menelan error — penanganan 409 `deleteProject` mati). Penegakannya test pemindai **sumber** (`confirm-inventory.test.ts`, cermin `placeholder-contract` SPEC-490) karena repo tak memakai ESLint dan `window.confirm` tak punya jejak di pohon render. **Tujuh gotcha:** (1) lupa merender `{dialog}` = promise menggantung SELAMANYA tanpa error — penjaganya penghitung `= useConfirm(` vs `{dialog}` per berkas, jadi nama `dialog` wajib baku (`confirm` boleh di-alias, `TriageScreen` sudah punya state bernama itu); (2) pemindai yang diam-diam berhenti = "semua lulus", jadi jumlah berkas & call site ditegaskan lebih dulu; (3) anti-klik-ganda bersandar **`useRef`**, bukan state — klik kedua tiba sebelum render ulang; (4) fokus awal jatuh ke tombol **"Tutup" header**, bukan "Batal" footer (aman; memindahkannya butuh prop `initialFocus` di `Modal` dan React `autoFocus` **tak cukup** — di-override `initial?.focus()` di layout effect yang berjalan sesudah `commitMount` anaknya); (5) focus restore hanya bisa menyasar node yang masih ada — apply VPS yang sukses meng-`clearSel()` dan toolbar-nya lenyap, jadi restore diuji di jalur batal; (6) `ConfirmDialog` bisa hidup DI DALAM `Modal` lain (checklist VPS) sehingga `getByRole("dialog")` cocok lebih dari satu; (7) `Icon` kini memancarkan `data-icon` supaya kontrak ikon bisa diuji tanpa menyentuh internal lucide. Satu pengecualian didokumentasikan: `GitGraph` "Dorong tag ke origin?" **bukan gerbang** — jawabannya NILAI `push`, bukan izin, dan membatalkannya tetap membuat tag
- [0126 — Durabilitas penghapusan transkrip riwayat: DB commit dulu, berkas disapu mark & sweep](0126-durabilitas-penghapusan-transkrip-riwayat.md) — **mengamandemen 0079 §5** (purge "ikut menghapus berkas transkripnya" kini punya URUTAN yang mengikat), menegakkan **0018/0019** (manifesnya kolom yang sudah ada, bukan tabel baru), **0024** (menumpang `setInterval` retensi, tanpa timer/proses kedua), **0086** & **0116**; **tidak mencabut apa pun**, tanpa skema/migration (SPEC-845, GitHub issue #8): `purgeHistory()` meng-`unlink` berkas transkrip LEBIH DULU lalu baru `deleteMany()`. Dua reproduksi terukur menentukan keputusannya. (1) `deleteMany` yang melempar meninggalkan baris **hidup** ber-`hasTranscript: true` sementara `transcriptOf()` `null` dan direktorinya kosong — bukti hancur permanen, dan retry tak bisa mengembalikannya; `SQLITE_BUSY` bukan hipotesis jauh karena sejak 0086 satu berkas DB ditulis bersamaan oleh sweep retensi, tap webhook, denyut lead, engine scheduler, dan tiap request, sementara `UPDATE_RESTART_EXIT = 75` (0088) dan `SIGTERM` membuka jendela yang sama **tanpa satu pun error tercatat**. (2) Premis yang menopang urutan lama terbukti **salah**: sesi yang ditutup antara `findMany` dan `deleteMany` tetap melahirkan berkas yatim, karena `deleteMany({where})` **menurunkan ulang** himpunannya saat dieksekusi sementara daftar `transcriptKey` berasal dari snapshot sebelumnya — jadi urutan lama tak pernah membeli sifat yang diklaimnya, ia hanya menambah mode gagal yang menghancurkan bukti. Keputusannya karena itu bukan "pilih urutan tanpa sisa" melainkan **pilih urutan yang sisanya bisa dipulihkan**: yatim ditemukan kembali sebagai selisih himpunan, byte yang telanjur di-`unlink` tidak. **(1) Berkas dihapus SESUDAH barisnya commit**, di KEDUA jalur — `purgeHistory()` dan sweep retensi, yang bentuknya sama per baris dan justru paling sering melewati jendelanya (tiap 24 jam, tanpa manusia). **(2) `deleteMany` menyebut himpunan id EKSPLISIT per potongan 200**, bukan `where` yang diturunkan ulang: tak ada baris yang bisa terhapus tanpa kuncinya dikenal, dan crash di titik mana pun konvergen (potongan selesai + satu potongan yatim + sisa baris yang tinggal di-purge ulang) — purge **idempoten**. **(3) `deleteTranscript` hanya menelan `ENOENT`**; `EACCES`/`EIO` dilempar, karena diam di sana membuat purge melapor sukses penuh atas berkas yang masih ada. **(4) `reconcileTranscripts()` mark & sweep dua arah** di dalam sweep retensi — berkas tak dirujuk disapu, `transcriptKey` yang menunjuk berkas hilang dikosongkan sehingga `hasTranscript` berhenti berbohong (ikut menyembuhkan kerusakan yang sudah telanjur terjadi di instance hidup); manifesnya **kolom `transcriptKey` itu sendiri**, sikap yang sama dengan `.trash` worktree 0116. **(5) `DELETE /terminal/history` → `{purged, transcriptsDeleted, transcriptsFailed}`**, aditif, tetap `200` karena barisnya memang terhapus. **Alternatif ditolak:** staging/trash + restore (opsi A finding) menukar satu masalah durabilitas dengan yang lain — direktori staging itu sendiri butuh pembersih dan catatan pemilik; transaksi Prisma yang membungkus `unlink` (mustahil, `unlink` tak ikut rollback); perintah CLI rekonsiliasi sendiri (melanggar 0024); tabel outbox berkas (mencatat apa yang direktori + kolom sudah catat lebih baik — cermin alasan 0116 menolak `WorktreeCleanup`). **Lima gotcha:** tenggang sapuan **wajib** — `saveTranscript()` menulis berkas SEBELUM `finishSession()` menulis kuncinya, jadi GC tanpa tenggang menghancurkan berkas hidup di jendela itu, **cacat yang sama dengan yang ADR ini perbaiki dilahirkan kembali oleh perbaikannya sendiri** (`TRANSCRIPT_GC_GRACE_MS` = 1 jam); sapuan disaring ke `.log` supaya berkas asing tak ikut tersapu; test yang memanggil `runRetention()` **wajib** menyetel `HANOMAN_TRANSCRIPT_DIR` atau ia menyapu `~/.hanoman/transcripts` milik instance sungguhan; `deleteMany` harus tetap menyebut `id` (mengembalikannya ke `where` "supaya ringkas" menghidupkan lagi jendela baris-tanpa-kunci); dan `reconcileTranscripts` hidup di `session-history.ts` karena berkas itu satu-satunya pemilik tabel `SessionHistory`. Kontrak retensi lama **dibalik** — "baris ditahan bila berkasnya gagal dihapus" tak berlaku lagi, dan test yang menguncinya diperbarui (pola gotcha SPEC-433/0107)
- [0125 — Riwayat sesi mencatat **cara** sebuah baris ditutup (`endedReason`/`reconciledAt`); kelas hasilnya diturunkan](0125-akhir-sesi-riwayat-tercatat.md) — **menegakkan 0079 · 0016 · 0090 · 0100 · 0011/0018**, tak mencabut apa pun (SPEC-844, dari GitHub issue #9). `SessionHistory` mencatat **kapan** sebuah baris berakhir, tak pernah **bagaimana**: `killSession()` mengirim `exitCode: p.exited ? p.code : null` dan menurut `pty.ts:55` TUI agen **tak pernah mati sendiri**, jadi `exitCode: null` adalah keadaan NORMAL sesi sehat — identik dengan yang ditulis `reconcileHistory()` untuk pane yang lenyap saat boot. Keduanya dirender hijau `selesai`, dan karena tak terbedakan di DB **tak ada perbaikan murni-UI yang mungkin**. Terukur di DB hidup 806 baris: **784 dari 797 baris berakhir (98,4 %) hijau tanpa satu pun bukti sukses**, hanya 13 memikul exit code (5×0, 2×1, 6×143 — kelas SPEC-402); **20 dari 20** baris rekonsiliasi berdurasi **0 ms** (klaim lama "`updatedAt` = waktu terbaik yang tersedia" keliru — service ini hanya menulis saat lahir & tutup, jadi `updatedAt` baris berjalan = waktu LAHIR, dan sesi yang hidup **22 j 50 mnt** sampai reboot tercatat "0 dtk") dan **20 dari 20** tanpa transkrip. Yang **disimpan** karena itu hanya cara baris ditutup — `endedReason` `closed`|`reconciled` + `reconciledAt` — sementara kelas hasilnya (`running|completed|failed|interrupted`) **diturunkan** satu fungsi murni `sessionOutcome()` di shared: `completed`/`failed` sudah bisa dihitung ulang dari `exitCode` (0011/0018), siapa yang menutup barisnya tidak (arah 0090). Enum empat nilai di kolom yang disarankan issue **ditolak** sebagai bentuk penyimpanan, tapi dikirimkan utuh sebagai turunan. `endedAt` baris rekonsiliasi tetap batas **BAWAH** (`reconciledAt` batas atas) dan durasinya **tak dirender sama sekali** — memindahkannya ke waktu boot mengarang klaim bahwa sesinya hidup selama seluruh downtime. Backfill sekali jalan berambang **60 000 ms** duduk di tengah celah empat orde besaran yang terukur (tutup normal **0–39 ms** n=777 vs rekonsiliasi **275 966–82 224 277 ms** n=20), fail-safe ke `closed` di kedua arah. **Enam gotcha:** `endedReason` dibaca **longgar** (`z.string().nullable()`, apa pun yang bukan `reconciled` = `closed`) — enum ketat menggugurkan 784 baris lama di setiap pembacaan; `endedAt` rekonsiliasi **bukan** waktu berakhir; ambang 60 dtk **backfill, bukan aturan render**; kolom baru wajib masuk `WEBHOOK_ENTITIES.fields` (salah ketik mengosongkan payload **tanpa satu pun error**, kelas 0094); baris `closed` sengaja **tak berubah** (melabeli 764 penutupan normal "terputus" menukar bug ini dengan kebalikannya yang 38× lebih besar); dan backfill memakai `CAST(… AS INTEGER)` karena Prisma menyimpan DateTime SQLite sebagai INTEGER ms — representasi lain jatuh ke selisih 0 → nol baris cocok, dibuktikan sebagai kontrol negatif. Pengulangan **keempat** kelas SPEC-402/433/451: menilai keadaan dari ketiadaan sinyal, bukan dari bukti
- [0123 — Source `no_effort`: flow satu fase `Kerjakan` untuk task remeh, payload menumpang bentuk goal](0123-source-no-effort-flow-satu-fase.md) — **memperluas 0089**, **menegakkan 0080 · 0108 · 0109 · 0113** (SPEC-825): lima source memetakan ke flow lewat satu fungsi, dan yang PALING PENDEK adalah `goal` = dua fase (`Goal → Verifikasi`). Untuk ganti copy/label, bump konstanta, typo docs, atau satu baris allowlist, fase `Verifikasi` menghabiskan satu giliran agen untuk membuktikan sesuatu yang diff-nya sendiri sudah membuktikan — dan biayanya bukan token melainkan SLOT: satu backlog = satu sesi di worktree sendiri, sementara mesin operator menjalankan beberapa sesi sekaligus (premis yang sama yang melahirkan 0080). Akibatnya task remeh difilekan sebagai `goal`, atau lebih buruk `brief` lima fase. **Keputusan: flow keenam `no_effort` = `PIPELINES.no_effort = ["Kerjakan"]`** — `Kerjakan` aktif ⇒ `executing`, done/skipped ⇒ `done`, tanpa stage antara karena memang tak ada fase antara. **Nama fase wajib unik lintas seluruh `PIPELINES`**: peta `REACHED` berkunci NAMA FASE saja, jadi memakai ulang `Execute`/`Goal` tak menghasilkan error — ia merusak deteksi fase SELURUH flow yang memakai nama itu (aturan yang sama melahirkan `Goal`/`Verifikasi` di 0089). **Payload menumpang bentuk `goal`, tak ada bentuk keempat** — dan alasannya mengikat, bukan hemat kode: bentuk keempat dengan field identik menuntut pembeda baru di `shapeOfPayload`, sementara TAK ADA field yang bisa membedakannya; `shapeOfPayload` adalah yang menjaga `payloadMatchesSource` di boundary `zCreateSpec`/`zPatchSpec`/`zChangeSpecSource`, jadi ia bukan sekadar duplikasi melainkan predikat yang tak bisa ditulis. Konsekuensi diterima sadar: **`Spec.source` satu-satunya pembeda** item `goal` dari item `no_effort` — kode yang perlu membedakannya wajib membaca source/flow, bukan menebak dari isi. **`WORK_PHASES` — satu daftar fase kerja untuk DUA gerbang di DUA paket:** sampai spec ini "sesi ini menulis kode" dinyatakan dua kali sebagai rantai `||` berisi nama yang sama (`writesCode` di runner, aturan fase-kerja-aktif di `stageFor` server), dan **suku yang lupa ditambah saat flow baru lahir tak menghasilkan error apa pun** — yang hilang adalah `verifyScope` (0080), klausa gaya kode (0108), dan `exitSkills` (0113), semuanya diam-diam. `writesCode` tetap SATU definisi; yang berubah cuma sumber daftarnya, dan server sudah mengimpor `PIPELINES` dari runner sehingga tak ada arah impor baru. **`isGoalShapedFlow`** (tetangga `flowForSource`) menggantikan `flow === "goal"` di `session-launch.ts` & `defaultGoalCondition`: mode goal DIPAKSA menyala (sesi satu fase justru paling gampang berhenti sebelum menulis baris fase & push), template global `Setting.goal.condition` DILEWATI, dan `goalFlowCondition` kini menyebut `PIPELINES[flow]` alih-alih `PIPELINES.goal` hardcode. **Prompt: satu builder, dua flow** — `startGoalPrompt` diparametrisasi flow, bukan disalin; yang berbeda hanya kepala prompt dan klausa "fase Verifikasi bukan formalitas", dan prompt flow `goal` tetap byte-identik. **Item berjalan terkunci OTOMATIS**: gerbang 0109 mengunci FLOW, dan `no_effort` berdiri sendiri — berkas fase item `feature` tak akan pernah memuaskan `phasesComplete(["Kerjakan"])` dan sebaliknya (bentuk SPEC-433); yang ditambahkan cuma test. Item BELUM dimulai tetap bebas pindah karena `convertPayload` berkunci BENTUK, jadi `brief ↔ no_effort` sudah dilayani jalur `brief ↔ goal` tanpa baris baru. **Gotcha:** `SOURCE_META` wajib berentri — fallback `?? SOURCE_META.brief` DIAM, jadi tanpa entri item `no_effort` memakai lencana "feature brief" tanpa satu pun error (persis yang menimpa `help`, 0109 poin 5). Tanpa migration (`Spec.source` sudah `String`; preseden `audit` SPEC-237, `help` 0062, `goal` 0089), tanpa entri `FIELDS.spec` baru. **Alternatif ditolak:** knob "lewati fase Verifikasi" di atas flow `goal` (knob tak mengubah `PIPELINES`, sementara `REACHED`/`phasesComplete`/`defaultGoalCondition`/gate sh codex semuanya menurunkan tuntutannya dari sana → sesi digerbangi fase yang tak akan pernah ditulis); bentuk payload keempat; dan memakai ulang nama fase `Execute`.
- [0122 — `constraints` di payload qa: bentuk payload seragam lintas source, `.default("")` untuk baris lama](0122-constraints-di-payload-qa.md) — **memperluas 0109 & 0089**, **menegakkan 0090** (SPEC-826): lima source dilayani tiga bentuk payload, dan hanya **qa** yang tak punya `constraints` — jadi pelapor temuan QA tak punya tempat menuliskan batasan pengerjaan ("jangan ubah kontrak API", "tanpa migration"), padahal ia justru sering tahu lebih dulu. Akibatnya terukur, bukan kosmetik: `convertPayload` **membuang** batasan di tiap konversi yang bermuara ke qa (`brief→qa` `dropped:["constraints"]`, `goal→qa` `["done","constraints"]`) dan **melahirkannya kosong** di arah balik, sehingga item yang pindah `brief → qa → brief` pulang tanpa batasannya — prosanya selamat di `Spec.sourceHistory`, tapi jejak bukan payload dan yang membacanya kemudian adalah prompt sesi. **Keputusan yang paling menentukan `constraints: z.string().default("")`, bukan `z.string()` polos:** `zQaPayload` dipakai `zSpec`/`zCreateSpec`/`zPatchSpec`/`zChangeSpecSource`, dan payload qa yang SUDAH tersimpan tak punya field itu — polos berarti setiap baris lama gagal validasi begitu ia dibaca, diedit, atau dikonversi; default sekaligus menormalkan sehingga hilir tak menjaga dua bentuk. `zBriefPayload`/`zGoalPayload` **tidak** ikut dilonggarkan (keduanya sudah mewajibkannya sejak lahir). **Yang sengaja TIDAK berubah dan itu keputusan:** `constraints` di luar `SHAPE_REQUIRED.qa` (kosong itu normal — menandainya "kurang" tiap konversi jadi kebisingan, alasan yang sudah ditetapkan untuk brief & goal); **`priority` tetap tak ada di payload qa** karena ia turunan `severity` (`priorityFromSeverity`) dan `deriveSpecFields` mengabaikan prioritas manual di sana — "field setiap source sama" tak berlaku untuk pasangan itu; dan pembeda `shapeOfPayload` tetap `severity`/`goal` sehingga qa & goal tak jadi ambigu, predikat tetap SATU. Label **"Batasan"**, dan `BRIEF_FIELDS` diseragamkan dari "Constraints" — field yang sama sudah bernama dua hal tergantung layar (form buat-backlog menulis "Batasan", katalog edit menulis "Constraints"), jadi memberi qa nama ketiga hanya menambah bentuk pada inkonsistensi itu. Tanpa migration (`Spec.payload` sudah `Json`), tanpa entri `FIELDS.spec` baru (payload satu kolom utuh), tanpa perubahan prompt (`JSON.stringify` apa adanya). **Lima gotcha:** (a) `.default("")` satu-satunya yang menyelamatkan baris lama — diuji dengan payload qa TANPA field itu, bukan hanya dengan yang baru; (b) `dropped` yang menyusut **mengubah UI** — blok `source-dropped` `ChangeSourceDialog` kini tak dirender sama sekali untuk `brief→qa`, jadi test pelaporan `dropped` harus pindah ke arah yang masih membuang (`brief→goal`), dan yang lama gagal dengan "unable to find testid" alih-alih menyebut sebabnya; (c) skema MCP wajib **mengiklankan tanpa mewajibkan** — agen hanya mengirim yang diiklankan, tapi `required` menuntut string yang boleh kosong; (d) **dua pabrik payload qa di server tak lewat zod** (`ticket-accept.ts`, `github-accept.ts` menulis langsung lewat `prisma.spec.create`), jadi tanpa penyebutan eksplisit item dari tiket Help Center & issue GitHub lahir tanpa field-nya — kelas ADR-0090/0093/0105; (e) `zEscalationPrefill.constraints` sudah ada sejak SPEC-340 tapi tak punya tujuan di bentuk qa sampai spec ini.
- [0121 — Operasi berkas dari IDE Explorer: satu path `entry`, unggah multipart di-stream, tanpa gerbang sesi](0121-operasi-berkas-ide-explorer.md) — **memperluas 0034** (IDE Visual) tanpa mencabut apa pun: sampai keputusan ini permukaan TULIS Explorer tepat satu endpoint, `PUT /projects/:id/file`, yang hanya bisa menimpa isi berkas yang path-nya sudah diketahui. Membuat berkas/folder, mengganti nama, menghapus, dan memasukkan berkas dari mesin operator tak punya jalur sama sekali — untuk berkas biner jalannya bahkan nihil, karena satu-satunya alternatif (menyuruh agen membuatnya di sesi) menuntut isinya diketikkan lewat prompt. Bentuknya **satu path `entry` untuk tiga operasi struktural** plus `upload` tersendiri (transportnya beda): ketiganya berbagi seluruh penjaga path yang sama, dan memecahnya jadi `/mkdir` + `/rename` + `/delete` berarti menyalin gerbang yang sama tiga kali — persis kelas bug yang menggigit repo ini di SPEC-431/448/475. Logikanya di service **murni** `services/repo-fs.ts` di atas `safe-repo-path.ts` yang sudah ada, jadi penjaga path tak ditulis ulang; harganya dinyatakan, bukan disembunyikan: komponen **symlink** ditolak, sehingga berkas symlink tak bisa dihapus/di-rename lewat IDE. Otorisasi **`ide:write`, bukan capability baru** — capability itu sudah memberi hak menimpa isi berkas apa pun lewat `PUT /file`, dan `rw()` menurunkannya dari METHOD sehingga tak mengulang kelas bug SPEC-405 (prefix dipetakan tanpa melihat method); yang menjaga hapus/rename adalah **konfirmasi di UI** (folder menuntut namanya diketik ulang lewat prop baru `ConfirmDialog.requireText`), bukan gerbang tambahan di server. **Tanpa gerbang sesi aktif**, alasan yang sama yang membebaskan `PUT /file` sejak awal: bukan operasi git, tak memindahkan HEAD, dan sesi hidup di `.worktrees/<id>` yang terpisah — memasangnya justru mematikan fitur ini pada project yang sedang dikerjakan. **Empat gotcha:** (1) folder kosong ditulis dengan **`.gitkeep`** — pohon Explorer dibangun dari `git ls-files` dan git tak melacak direktori kosong, jadi tanpa itu folder baru adalah folder hantu yang lenyap saat muat ulang; (2) struktur folder dibawa field **`manifest`** (JSON array path relatif, urut sama dengan part berkas), **bukan** `filename` multipart — nama ber-`/` tak punya jaminan lintas implementasi; (3) part **di-stream** ke `.tmp` lalu di-`rename`, karena pada batas 100 MB × 1000 berkas pola `toBuffer` (lampiran gambar 5 MB, SPEC-816) adalah cara termudah membunuh instance 8 GB — dan batas route ini disebut **per-request**, registrasi multipart global tetap 5 MB/12 berkas; (4) `saveUpload` memulangkan `exists` **tanpa membaca** stream part, jadi pemanggil multipart wajib `part.file.resume()` — tanpa itu busboy menunggu part yang tak pernah selesai dan seluruh request menggantung, gejalanya test yang timeout, bukan error. Bentrok **dilewati** kecuali `overwrite` diminta dan dilaporkan di `skipped` pada respons **200** (pola `POST /branches/delete`, SPEC-360), supaya unggah folder besar ke checkout ber-perubahan-belum-commit tak menghapus kerja itu tanpa satu pun pertanyaan
- [0120 — Tandai backlog selesai manual: operasi khusus `POST /specs/:id/done`, jejak `Spec.manualDone`](0120-tandai-backlog-selesai-manual.md) — **mengamandemen 0103** (kandidat sweep auto-merge kini disaring `manualDone`), **menegakkan 0008 · 0047 · 0099 · 0105** (SPEC-804): stage backlog hanya bergerak sebagai turunan sesi, dan `PATCH /specs/:id {stage}` sengaja **backward-only** (SPEC-167), jadi item yang beres DI LUAR sesi — dikerjakan langsung, sudah ter-merge lewat PR, atau ternyata sudah tercakup item lain — menggantung selamanya di stage lama. Bukan cuma soal tampilan: predikat `UNSTARTED_SPEC_WHERE` (`baseSha = null ∧ stage ≠ done`, SPEC-431) membuat checker scheduler **dan** denyut lead terus mengantrekannya, sementara satu-satunya jalan keluar yang ada adalah **menghapus** item berikut id SPEC-nnn, riwayat, dan `dependsOn`-nya. Keputusannya **operasi khusus `POST /specs/:id/done`**, bukan pelonggaran PATCH: melonggarkan PATCH berarti setiap stage bisa dimajukan ke mana pun dan premis "kemajuan hanya berasal dari fase sesi" (0008) runtuh bersama ketiga guard CAS yang berdiri di atasnya — sedangkan bentuk "operasi khusus untuk perubahan yang punya gerbang & efek sampingnya sendiri" sudah ditetapkan **0064** (rename project) dan **0109** (ubah source). Jejaknya **satu** kolom `Spec.manualDone Json?` = `{at, by, reason?}`, bukan tiga skalar yang bisa drift tanpa tipe pemaksa (kelas 0090/0093/0094/0105), dan **`doneAt` tak berubah maknanya** — tetap "selesai pertama", tetap ditulis hanya di dalam `recordCompletion()`, sementara `manualDone.at` menjawab "kapan operator menandai" sehingga keduanya tak pernah bersaing (terlihat persis pada reopen: keduanya terisi, keduanya benar). Eksekusinya **satu titik cekik** `completeSpecManually()` — CAS `stage != done` → `recordCompletion` → `recordSessionResult` → `notifySynced` — karena menyalin efek samping penyelesaian ke call site sudah tiga kali menggigit repo ini (SPEC-431/448/475). Sesi hidup digerbangi **dua langkah** (`409 confirm-required` + identitas sesi, cermin 0088) dan sesinya **tidak dibunuh**: penutupan sesi punya konsekuensi worktree sendiri (0116) dan sudah punya tombolnya. **Lima gotcha:** (1) `manualDone` wajib di `FIELDS.spec` **dan** `JSON_FIELDS`, **bukan** `DATE_FIELDS` (`at` di dalam JSON) — kolom terlewat mendarat sebagai null palsu tanpa satu pun error; (2) kandidat sweep auto-merge = notifikasi `done:` yang kini juga ditulis jalur manual, jadi `settleOne` **melewati** item ber-`manualDone` — tanpa itu item tanpa sesi melahirkan notifikasi "belum ter-push" yang salah, dan item ber-branch sesi lama yang DITINGGALKAN **di-merge setengah jadi**; (3) durabilitas terhadap overlay stage-live adalah konsekuensi guard forward-only `liveSpecs`, **dikunci test**, bukan diasumsikan; (4) gerbang "sesi hidup" membaca `specId` pane lewat `listSessions()` — bukan `getSession(sessionIdForSpec(id))`, dan pane MATI bukan sesi hidup; (5) `manualDone` **ditimpa** tiap penandaan dan revert stage sengaja **tidak** mengosongkannya (cermin `doneAt`; riwayat transisinya di `SessionResult`). Tool MCP sengaja **tak** ditambahkan — 0099 meniadakan tool yang memindahkan stage
- [0119 — Tombstone sync: hard-delete + `SyncTombstone`, `SyncLog.op`, delete menang tanpa syarat](0119-tombstone-sync-penghapusan-menyeberang.md) — **memperluas 0045**, **mencabut sebagian 0068 & 0082** ("feed append-only tanpa tombstone"; kontrak apply 0082 justru **ditegakkan**), 0043/0046/0067/0064/0100 utuh (SPEC-799): mesin sync dulu hanya mengenal UPSERT, jadi penghapusan tak pernah menyeberang dan sisi lain menghidupkannya kembali lewat **dua jalur berlawanan arah** — hapus di client (feed hub memutar ulang record itu lewat edit siapa pun, `backfillFeed` saat boot, atau tombol Tarik ulang) dan hapus di hub (`applyPush` menerima id yang absen sebagai INSERT BARU yang **selalu** diterima). Keputusannya **hard-delete + tabel `SyncTombstone`**, bukan soft-delete `deletedAt` per entitas: bentuk itu menyentuh SETIAP query baca delapan entitas (satu penyaring terlewat = bug yang sedang diperbaiki, gagal SENYAP), menggugurkan `onDelete: Cascade`, menabrak `@@unique([projectId, number])` saat pembuatan ulang, dan membuat tap Prisma ADR-0100 membaca hapus sebagai `update`. Ide intinya: **tombstone ADALAH versi record itu sendiri, berkeadaan dihapus** — dengan menjadikannya sumber `existing.version` saat barisnya tak ada, penolakan kebangkitan **jatuh dari optimistic-concurrency yang sudah ada**, tanpa cabang khusus. **Delete menang TANPA SYARAT** (hub tak melihat `baseVersion`; tombstone menggilas edit lokal pending) supaya hasilnya independen urutan tiba — antrean konflik ditolak karena ia mengembalikan bug ini sebagai "masih ada sampai manusia mengklik", dan modal rekonsil sudah ramai oleh konflik telemetri VPS (amandemen 0067); edit yang tergilas dinotifikasi, tak senyap. Anak yatim bagi induk bertombstone dibuang **sengaja** lewat peta `PARENTS` ber-gerbang DMMF (`sessionResult` sengaja absen — `projectId`-nya tanpa `@relation`; `ticketAttachment.projectId` denormal, yang FK cuma `ticketId`), menggantikan `console.warn("induk absen?")`. Retensi otomatis **tidak** menerbitkan tombstone: `retention.ts` & `pruneOldTickets` memang sudah di luar permukaan `notifySynced`, dan pemangkasan adalah kebijakan penyimpanan per-instance. Jalan pulang bagi data yang terlanjur bangkit: **hapus ulang sekali di sisi mana pun** — tanpa migrasi data. **Enam gotcha:** (1) `op` wajib TOP-LEVEL, tak pernah di dalam `data` — allowlist `validateSyncData` menolaknya di client lama → `feedHole` → kursornya tertahan SELAMANYA; (2) baris feed `op:"delete"` wajib membawa snapshot yang sah — objek kosong membuat hub lama 500 (create tanpa kolom required) dan client lama menerapkan baris kosong, jadi `SyncTombstone.data` prasyarat bukan kenyamanan; (3) `SyncTombstone` wajib masuk `PG_ORDER` (`cli/test/migrate-pg.test.ts` satu-satunya gerbangnya, cermin 0105); (4) `writeTombstone` wajib MONOTON — replay full-pull memutar feed dari awal, jadi kedatangan tak berurutan itu normal; (5) konsumsi tombstone saat pembuatan ulang duduk di **`notifySynced`** (kelas bug SPEC-431/448/475/481) dan wajib mengangkat `version` baris ke versi tombstone — baris baru lahir di `version = 0` sehingga tanpa itu pembuatan ulang yang sah ditolak selamanya; (6) `op` tak dikenal **dilewati**, tak pernah melempar — melempar berarti hub yang lebih baru bisa mematikan client lama dengan satu jenis peristiwa. Tambahan mengikat: **rename bukan hapus** — pintu `renamedFrom` (0064) memang melewati optimistic-concurrency, jadi ia mendapat gerbang tombstone sendiri.
- [0118 — Workspace Terminal kanonik per user dengan optimistic concurrency](0118-workspace-terminal-kanonik-per-user.md) — **mengamandemen sebagian 0115**, **menegakkan 0016**, tidak memakai 0043/0045 (SPEC-786): mapping kerja Terminal (`groups` berurutan + grid row-major + `sessionId`) bukan preferensi satu browser; ia kini `TerminalWorkspaceV1` tervalidasi pada `User.terminalWorkspace`, ber-revision CAS dan LOCAL-only per akun admin. `GET/PUT /api/terminal/workspace` cookie-only; PUT stale menjawab `409 revision-conflict + current`, klien reapply fungsi mutasi tepat sekali lalu refetch/fail visible. Bootstrap selalu server-first: legacy `hanoman.terminal.workspace` hanya boleh seed bila GET mengembalikan null; browser kosong tak pernah PUT; seed yang kalah race mengadopsi server. Cache `hanoman.terminal.workspace.v2.<userId>` hanya recovery read-only. Rekonsiliasi sesi mati menunggu workspace server **dan** daftar tmux sukses; kegagalan list bukan `[]`. Responsive hanya proyeksi — fullscreen, active group/cell, modal, viewport tetap lokal dan perubahan tier tidak pernah mengubah koordinat atau menulis server.
- [0117 — Boundary deployment publik, otoritas efektif, dan sandbox sesi](0117-boundary-deployment-publik-otoritas-efektif-sandbox-sesi.md) — **mengamandemen 0028/0037/0065/0100** (SPEC-761): satu paket tetap dipakai, tetapi exact host policy memisahkan public Help/status dari control plane yang wajib di belakang SSO/MFA/VPN/access proxy dan origin tetap loopback. Input Help berhenti sebagai data; setiap launch memerlukan approval durable dari cookie admin atau `sessions:write` dan diperiksa ulang di launcher. API/worker non-root; sesi production wajib rootless Podman/VM dengan mount, secret, resource, dan egress minimum — permission bypass tetap di dalam sandbox dan deny hook ADR-0037 **tidak** kembali. Choke point bersama menegakkan path canonical no-follow, HTTP pinned-address/no-redirect, sync rotation cookie-only, WS ticket exact-origin tanpa query token, bootstrap one-time atomik, limiter bounded, upload quarantine/scan, permission 0700/0600, dan retention sweep. Public deployment lama gagal boot sampai boundary wajib dikonfigurasi; credential service root wajib dirotasi.
- [0116 — Penutupan sesi asinkron: worktree dipindah ke `.trash`, penyapu latar yang menghapus](0116-penutupan-sesi-asinkron-worktree-trash.md) — **menegakkan 0079** (gerbang `ownsWorktree` SPEC-362 tak dilonggarkan), **0030 & 0093** (bukti `headSha`/stage tetap direkam sebelum worktree lenyap), **0018/0019** (catatan pembersihan = nilai turunan filesystem, bukan tabel) dan pola sweep **0072/0103**; **tidak mencabut apa pun**, mengubah SATU kontrak (`DELETE /terminal/sessions/:id` `204 → 202`) (SPEC-742): menutup satu sesi membuat UI menggantung **dan** memblokir seluruh pekerjaan lain, karena route mengerjakan `advanceStage` → `recordHeadSha` → `killSession` → `realGit.removeWorktree` sinkron di dalam satu request. Diagnosis yang mudah — "balas lebih awal" — **tidak cukup**, dan pengukuranlah yang menunjukkannya: atas salinan worktree nyata (562 MB / 25 040 entri), `rmSync` **1 370 ms dengan event loop terblokir 1 364 ms dan NOL tick**, `fs.promises.rm` **947 ms dengan lag 3 ms**, `renameSync` **1 ms**. Yang membeku bukan request penutupnya melainkan seluruh server — itulah "sesi lain tak bisa dibuka" — jadi 202 + `setImmediate` hanya memindahkan pembekuannya beberapa milidetik. Biayanya bahkan dibayar dua kali (`git worktree remove --force` sudah menghapus pohonnya, lalu `rmSync` lagi), dan jalur MEMBUKA terkena hal yang sama lewat reclaim `addWorktree` — yang dilewati setiap kali backlog `done` dibuka lagi (SPEC-172). **(1) Pindahkan, jangan hapus:** worktree di-`rename` ke `<repoDir>/.worktrees/.trash/<sesi>.<stempel>` di dalam request, byte-nya dihapus penyapu latar (`services/worktree-reaper.ts`, `fs.promises.rm`). **(2) Tiga sifat sekaligus** — path bebas SAAT respons kembali (bukan dijanjikan bebas); **domain penyapu lepas total dari setiap path hidup**, jadi penutupan tumpang-tindih dan sesi baru yang lahir selagi penyapuan jalan aman **menurut konstruksi, bukan menurut penguncian** (nol mutex, nol antrean); dan `.trash` **itu sendiri** catatan durable-nya — isinya menurut konstruksi sampah, jadi "bisa disapu ulang" gratis tanpa tabel/kolom/migration. **(3) Reclaim `addWorktree` memakai primitif yang sama** — outcome-nya menyebut "sesi baru untuk backlog yang sama" harfiah, dan tanpa ini penutupan cepat sementara pembukaan tetap membekukan server. **(4) `202 { cleanup }`**, bukan 204: "diterima, pembersihan lanjut di latar" bukan "no content", dan `cleanup: null` menyatakan sesi tanpa worktree. **(5) `closing → closed` diamati pada PEMBERSIHANNYA, bukan pada sesi** — pane sudah mati saat kita membalas, jadi sesi hantu di `GET /terminal/sessions` justru melawan janji "UI langsung melepas tab"; entri muncul di `GET /terminal/cleanups` = `closing`, hilang = `closed`. **(6) Kegagalan bersuara**: `Notification` `type:"cleanup"` ber-`key` unik per entri → satu baris per sampah, bukan satu per tick, dan entrinya tetap tinggal untuk dicoba lagi. **Alternatif ditolak:** kunci per-path (memblokir lagi, cuma pindah tempat), path worktree ber-suffix unik (mengubah kunci review/integrate/resume di belasan tempat), tabel `WorktreeCleanup` (mencatat sesuatu yang filesystem sudah catat lebih baik), dan menyapu worktree yatim dari `git worktree list` (worktree sesi yang pane-nya mati tampak yatim padahal justru artefak "Lanjutkan", ADR-0084). **Delapan gotcha:** `rmSync` → `fs.promises.rm` bukan kosmetik (1 364 ms vs 3 ms); `advanceStage`/`recordHeadSha` **wajib tetap di dalam request sebelum `rename`** (keduanya membaca dari DALAM worktree); `ownsWorktree` berdiri **sebelum** `trashWorktree` (rename sama merusaknya dengan rm); `rename` gagal → jatuh ke `removeWorktree` sinkron, **jangan** ke penghapusan latar atas path aslinya (path itu bisa direbut peluncuran berikutnya); nama entri memuat id sesi (satu-satunya cara "milik sesi mana" bertahan melewati restart tanpa tabel); `git worktree prune` sekali per repo per sapuan; dan peta pembersihan di memori adalah **read model**, diisi ulang dari `.trash` tiap sapuan — mempercayainya sebagai sumber berarti restart di tengah penghapusan meninggalkan sampah yang tak seorang pun tahu ada; dan **pemindahan + pencatatan + tendangan penyapu adalah SATU panggilan** (`releaseWorktree()`) — memisahkannya berarti tiap call site baru harus mengingat ketiganya, sementara `session-worktree.ts` tetap MURNI (hanya `ownsWorktree`) karena ia diuji murni justru sebab kesalahannya menghapus direktori sungguhan. Tiga jalur `killSession` lain (`lead/apply.ts`, `telegram/session.ts`, `session-launch.ts`) **tidak diubah**: tak satu pun menghapus worktree, jadi tak satu pun bisa balapan dengan penyapu.
- [0115 — State tampilan dashboard persisten: kunci `hn.ui.v1.<screen>.<field>`, satu hook bersama](0115-state-tampilan-dashboard-persisten.md) — **menegakkan 0107** (yang dipulihkan `page`, tak pernah `limit`) & **0071** (deep-link hash menang atas state yang dipulihkan), lalu **diamandemen sebagian 0118** khusus mapping kerja Terminal (SPEC-740): dashboard menavigasi lewat state `section` di App, bukan router URL, jadi tiap layar di-unmount saat pengguna pindah dan seluruh `useState`-nya hilang — filter, halaman, scroll, baris terpilih, sub-tab. Refresh lebih buruk lagi: `section` sendiri lahir `"overview"`, jadi buka-ulang browser **selalu** melempar balik ke beranda. `localStorage` sebelum ini dipakai **dua** tempat (workspace grid Terminal, flag Pet), keduanya menulis try/catch sendiri — tanpa satu mekanisme, layar ke-14 menambah tambalan ketiga. **(1) Satu modul `src/src/ui-state/`**: `store.ts` bebas React (bisa diuji langsung, pola `terminal-layout.ts`), `hooks.ts` (`usePersistedState`/`useScrollRestore`), `ResetViewButton.tsx`. **(2) Kunci `hn.ui.v1.<screen>[@<scope>].<field>` — versi hidup DI DALAM kunci**, bukan di dalam nilai: menaikkan `v1 → v2` membuat seluruh state lama tak terlihat tanpa satu baris migrasi, dan `pruneUiState()` menyapu sisanya saat App mount. `@<scope>` untuk state per-project (Docs/IDE/Changelog) — tanpa itu filter project A muncul saat membuka project B. **(3) Nilai rusak jatuh ke default, tak pernah melempar**: `getItem` **dan** `JSON.parse` di dalam try (mode privat melempar saat DIAKSES), lalu guard bentuk — bawaan = *bentuk sama dengan `fallback`*, union menyebut `oneOf`, nullable menyebut `nullableStr`; tanpa guard, `page: "abc"` dari bentuk lama membuat Pager menghitung NaN. **(4) `localStorage`, bukan `sessionStorage`** (syaratnya buka-ulang browser); **BUKAN URL** — keputusan pengguna, link yang bisa dibagikan adalah backlog terpisah. **(5) Reset lewat pub/sub**: menghapus kunci saja tak cukup karena komponen ter-mount memegang nilainya di `useState`; `resetUiState(screen)` menghapus lalu memancarkan prefix, dan tiap hook yang cocok kembali ke nilai awal — nol prop drilling, layar baru ikut dapat. `ResetViewButton` sekaligus merender lencana **"N filter aktif"**, memenuhi syarat "filter yang dipulihkan harus terlihat menyala". **(6) Scroll dipulihkan SESUDAH data ada** lewat loop rAF berbatas, dipasang di `<main>` milik `Shell` (satu titik → semua halaman, termasuk yang belum ada) + container daftar Backlog/Projects/Triase. **Tujuh gotcha:** nilai disimpan BESERTA kuncinya & disinkronkan **saat render** (kalau terpisah, ganti project menimpa state project lain); pemulihan scroll wajib membisukan penulisnya + berbatas 20 frame (percobaan pertama pada konten pendek menulis balik nilai TERPOTONG); `section` digerbangi **`NAV_KEYS`** (section transien `project`/`review` = mendarat di layar kosong, key mati = App merender kosong berikut sidebar-nya, SPEC-519); **hanya `page`, tak pernah `limit`** (PLAFON, ADR-0107); `src/test/setup.ts` wajib `localStorage.clear()` tiap test (satu jsdom per berkas → state bocor antar-test, terbaca seperti regresi komponen); `ResetViewButton` & `shell.tsx` mengimpor lewat berkas, bukan barrel (`ds → shell → ui-state → ds` = lingkaran impor yang mati saat init); dan state milik App yang dipakai sebuah layar (`projectFilter`) di luar jangkauan reset berskop layar → ditangani lewat prop `onReset`. SPEC-740 sendiri murni state klien; sejak 0118 key Terminal global hanya input migrasi, sedangkan filter/scroll/fullscreen dan flag Pet tetap lokal.
- [0114 — Status pemasangan skill metode: deteksi per agen LOCAL-only, pemasangan lewat sesi terminal](0114-status-pemasangan-skill-metode.md) — **melengkapi 0113**, memakai **0056** sebagai satu-satunya pemasang, **menegakkan 0011/0018/0037/0087/0088** (SPEC-739): ADR-0113 memasang `MethodDef.requires` di katalog, tapi field itu punya **NOL pembaca runtime** — dua titik render teks, dan `hanoman doctor` tak menyentuh skill sama sekali; hanoman menjanjikan metodologi yang tak pernah ia pastikan ada. Skill yang hilang **tak mematikan sesi** (tool error, agen lanjut) — dan justru itu bahayanya: gerbangnya ikut mati, karena instruksi "tulis plan berkotak" datang dari skill fase Plan (tanpa berkas plan `planComplete` mengembalikan `true` HAMPA) dan `superpowers:verification-before-completion` — INVARIAN 2 ADR-0113 — jadi **no-op**. **DUA KOREKSI TERUKUR atas premis brief:** (a) **codex punya DUA akar**, `~/.codex/skills/` **dan** `~/.codex/plugins/cache/<mkt>/<pkg>/<versi>/skills/` — `superpowers@openai-curated` terpasang & enabled di mesin dev, jadi memindai satu saja memberi **salah-negatif di mesin yang sehat**; (b) **"terpasang" ≠ "aktif"** — kedua agen menyatakan gerbangnya dalam bentuk yang SAMA (`"<plugin>@<marketplace>": bool`; claude di `settings.json` `enabledPlugins`, codex di `config.toml` `[plugins."x@y"] enabled`), dan `ponytail` ada lengkap di cache **dan disabled**. **(1) Deteksi di `runner/src/skills.ts`** (bukan `shared` — ia dibundel Vite ke browser; preseden `paths.ts` yang dipakai server DAN cli), akar ber-env `HANOMAN_CLAUDE_HOME`/`HANOMAN_CODEX_HOME` (+ `CODEX_HOME` untuk codex, tapi `HANOMAN_*` menang supaya test tak menyentuh HOME nyata). **(2) FAIL-OPEN adalah sifat GERBANG, bukan sifat VONIS** — kebalikan INVARIAN 1 ADR-0113 dan itu disengaja: gerbang plan menjawab "apakah pekerjaan selesai" (ragu = tahan), deteksi menjawab "apakah lingkungan siap" (ragu = jangan blokir). Tapi "jangan blokir" **bukan** "anggap terpasang": pencocokan **ketat & id persis**, karena vonis optimistis palsu ADALAH kegagalan senyap yang spec ini hapus. Yang fail-open adalah **tiap sumber IO** (direktori hilang/JSON rusak → nol skill, tak melempar, tak mengosongkan sumber lain). **(3) DUA daftar wajib dilaporkan:** `requires` = nama PAKET, `phaseSkills ∪ exitSkills` = id SKILL yang benar-benar dipanggil prompt — paket ada dengan skill kurang itu keadaan nyata. **(4) Status LOCAL-only, NOL tabel** — `GET /api/methods/status` diturunkan live tiap request (cermin coverage docs 0011/0018); nol model Prisma ⇒ nol entri `FIELDS` sync, properti MESIN bukan properti workspace; cookie-only seperti `GET /codex/version`. **(5) Perintah instalasi = data katalog** (`MethodDef.install` per agen), diikat test SUMBER — kalau tidak, metode ketiga kembali menuntut sunting di server/web (AC-10 ADR-0113). **(6) Pemasangan lewat SESI TERMINAL** — varian shell `POST /terminal/sessions` menerima `install?: {method, agent}` dan server menurunkan perintahnya dari katalog (klien tak pernah mengirim teks perintah, jadi endpoint ini tak jadi "jalankan shell arbitrer"); yang menjalankan adalah shell di pane tmux — **nol executor baru**, 0037 utuh, dan tak ada protokol restart untuk sesuatu yang tak butuh restart. **(7) `doctor`** melaporkan metode DEFAULT saja, hanya untuk agen yang CLI-nya ada, baris `!` **non-fatal** + perintahnya. **Gotcha:** cermin `MethodAgent` di `method-catalog.ts` (impor balik ke `entities.ts` menutup lingkaran; dijaga test yang mengadu kunci `install` dengan `zAgent.options`); metode tak dikenal di jalur install → **400**, sengaja tak lenient seperti `resolveMethod` karena ini TINDAKAN; dot-dir dilewati (`~/.codex/skills/.system/` milik codex); bukti test wajib dari akar ber-env; `exec` di ujung skrip pemasangan disengaja (install gagal meninggalkan shell hidup, dan `npx skills add` interaktif); 18 berkas test web yang me-mock `api` sebagian wajib ikut menyebut `getMethodStatus`.
- [0113 — Registry metode workflow: katalog `METHODS` di `shared`, gerbang plan memindai union direktori](0113-registry-metode-workflow.md) — **menegakkan 0029** (memperluas jangkauan gerbang plan terceklist ke seluruh metode tanpa mengubah aturannya), mengikuti pola knob **0080** & **0081**; **menegakkan 0024/0037/0074/0089/0090/0109** (SPEC-734): hanoman hanya mengenal satu metodologi kerja, dan itu tak dipilih di mana pun — ia tertulis literal, sebagian sebagai string path. Enumerasi ulang menemukan **sembilan** titik fungsional, tiga di antaranya TAK disebut brief: `runner/src/goal.ts` (grep plan di kondisi mode goal), `continuePrompt`/`resumeClause`, dan prompt hanoman-lead. **(1) Satu registry `METHODS` di `shared/src/method-catalog.ts`** — `{id,label,planDir,specDir,phaseSkills,exitSkills,extraClause?,requires}`; metode ketiga = SATU entri. Modul ini **sengaja bebas zod** karena diimpor `runner`, lapis yang selama ini bebas skema. **(2) DEVIASI SADAR: registry DI-IMPORT runner, bukan dicerminkan** seperti `Flow`/`Agent`/`VerifyScope` di `enums.ts` — cermin masuk akal untuk enum tiga kata, bukan untuk tabel yang harus identik di tiga paket; SPEC-407 sudah membayar konvensi itu dengan **EMPAT cermin `Flow`**. **(3) INVARIAN 1 — gerbang plan fail-closed:** setiap GERBANG memindai **union** `PLAN_DIRS`, dan yang menentukan adalah bentuk penanganan direktori yang tak ada — `planComplete` wajib **`continue`, BUKAN `return true`**, kalau tidak direktori metode PERTAMA yang absen mengakhiri pemindaian sebelum metode kedua dilihat dan backlog lompat ke `done` dengan plan lama penuh `- [ ]`. **Prompt** tetap menyebut direktori metode sesi itu (AC-4); yang union adalah gerbangnya. **(4) INVARIAN 2 — pintu keluar tak bisa dinegosiasikan:** `exitSkills` non-kosong + wajib memuat `superpowers:verification-before-completion`, ditegakkan test DI SUMBER (pola SPEC-490), dan digabungkan **secara struktural** ke fase TERAKHIR pipeline lewat gerbang `writesCode` yang SAMA dengan `scopeClause`/`codeStyleClause` — mattpocock tak punya padanannya, dan tanpa itu flow `goal` kehilangan satu-satunya gerbangnya (fase `Goal` sengaja tanpa skill, 0089). **(5) Resolusi** `opts.method → Spec.payload.method → Setting.method → superpowers`, distempel ke payload di peluncuran PERTAMA lalu **beku** (fakta historis, cermin `startedAt` 0090). **(6) `zMethod` LENIENT** (`z.string()`, bukan `z.enum`) dan nilai mentah **tak pernah dikoersi** saat simpan/baca — id dari hub harus jadi fallback diam di titik pakai, bukan baris Setting yang gagal parse lalu mengosongkan layar Settings. **(7) PIPELINES tidak berubah** — nama fase adalah kunci peta `REACHED`. **(8) Rekonsiliasi "hanya skill model-invoked":** yang dilarang adalah skill yang MEWAWANCARAI manusia (sesi tak berpenunggu → deadlock) dan penulis tracker EKSTERNAL (`triage`/`to-spec`; hanoman ADALAH tracker-nya), ditegakkan **denylist di test sumber** — itulah satu-satunya bacaan yang membuat constraint "hanya model-invoked" dan constraint yang mengatur pemakaian `to-tickets` benar bersamaan; katalog memilih `grilling` (primitif) di atas `/grill-me` (pembungkus) persis atas pemisahan ini. **Default `superpowers` byte-identik** dengan sebelum spec ini (dedup menyerap `exitSkills`, `extraClause` kosong), dibuktikan 78 test prompt lama yang hijau tanpa disunting. Tanpa migration, tanpa endpoint baru. Metode adalah properti sesi **BACKLOG**; sesi project-level/cron/konflik tetap default. **Gotcha:** `goal.ts` mudah terlewat karena ia bukan "prompt" DAN gerbangnya menuntut hasil grep KOSONG — direktori salah membuatnya **lulus**; label `superpowers` sengaja huruf kecil (byte `"Skills superpowers WAJIB"`); assertion `phaseSkills ⊆ PIPELINES` hidup di `runner/test/` karena mengujinya dari `shared` = siklus paket.
- [0112 — Cronjob per project di scheduler: sesi ber-id deterministik, satu jatuh tempo satu baris, anggaran slot bersama](0112-cronjob-per-project-scheduler.md) — **memperluas 0072**, **menegakkan 0015/0016/0024/0045/0065/0107/0108** (SPEC-646): ADR-0072 menunda jadwal HH:MM di bagian Konsekuensinya sendiri ("cadence hanya interval menit"); ADR ini menjawabnya **tanpa subsistem kedua** — nol timer baru, nol dependensi runtime, nol broker. **(1) Unit peluncurannya bukan `Spec`:** `SchedulerQueueItem.specId` NOT NULL `@unique` ITU kunci idempotensi "satu sesi per spec", jadi memaksakan cron ke sana melonggarkan kunci yang menjadikannya benar. Sesi cron adalah sesi project-level ber-id **deterministik** `cron-<cronId>` di worktree isolasi, tanpa `flow` — dan id deterministik itu **adalah** mekanisme "satu sesi per cron" (0015): pane hidup adalah satu-satunya bukti, dan bukti itu selamat dari restart tanpa kolom yang bisa basi. **(2) `@@unique([cronId, dueAt])` = idempotensi struktural:** satu jatuh tempo bisa diklaim paling banyak sekali, apa pun yang terjadi pada tick berulang, dua tick yang balapan, atau restart di tengah — P2002 adalah jalur normal, bukan galat. `nextRunAt` **bukan** kuncinya (ia bisa gagal ditulis sementara run-nya sudah lahir). **(3) Tertunggak ≠ terlambat:** sweep memajukan `nextRunAt` ke jatuh tempo TERBARU yang ≤ now dan membuat SATU baris; yang dilompati jadi angka di dalam alasan, tak pernah jadi burst — tapi jatuh tempo terbaru tetap dinilai dengan `GRACE_MS` yang sama seperti jatuh tempo biasa. Satu angka grace itu sengaja menjawab dua sebab sekaligus (server mati · cap penuh): jadwal 07:00 kehilangan maknanya bila berjalan 09:00, apa pun sebabnya. **(4) `sweepCronDue` jalan SEBELUM gerbang `paused`** — Pause adalah rem PELUNCURAN, bukan penghapus antrean (0072 keputusan 4); memindahkannya ke bawah membuat Pause *menghapus* jatuh tempo. **(5) Anggaran slot BERSAMA, cron lebih dulu** (jatuh tempo punya makna waktu; baris antrean spec tak punya tenggat), dan `drainCrons` dipanggil **juga saat slot habis** supaya barisnya mendapat catatan "cap penuh" alih-alih menggantung tanpa penjelasan. **(6) `/scheduler/crons*` COOKIE_ONLY:** cron adalah `POST /terminal/sessions` yang DITUNDA — membiarkannya di `settings:write` berarti token mana pun bisa menjadwalkan sesi tanpa batas, kelas eskalasi yang ditutup SPEC-405 dan 0097/0100; "Jalankan sekarang" karena itu juga hanya membuat baris run manual (tick yang meluncurkan), sehingga cap/Pause/master tak perlu disalin ke route. **(7) `GovernorDeps.drainCrons` WAJIB, bukan opsional** — terukur saat implementasi: satu literal deps tanpa anotasi tipe lolos tsc dan gagal sebagai `launches === 0` karena `undefined` melempar lalu ditelan `catch` di `tick`; field opsional membuat kegagalan itu permanen dan senyap. **(8) Parser cron ditulis sendiri di `shared`** bukan demi hemat: preview dihitung di browser dan jadwal di server, dan satu modul murni yang keduanya impor adalah satu-satunya jaminan mereka sepakat. Kandidatnya dibangun `new Date(y,mo,d,h,mi)` (komponen LOKAL) — itulah yang aman DST, dan invarian yang diuji **`hasil > after`**, bukan jam persisnya. Cron tetap tunduk `Project.schedulerOptIn` (0072 keputusan 5); kesenyapannya ditutup dua arah — alasan di riwayat run + lencana & tombol opt-in inline di panelnya.
- [0111 — Portal klien mengirim tiket help desk: satu route tulis, satu pipeline intake, lepas dari `helpEnabled`](0111-portal-klien-kirim-tiket.md) — **mengamandemen 0110**, **menegakkan 0038/0062/0065/0066/0068** (SPEC-626): portal klien lahir sebagai PEMBACA, jadi satu-satunya cara klien mengeluh adalah halaman Help Center **publik** — honeypot, rate-limit per IP, email diketik manual, digerbangi `helpEnabled`. Klien yang sudah login harus KELUAR dari portal untuk memakainya, dan otentikasi yang baru saja ia lewati tak dipakai sama sekali. **(1) Lubangnya bentuk PATH, bukan method:** `clientRouteAllowed` menerima `POST` + segmen persis `portal/projects/<id>/tickets` (`isPortalTicketSubmit`), bukan "portal boleh POST" — melonggarkan method membuat setiap route portal yang lahir nanti ikut terbuka tanpa seorang pun memutuskannya, persis arah kegagalan yang dibalik 0110. Sifat baca-saja selebihnya tetap diuji DUA lapis (tabel route Fastify + fungsi murni), lihat gotcha 7 di 0110. **(2) Satu pipeline, dua pintu:** badan submit `routes/help.ts` diangkat jadi `services/ticket-intake.ts` (`parseTicketUpload` + `intakeTicket`) yang dipanggil KEDUA route, jadi tiket kedua jalur identik SECARA KONSTRUKSI — efek samping (notifikasi, feed sync, urutan INDUK-sebelum-ANAK SPEC-382) justru yang paling gampang menyimpang karena tak punya tipe pemaksa; kelas bug SPEC-431/448/475/481. **(3) `helpEnabled` TIDAK berlaku untuk jalur portal** — keputusan eksplisit, bukan efek samping: knob itu menjawab "boleh-kah orang ASING tanpa login mengirim keluhan", sedangkan klien portal sudah lewat dua gerbang lebih kuat (akun ber-password + baris `ClientProjectAccess`). Mematikan Help Center publik — tindakan untuk menghentikan kiriman anonim — tak boleh ikut membungkam klien yang sengaja diundang. Jalur publik tetap bergantung padanya. **(4) Honeypot dicabut, rate-limit berbasis AKUN:** `hc_trap` menebak "apakah ini bot" sedangkan portal sudah tahu SIAPA; `portalTicketRateOk` memakai bucket akun (`HANOMAN_PORTAL_TICKET_RATE_PER_MIN`, default 5/mnt) lalu bucket per-project yang SAMA dengan jalur publik — satu atap per project — dengan short-circuit SPEC-352 dipertahankan supaya percobaan yang sudah pasti ditolak tak menguras jatah pelapor publik. Email dari `req.user.email`, tak pernah dari body. **(5) Respons = `toPortalTicket`**, allowlist field yang sama dengan route baca; kunci opaque pelapor sengaja TIDAK dikembalikan (klien memantau di portal, bukan lewat link status publik) dan project bukan-haknya tetap 404 generik. **Gotcha:** `parseTicketUpload` MENGURAS stream multipart — gerbang murah (akses project, rate-limit) wajib jalan lebih dulu; dan bila suatu hari `helpEnabled` terasa "kurang konsisten", keseragamannya justru bug.
- [0110 — Portal klien read-only: peran `admin|client`, akses per project, gerbang deny-by-default](0110-portal-klien-read-only.md) — **memperluas 0028**, **menegakkan 0038/0062/0064/0065** (SPEC-617): klien sebuah project tak punya cara melihat progres tanpa diberi akses penuh dashboard — Help Center (0062) hanya jalur MENGIRIM keluhan, tak ada permukaan untuk MELIHAT. Penghalangnya bukan halaman yang belum dibuat melainkan satu baris di `app.ts`: `if (user) return; // cookie sesi = akses penuh (tak ada RBAC)`. **(1) `User.role` default `"admin"`** + `disabled` — defaultnya bukan kenyamanan melainkan **syarat migrasi**: hub produksi berisi akun rekan sungguhan, jadi setiap baris lama harus tetap berakses penuh tanpa satu pun langkah backfill (migrasi murni additif; SQLite hanya melarang `DEFAULT CURRENT_TIMESTAMP`, lihat 0090). Dua peran, bukan RBAC per-permission: yang dibutuhkan satu garis antara operator dan pengamat, dan permission-set yang bisa dirakit adalah permukaan konfigurasi jauh lebih besar daripada masalahnya. **(2) `ClientProjectAccess`** = tabel join ber-`@@unique([userId, projectId])`, cascade dua sisi, dan `onUpdate: Cascade` bawaan Prisma membuat rename `Project.id` (0064) merambat gratis; kolom `Json` berisi daftar id ditolak karena tanpa FK project yang dihapus meninggalkan akses hantu. **LOCAL-only** (cermin `User`/`Session`/`AgentToken` — akun adalah kredensial per-instance) dan **tak** masuk `WEBHOOK_ENTITIES` (permukaan kredensial tak dipancarkan keluar, preseden 0097), **tapi WAJIB** di `PG_ORDER`. **(3) Gerbang deny-by-default** di `onRequest`: `clientRouteAllowed()` adalah **allowlist**, bukan peta seperti `capabilityForRoute` — `GET|HEAD /api/portal/**` · `/api/help/**` · logout · change-password · sisanya **403**. Denylist akan menyebar kewajiban ke setiap route yang lahir nanti (kelas bug SPEC-431/448/475/481) dan yang lupa gagal ke arah **terbuka**; allowlist membalik arah kegagalannya. `/api/help/**` sengaja ikut terbuka — permukaan itu sudah publik tanpa login, jadi menolaknya membuat klien yang login punya hak LEBIH SEDIKIT daripada pengunjung anonim. Upgrade WS `/api/terminal/**/ws` & `/api/events/ws` lewat hook yang sama, jadi PTY dan feed siar tertutup secara struktural. **(4) `/api/portal/*`** = lima GET yang membaca **`liveSpecs()` dan `prisma.ticket` yang SAMA** dengan dashboard (nol pipeline kedua; stage yang dilihat klien = stage live operator, 0038), diproyeksikan `toPortalSpec`/`toPortalTicket` yang menyebut fieldnya satu per satu — **bukan `Omit<>`**, yang gagal senyap persis saat kolom Prisma bertambah. `payload`/`author`/`baseSha`/`headSha`/`branchFrom`/`dependsOn`/`sourceHistory`/`reporterEmail`/`shareToken` tak pernah menyeberang; status tiket dari `publicStatus()` (SPEC-293). **(5) `/api/client-accounts`** cookie-only yang hanya menyentuh baris `role="client"` — akun operator tetap milik `/auth/users`, supaya "undang rekan" dan "beri akses klien" tak pernah tertukar. **Delapan gotcha:** default `"admin"`; letak gerbang **sebelum** bypass `/api/sync` & `/api/help` (kalau tidak, urutan cabang jadi bagian aturan keamanan); nonaktif ditegakkan di **DUA** titik — login **dan** `lookupSession` (sesi hidup 7 hari, jadi menutup login saja adalah pencabutan yang tak mencabut); `DELETE /auth/users/:id` menjaga **admin terakhir**, bukan user terakhir (kalau tidak, adanya satu akun klien membuat operator terakhir bisa dihapus dan workspace terkunci); proyeksi allowlist bukan `Omit<>`; project bukan-miliknya & project tak-ada dijawab **404 yang sama** (0062 — kalau beda, portal jadi alat enumerasi project), dan id item tak boleh jadi jalan pintas; **hook `onRequest` ber-scope Fastify tak berjalan untuk path tanpa route** sehingga "404" tak membuktikan apa pun tentang gerbang — sifat baca-saja karena itu diuji dua lapis (`app.hasRoute` untuk "tak ada route tulis", fungsi murni untuk "method tulis ditolak"); dan LOCAL-only vs `PG_ORDER` adalah dua daftar dengan aturan berbeda.
- [0109 — Ubah type/source backlog item: operasi khusus, gerbang mengunci flow, jejak berpayload](0109-ubah-source-backlog-item.md) — **menegakkan 0064/0090/0100**, **menjangkau 0038/0045** (SPEC-546): `Spec.source` ditetapkan sekali saat `POST /specs` dan tak pernah bisa diubah, sementara type sering salah tebak di awal — jadi satu-satunya jalan adalah hapus lalu buat ulang, dan itu membuang nomor SPEC, memutus riwayat/notifikasi/dependency, serta menghapus jejak triase yang menurut SPEC-520 **hanya hidup di prosa `payload`**. Koreksi scope yang mengikat: brief menyebut enam source, tapi **`cross-audit` sudah tak ada** (dicabut SPEC-384/0092) — yang berlaku persis isi `zSpecSource`, lima nilai. **(1) Operasi khusus `POST /specs/:id/source`, bukan field `PATCH`:** handler PATCH sudah memikul lima gerbang berbeda dan `source` punya gerbang yang berbeda lagi, jadi menumpuknya di sana membuat setiap kombinasi `{source, stage}`/`{source, title}` jadi pasal tersendiri; **0064 sudah memutuskan arah ini** untuk kasus sebentuk (rename `Project.id` = operasi khusus, bukan field PATCH). `payload` **opsional** — tak dikirim berarti server memakai `convertPayload`, sehingga panggilan agen lewat REST menghasilkan baris sah alih-alih 400; `source` yang sama dijawab **400**, karena no-op yang diterima diam-diam menulis jejak "brief → brief" yang mengotori satu-satunya alasan kolom jejak itu ada. **(2) Gerbangnya mengunci FLOW, bukan label** — dan inilah keputusan yang paling menentukan. Yang dilindungi SPEC-186 adalah **pekerjaan yang sedang berjalan**: sesi yang sudah lahir menulis nama fase `PIPELINES[flow]` ke berkas fase, jadi memindahkan item ber-flow `feature` (lima fase) ke `goal` (dua fase) meninggalkan berkas fase yang **tak akan pernah** memuaskan `phasesComplete` flow barunya — persis bentuk SPEC-433, keadaan yang secara struktural tak bisa tercapai. Sebaliknya `brief → help` tak menyentuh apa pun yang dipegang sesi (flow sama, bentuk payload sama, prompt sama), jadi mengunci total berarti menolak justru kasus yang paling sering disebut brief ("item Help Center ternyata fitur baru") hanya karena sesinya kebetulan sudah pernah jalan. Item yang sudah dimulai karena itu boleh pindah **hanya** ke source ber-`flowForSource` sama, dan **tanpa** payload (konversi se-flow selalu se-bentuk, jadi larangan itu tak memotong apa pun). **(3) `Spec.sourceHistory Json?` — kolom, bukan turunan** (0090: aturannya *bisakah dihitung ulang dari sumber lain*, dan kapan sebuah baris berganti type tidak bisa), append-only, **membawa `payload` bentuk LAMA utuh**. Itulah yang membuat konversi beda-bentuk aman: field tanpa padanan tak lenyap, ia pindah ke jejak — janji "tanpa kehilangan riwayat" jadi harfiah. **Tanpa cap**, sebab cap yang diam-diam membuang mematahkan satu-satunya alasan kolomnya ada. **(4) `convertPayload(to, payload)` MURNI, field-ke-field, TAK PERNAH menyambung prosa** — dipakai dialog UI untuk prefill **dan** server sebagai default; prosa yang disambung tak bisa diurai lagi, sementara operator toh ada di depan form. Bentuk asal dibaca dari **payload**-nya, bukan dari `source` lama, supaya baris yang keduanya terlanjur berselisih tetap terkonversi benar. `priority ↔ severity` memakai aturan `deriveSpecFields` yang sudah ada, dan karena petanya **3→2 nilai**, prioritas **tidak** round-trip (`rendah → minor → sedang`) — dinyatakan & diuji, yang round-trip adalah prosanya. **(5) `author` sengaja TIDAK disentuh** (`QA ·`/`Audit ·`/`Goal ·` menjawab *siapa & lewat pintu mana item difilekan* — fakta historis, cermin `createdAt` 0090); yang berpindah lencana `SOURCE_META`, yang sekalian mendapat entri **`help`** yang selama ini hilang (item Help Center memakai lencana "feature brief" lewat fallback) berikut tab filternya. **Tujuh gotcha:** (a) `sourceHistory` **wajib** di `FIELDS.spec` — `upsert` yang tak menyebut kolom tetap berhasil, jadi yang terlewat mendarat sebagai null palsu tanpa satu pun error (0090/0093/0094/0105); (b) **tak boleh** di `WEBHOOK_ENTITIES.fields` — ia membawa payload, dan `payload` memang sudah dikecualikan dari allowlist itu; (c) predikat bentuk payload wajib tetap **satu** (`shared/src/spec-source.ts`, dipanggil `zCreateSpec.superRefine` **dan** `zChangeSpecSource`) — menyalinnya balik mengembalikan kelas SPEC-431/448/475/481; (d) **tak ada salinan `source`** yang perlu ikut diperbarui: `flowForSource` dibaca saat sesi lahir oleh ketiga pemanggilnya, dan `SchedulerQueueItem.source` itu asal *checker* (`backlog`|`triase`) bukan source Spec; (e) dialog UI **mencerminkan** gerbang server, tak menggantikannya — `checkSourceChange` memeriksa bentuk payload lagi walau `zChangeSpecSource` sudah, sebab jalur non-HTTP tak boleh bisa menyelundupkan bentuk salah; (f) `sourceHistory` masuk `zSpec` ber-`.default([])`, jadi tiap literal `Spec` di test wajib menyebutnya (kolom DB `Json?` → baris lama mengirim `null`; UI menulis `?? []`, cermin `blockedBy`); (g) env test berperan **hub**, jadi test yang membuktikan konversi merambat sync memeriksa **`SyncLog`**, bukan `syncOutbox` — dan yang diperiksa isi snapshot-nya (`source` baru **dan** jejaknya), bukan sekadar ada barisnya.
- [0108 — Klausa gaya kode: satu konstanta di setiap prompt agen, tanpa knob](0108-klausa-gaya-kode-prompt-agen.md) — **memperluas POLA 0080**, **menjangkau 0094/0091/0105**, **menegakkan 0037/0098** (SPEC-543): lubangnya identik dengan yang ditutup ADR-0080 untuk scope verifikasi — prompt sesi bicara fase, otonomi, skill, commit, dan push, tapi tak sekali pun menyebut bentuk kode yang diharapkan, jadi tiap sesi jatuh ke kebiasaan default modelnya dan kebiasaan itu adalah **menarasikan kode yang baru saja ditulisnya**. Ironinya repo ini punya konvensi yang berlawanan arah dan tak pernah menyatakannya ke agen mana pun: komentar panjang di `verify-scope.ts`/`brain.ts` bukan narasi kode melainkan rekaman **alasan, angka terukur, dan rujukan SPEC/ADR**. Aturan yang berlaku di sini karena itu bukan "sedikit komentar" melainkan "komentar yang membawa informasi yang tak ada di kode" — dan klausa yang terbaca sebagai "kurangi komentar" akan menghapus justru yang tak bisa dipulihkan, jadi larangannya dinyatakan sebagai **mengulang**, disertai butir yang menyebut apa yang JUSTRU layak dikomentari. Mekanismenya satu konstanta `CODE_STYLE_CLAUSE` (`runner/src/code-style.ts`, tetangga `verify-scope.ts`) yang dipasang di **enam** permukaan: empat builder prompt sesi backlog/goal (digerbangi `writesCode(flow)` yang sudah ada — **bukan** daftar flow yang disalin, sebab dua definisi "sesi ini menulis kode" akan berselisih saat flow baru lahir seperti `goal` di SPEC-407), tiga pintu konflik rebase/merge yang merakit prompt-nya **inline di route** sehingga tak terjangkau gerbang itu, `agentPromptOf` custom agent, `leadPrompt`, dan `changelogPrompt`. **Tiga keputusan yang menentukan bentuknya:** (1) **gerbangnya hidup DI DALAM teks klausa** (baris pertama: "berlaku setiap kali kamu menulis atau mengubah kode") — tanpa itu konstanta yang sama harus bercabang jadi dua varian untuk prompt yang keluarannya bukan kode (lead, narator changelog), dan dua varian teks yang wajib tetap sepakat adalah kelas bug "satu definisi, N call site" (SPEC-431/448/475/481) tanpa satu pun tipe yang memaksanya konsisten; (2) **`agentRosterBlock` codex sengaja TIDAK menerimanya** — roster itu ditempel ke akhir prompt sesi yang sudah membawa klausa, sementara subagent claude dari `--agents` punya konteks **terpisah** yang tak pernah melihat prompt sesi, jadi jalur claude adalah satu-satunya yang harus membawanya sendiri; (3) **tanpa knob**, berbeda sadar dari 0080 — scope verifikasi punya `Setting` karena biayanya nyata dan ada keadaan di mana `full` benar, sementara "sesi ini boleh menulis komentar yang mengulang kode" bukan pilihan yang masuk akal untuk ditawarkan, dan knob yang tak pernah benar digeser hanyalah permukaan konfigurasi yang harus dijaga selamanya. **Dua gotcha:** klausa ini adalah **muatan `pkill -f`** (prompt hidup di ARGV agen — sebab SPEC-402, di sana dipicu `vitest`/`tsc` di dalam klausa scope), jadi nama perintah dilarang masuk dan dijaga test; dan bukti "terkirim" **wajib dibaca dari pane tmux sesi sungguhan**, bukan dari `startPrompt()` — yang dikhawatirkan spec ini justru call site yang lupa memanggil builder-nya, dan assertion atas builder secara struktural buta terhadap itu. Mengarahkan, bukan memaksa: nol hook deny, 0037 utuh.
- [0107 — Paginasi seragam seluruh daftar dashboard: amplop tunggal, `skip`/`take` sah tanpa overlay, tiga pengecualian dinyatakan](0107-paginasi-seragam-daftar-dashboard.md) — **memperluas 0038**, **menegakkan 0039/0087/0092** (SPEC-523): 0038 menetapkan POLA paginasi hanoman (amplop `{items,total,page,pageSize}`, dipotong di layer response, `serverPage()` + `Pager` di design system) tapi tak pernah menetapkan JANGKAUANNYA — puluhan fitur kemudian pola itu baru dipakai `GET /specs` dan `GET /projects`. Audit atas instalasi hidup memberi angkanya: notifikasi `take: 50` hardcode dengan **287 baris** (237 tak terjangkau), `GET /lead/decisions` `{items}` tanpa `total` atas **393 baris**, antrean scheduler (**56**) ikut utuh di `GET /scheduler/state` lalu difilter di klien, dan tiket serta changelog punya server beramplop yang **UI-nya tak pernah memakai**. Changelog paling tajam: UI memanggil `{limit: 10}` **tanpa `page`**, jadi `limit` di sana bukan halaman melainkan **PLAFON** — item ke-11 dst permanen tak terjangkau; cacat yang **tak bisa ditangkap satu pun test render**, hanya test yang memeriksa argumen panggilannya. Keputusan intinya sederhana (semua daftar utama memakai amplop & paginator yang sama, tanpa paginator kedua), yang mahal adalah **batas-batasnya**. `skip`/`take` di query DB dinyatakan **SAH** untuk daftar tanpa overlay: larangan 0038 mengikat `GET /specs` secara spesifik karena overlay stage-live + write-through + notifikasi `done` di sana bergantung set penuh — ia **bukan** aturan umum, dan membacanya begitu akan memaksa memuat 287/393 baris ke memori tiap request tanpa alasan. **Tiga pengecualian ditulis eksplisit supaya audit berikutnya tak 'memperbaikinya':** (1) **git graph tetap jendela tumbuh** SPEC-351 — lane dihitung dari commit **KONTIGU**, jadi halaman diskrit memutus tautan induk–anak di batas halaman dan mencabut auto-scroll; premis brief ('limit hardcode 200') memang sudah usang, 200 kini ukuran langkah. Yang kurang cuma angkanya, jadi `total` ditambahkan lewat `git rev-list --count` dengan **ref selector yang SAMA** dengan `git log`-nya (kalau berbeda, angkanya menghitung ref yang tak digambar); (2) **docs project tetap pohon** (`{coverage, tree}` kategori→berkas, memenggalnya memutus navigasi); (3) **error monitoring tak punya daftar** — dicabut 0092, brief menyebutnya dari ingatan permukaan lama. **Notifikasi: bell adalah BAKI, arsip adalah DAFTAR.** `notificationsFeed()` tanpa argumen **tetap 50 teratas** karena ia memberi makan frame siar WebSocket tiap 3 detik (0039) — menyiarkan seluruh riwayat tiap 3 detik regresi biaya, bukan perbaikan; yang ditambahkan `total` supaya 50 berhenti berpura-pura jadi semuanya, dan halaman 2+ hidup di modal arsip ber-`Pager`. `unread` **selalu** dari seluruh baris: lencana yang mengecil saat operator membuka halaman 2 adalah kebohongan. **Satu field dihapus dari kontrak:** `GET /scheduler/state` berhenti mengirim `queue`, diganti `queueCounts`; alternatif 'kirim `queue` yang dipotong diam-diam' **ditolak** — daftar terpotong yang tampak utuh adalah kelas bug SPEC-431/451/475. Daftar lead **menambah** `page`/`limit` tanpa mencabut `take`/`skip` (aditif; `page`/`limit` menang bila keduanya dikirim), dan penurunannya hidup di **satu** tempat (`lead/page.ts` `leadWindow()`) — menyalinnya ke `trail.ts` dan `flow.ts` persis kelas bug SPEC-431/448/475. **Gotcha:** (1) modal arsip wajib mengimpor DS dari `ds/kit` **langsung**, bukan barrel `../ds` — barrel mengekspor `shell.tsx` yang mengimpor `NotificationBell`, dan itu menutup siklus impor; (2) test SPEC-362 yang mengunci muat-lebih (append) sebagai kontrak wajib diperbarui — pola SPEC-433, test yang mengunci bug/pola lama sebagai kontrak; (3) jumlah halaman diturunkan dari ukuran halaman yang **DIMINTA**, bukan dari panjang `items`, jadi mock test ber-`pageSize` mungil tapi `total` kecil tak akan pernah memunculkan halaman kedua. Dijaga `server/test/pagination-contract.test.ts`: satu konstanta berisi **10 endpoint** yang wajib beramplop dan menghormati `limit` — daftar baru yang lahir tanpa halaman punya satu tempat yang menolaknya. **Ponytail:** `paginate()` tak menjepit `limit` dari atas; bila agen mulai meminta `limit=999999`, jepit di satu tempat itu, tanpa ADR baru. **SPEC-647 menerapkannya ke portal klien** (tanpa ADR baru): endpoint portal `backlog`/`tickets` sudah beramplop & sudah membaca `page`/`limit` sejak ADR-0110 — yang tak pernah ada adalah **kliennya**, `src/src/api/portal.ts` memanggil keduanya tanpa satu pun parameter sehingga `paginate()` membalas SELURUH baris dan `ClientPortal` merendernya sekaligus; pencegahannya kini **struktural** (satu argumen `PortalPage = {page,limit}` yang tak bisa dikirim setengah), dan angka di tab portal wajib `total` — cermin aturan `unread` di atas. Pemilih project portal adalah **pengecualian keempat yang dinyatakan**: beramplop `paginate()`, tapi tanpa kontrol halaman, karena project terpilih yang jatuh dari halaman justru mematahkan syarat “perpindahan halaman mempertahankan konteks”.
- [0106 — Pembatalan antrean scheduler: status `canceled` sebagai tombstone, dua endpoint CAS](0106-batalkan-antrean-scheduler.md) — **mengamandemen keputusan #2 dari 0072**, **menegakkan 0015/0016/0065**, memperluas pola tombstone SPEC-431 (SPEC-522): panel scheduler menampilkan antrean tapi tak punya satu pun tombol yang menyentuh sebuah baris, jadi jalan keluar dari antrean cuma dua dan keduanya kasar — **menunggu** item meluncur lalu menutup sesinya, atau menarik **rem global** Pause/Stop yang menghentikan seluruh antrean demi satu baris. Menunggu bukan sekadar tak nyaman: peluncuran membuat worktree + branch `hanoman/<sessionId>` dan menulis `Spec.baseSha`/`startedAt` — stempel *mulai pertama* yang menurut 0090 sengaja **tak pernah** ditulis ulang — lalu membakar kuota agen; membiarkan item meluncur hanya supaya bisa dimatikan adalah kerusakan yang tak perlu. **(1) `canceled` sebagai nilai kelima `status`.** 0072 #2 mengeja kosakatanya (`queued|launched|done|failed`), jadi menambahnya adalah amandemen — bukan sekadar detail implementasi. Kolomnya `String` → **tanpa migration**; tabelnya LOCAL-ONLY sehingga nilai baru ini tak menyeberang antar-instance, dan itu benar (antrean = state operasional mesin ini). **(2) Tombstone, bukan `DELETE`.** Barisnya sengaja ditinggalkan: `enqueue()` memakai `upsert` ber-`update:{}`, jadi checker `backlog` yang menjumpai spec yang sama pada cadence berikutnya tak bisa menghidupkannya. Menghapusnya justru membuat pembatalan **membatalkan dirinya sendiri** dalam ≤1 cadence, karena spec-nya masih cocok `UNSTARTED_SPEC_WHERE` — mekanisme yang sama sudah dipakai SPEC-431 (`markDone` + `ALREADY_DONE_NOTE`). **(3) CAS, bukan baca-lalu-tulis.** Kedua transisi ditulis `updateMany({ where: { id, status: <asal> } })` dan dinilai dari `count`; di antara `findUnique` dan `update` governor bisa meluncurkan barisnya, sehingga kendala "item yang sudah punya sesi aktif tak boleh dibunuh diam-diam" hanya bisa ditegakkan **struktur**, bukan niat. Alasan penolakan disusun **sesudah** CAS gagal (404 bila barisnya hilang, 409 + `status` saat ini bila transisinya haram) — memeriksa lebih dulu adalah persis balapan yang dihindari. **(4) Reversibel.** `requeue` ada karena tombstone-nya permanen secara mekanis: tanpa jalan pulang, "Batalkan" diam-diam berarti "jangan pernah dijadwalkan lagi"; konsekuensinya UI tak perlu dialog konfirmasi. **(5) Dua gerbang governor.** `queued()` memang menyaring `canceled`, tapi daftar itu **snapshot** — `drain()` memprosesnya berurutan dan tiap `launch` men-spawn worktree + sesi tmux (hitungan detik), jadi item di ekor daftar bisa duduk puluhan detik di dalam loop. Gerbang A (`isQueued`, dibaca ulang dari DB, pola `isDone` SPEC-431 & `blockers` SPEC-447) ditaruh di **puncak badan loop** — bukan tepat sebelum `launch` — supaya ia melindungi semua mutasi di sana: baris `canceled` tak boleh ditimpa jadi `done` oleh gerbang SPEC-431 maupun jadi `launched` oleh cabang idempoten `isLive`. Gerbang B = `markLaunched` jadi CAS, menutup sisa jendela seluas satu spawn. **(6) Sesi yang telanjur lahir tak dibunuh.** Saat gerbang B menyala sesinya sudah nyata: governor mencatat id-nya di `note` (operator menutupnya dari Terminal) dan **tetap** `slots--`, karena cap concurrency tak boleh dilanggar hanya karena barisnya dibatalkan. `reconcile()` tak tersentuh — ia hanya memindai `launched`, jadi tak ada `Notification fail` palsu untuk item yang sengaja dibatalkan. **Gotcha test:** `vi.spyOn(prisma.<model>, "update").mockRestore()` **menghapus** metodenya alih-alih mengembalikannya (delegate Prisma menyajikannya lewat proxy, jadi tak ada yang mengambil alih setelah own-property vitest dibuang) — setiap test sesudahnya di berkas yang sama kehilangan `update`; simpan referensinya dan pasang balik sendiri.
- [0105 — Changelog per project: `Spec.doneAt` berkolom, hasil tersimpan LOCAL-only, narasi agen ber-fallback](0105-changelog-per-project.md) — **melanjutkan arah 0090**, **menegakkan 0018/0019, 0033, 0078, 0091, 0099, 0100** (SPEC-516): hanoman menyimpan seluruh bahan sebuah changelog tapi tak punya permukaan yang menyajikannya untuk manusia non-teknis. Tiga hambatan muncul sekaligus. **(1) Tak ada stempel "selesai".** `Spec` punya `createdAt`/`startedAt` sejak 0090 tapi tak punya kolom yang menjawab *kapan item ini selesai*, dan `updatedAt` **bukan** proksinya (mesin sync mem-*bump* `version`, overlay stage-live menulis tiap `GET /specs` dibaca). Kolom `doneAt` karena itu ditambahkan dengan arah yang **berlawanan dengan 0018/0019** dan itu disengaja: aturannya bukan "selalu turunkan" melainkan *bisakah dihitung ulang* — coverage bisa, diff bisa, kapan sebuah baris selesai tidak. Penulisnya **satu**: bukan di ketiga jalur yang mempersist `stage="done"` (`advanceStage` · `scheduler/reconcile` · `live-specs`) melainkan **di dalam `recordCompletion()`**, satu-satunya fungsi yang sudah dipanggil ketiganya dan yang oleh 0103 sudah ditetapkan sebagai stempel selesai — menyalin efek samping ke banyak call site adalah kelas bug SPEC-431/448/475, dan efek samping (tak seperti predikat) **tak punya tipe yang memaksanya konsisten**. **Tulis-sekali** (`updateMany` ber-guard `doneAt: null`) sehingga maknanya *selesai pertama*, cermin `startedAt` dan cermin idempotensi 0033; revert stage **tidak** mengosongkannya. Backfill sekali-jalan dari `Notification` ber-key `done:<specId>` — sumber yang sama dengan sweep 0103; item pra-SPEC-180 tetap null dan **dilaporkan sebagai catatan**, bukan disamarkan. **(2) Sumbernya teknis, keluarannya tidak boleh.** Narasi dihasilkan satu panggilan agen one-shot lewat **`think()` yang DIIMPOR** dari `lead/brain.ts` — bukan kenyamanan melainkan inti keputusannya: titik spawn agen ketiga akan mengulang SPEC-448, di mana `rootBypassEnv` ada di `pty.ts` tapi tak pernah menyeberang ke `brain.ts` dan lead gagal **100 %** di setiap instance ber-`User=root` (konfigurasi deploy RESMI). Prompt **menyebut anggaran waktunya sendiri** (SPEC-432: 306 dtk → 101 dtk). Agen gagal/kosong/tak terpasang **bukan galat** — baris tetap lahir dengan draf deterministik + `warning`, jadi fitur hidup di mesin tanpa CLI agen dan seluruh logikanya bisa diuji tanpa men-spawn apa pun. Scrub dijalankan **dua kali** dan yang menentukan sisi **INPUT**: SHA bahkan tak pernah dikumpulkan dari `git log`, bukan sekadar tak dirender. **(3) Repo tanpa remote/tag adalah keadaan lumrah**, bukan kegagalan: pengumpul memulangkan `{ok:false, reason}` berbahasa manusia → **422 + pesan**, dan `GET …/changelog/sources` menjawab **200 dengan `reason`** supaya form menjelaskan alasannya sebelum tombol ditekan. Hasil disimpan sebagai model **`Changelog` LOCAL-only** (tanpa kolom `version`, cermin `LeadFlow`/`WebhookEndpoint`/`Project.autoMerge`) karena dua dari tiga modenya diturunkan dari checkout di mesin ini; yang portabel keluarannya, dan jalannya sudah ada lewat unduh `.md` (0078). Capability **domain `docs`**, bukan `projects` — tanpa entri eksplisit ia jatuh ke `rw("projects")`, artinya agen harus dipercaya menyunting & menghapus project hanya untuk membaca changelog. **Non-goal sadar:** tanpa tool MCP (0099 punya versi skema sendiri), tanpa peristiwa webhook (0100 — artefak atas permintaan, bukan perubahan keadaan), tanpa sync, tanpa penjadwalan, tanpa terbit ke luar. **Lima gotcha:** (1) `PG_ORDER` wajib memuat model baru **sesudah `Project`** — `cli/test/migrate-pg.test.ts` menuntutnya sama persis dengan DMMF dan itulah satu-satunya gerbangnya; (2) `doneAt` wajib di `FIELDS.spec` **dan** `DATE_FIELDS.spec`, sebab `upsert` yang tak menyebut sebuah kolom **tetap berhasil** (kelas gagal-senyap 0090/0093/0094); (3) batas hari wajib **LOKAL** (`new Date("2026-07-31")` = tengah malam UTC → hampir seluruh hari itu terbuang di WIB); (4) regex scrub camelCase wajib menuntut ≥2 huruf kecil di **kedua** sisi kapital, kalau tidak `macOS`/`iOS` ikut terbuang, dan regex hash wajib menuntut satu digit **dan** satu huruf a–f, kalau tidak `1000000` terbaca sebagai sha; (5) "versi sebelumnya" diturunkan `git describe --abbrev=0 <tag>^` (**riwayat**), bukan urutan tanggal — tanggal tag anotasi beresolusi DETIK, dua rilis di menit yang sama berakhir seri dan git jatuh ke urutan NAMA (terukur di fixture: `v1.0.0` mendahului `v1.1.0`).
- [0104 — Kehadiran gateway Telegram adalah indikator typing, dan long-poll adaptif yang jadi denyutnya](0104-telegram-typing-indicator-long-poll-adaptif.md) — **mengamandemen 0096 §5**, **menegakkan 0024** (SPEC-493): SPEC-491 memberi gateway sebuah suara supaya chat tak lagi diam total, tapi bentuknya **pesan teks durable** — satu baris `TelegramOutbox` ber-kind `gateway-progress` tiap update di-dispatch. Ongkosnya terbaca langsung di layar user: 2026-08-01, **7 update → 7 pesan robot** sebelum satu pun jawaban asli keluar, dan satu-satunya kendali (`Setting.telegram.progress`) semua-atau-tidak-sama-sekali sehingga mematikannya mengembalikan diam total pada giliran yang bisa makan **95 detik**. Yang menggantikannya adalah bentuk yang memang disediakan Telegram: `sendChatAction` "typing…" — **sesaat, tak meninggalkan jejak**. Yang mahal bukan panggilan API-nya melainkan **denyutnya**: status typing padam ~5 detik, Telegram **tak punya API stop-typing**, sementara gateway hanoman satu loop sekuensial yang **memblokir 25 detik** di `getUpdates` dan ADR-0024 melarang timer baru. Jawabannya **long-poll adaptif**: selama ada `TelegramUpdate` `dispatched` tanpa balasan final, timeout `getUpdates` turun 25 → **4 detik**, jadi iterasi loop yang SUDAH ADA menjadi tick typing **sekaligus** `flushOutbox()` — yang bertambah cuma satu argumen, bukan sumber waktu, dan jeda kirim balasan **10,8/11,3/11,9 detik** yang terukur ikut hilang sebagai efek samping. Keaktifan **diturunkan** dari `TelegramUpdate.state` + `TelegramOutbox.kind` (nol kolom, nol migration, restart pulih sendiri); `TelegramChat.lastProgressKey` tetap tak dipakai. `gateway-failure` **TETAP pesan teks** dan tetap tak digerbangi `progress` — indikator yang hilang diam-diam tak bisa membedakan "sudah dijawab" dari "pesanmu hilang". **Enam gotcha:** (1) `retry_after` hidup di **BADAN** respons 429 dan `call()` dulu melempar sebelum membacanya → cooldown akan selamanya memakai default **dengan test hijau**; (2) umur menunggu wajib berpagar (10 mnt) — update yang sesinya mati mengendap `dispatched` selamanya dan mengunci long-poll di 4 detik selamanya; (3) arm pasca-chunk **memaksa**, refresh **ter-throttle** — tertukar berarti diam persis saat paling dibutuhkan, atau banjir saat update beruntun; (4) poll adaptif **tetap hidup** saat `progress` mati (flag itu menggerbangi suara, bukan latensi); (5) kosakata kind duduk di `protocol.ts` — dua pemakainya gateway **dan** store, menaruhnya di gateway = siklus import; (6) `decision`/`confirmation` **final** (giliran kembali ke manusia). Seluruh state typing in-memory dan **tak satu pun method-nya bisa melempar**: typing kosmetik, jalur at-most-once update/outbox (0096 §4) tak tersentuh.
- [0103 — Auto-merge saat sesi selesai: kebijakan per project/spec, dieksekusi sweep tanpa call site](0103-auto-merge-saat-sesi-selesai.md) — **memperluas 0031**, **menegakkan 0002/0030/0033/0072/0100** (SPEC-486): ADR-0031 memberi *tombol* merge dari dashboard tapi tak pernah memberi *kebijakan* — setiap backlog yang selesai harus digabungkan dengan tangan, satu per satu. Yang ditambahkan cuma dua hal: blok kebijakan `Json?` (`Project.autoMerge` default + `Spec.autoMerge` override; `null` = off / warisi) dan **pemicunya**. Mesin merge-nya `integrate()` ADR-0031 apa adanya, dengan kosakata target yang sama persis (`local:<b>` / `origin:<b>`) supaya dropdown, gerbang validasi, dan setting berbicara satu bahasa. Keputusan yang mahal adalah **di mana ia dipicu**: `stage = "done"` dipersist di **TIGA** jalur (`live-specs`, `scheduler/reconcile`, `DELETE /terminal/sessions/:id`), dan menempelkan efek samping di ketiganya adalah kelas bug yang sudah digigit repo ini **empat kali** (SPEC-431 predikat, SPEC-448 env spawn, SPEC-475 efek samping, SPEC-481 emit peristiwa). Lebih menentukan lagi: **tak satu pun dari ketiganya aman** — prompt sesi menyuruh agen menulis baris fase terakhir LEBIH DULU, baru `commit` + `git push`, jadi `liveSpecs` bisa memindahkan stage ke `done` beberapa detik sebelum `hanoman/<spec>` ada di origin. Karena itu pemicunya **sweep periodik dengan NOL call site** (cermin tap Prisma ADR-0100 & worker antrean ADR-0072): kandidat diambil dari notifikasi `done:<specId>` yang sudah ditulis `recordCompletion` di ketiga jalur — stempel "kapan selesai" yang **sudah ada**, jadi tanpa tabel baru — disaring **window 24 jam** (satu-satunya pagar yang mencegah menyalakan setting menggabungkan seluruh sejarah project) dan penanda `automerge:<specId>` yang merangkap laporan + idempotensi durable lintas restart. Kesiapan branch diuji `headSha ⊆ tip` (`merge-base --is-ancestor`), bukan sekadar "branch ada"; belum siap dalam **grace 15 menit** → diam & coba lagi, lewat grace → menyerah dengan suara. Operasi **terkunci ke `merge`** (rebase selalu force-push, dilarang batasan) dan branch kerja tak pernah dihapus sebelum hasil `clean` — `deleteBranch` knob opt-in, default mati, keputusan operator saat brainstorm. Konflik **tidak** melahirkan sesi agen (membakar kuota tanpa diminta; yang diminta objective adalah notifikasi berisi alasan) — sweep justru **membuang** worktree `merge-<spec>` yang sengaja ditinggalkan `integrate`, dan branch kerja tetap utuh sehingga tombol Rebase / Merge ADR-0031 masih memberi jalur konflik yang lengkap. Kolomnya **LOCAL-only** (cermin `repoDir`/`schedulerOptIn`): nama branch tujuan properti checkout mesin ini. Gotcha yang mengikat: reopen-lalu-selesai-lagi **tak** di-auto-merge ulang karena `recordCompletion` idempoten (cermin batasan ADR-0033); `Prisma.DbNull` bukan `null` polos untuk mengosongkan kolom `Json?`; default branch diresolve **saat eksekusi** dan **tak pernah** di-hardcode `"main"` (SPEC-227/ADR-0077 — `origin/HEAD` → main → master → **null**, sebab merge ke branch yang salah tak bisa dibatalkan dari dashboard)
- [0102 — Pilihan lead jamak & rantai keputusan: `LeadFlow` sebagai alur, jawaban selalu daftar](0102-lead-multi-select-dan-rantai-keputusan.md) — **memperluas 0098** & SPEC-452/474, **menegakkan 0091**, **tidak menyentuh 0024/0037/0039** (SPEC-485): permukaan keputusan lead punya dua batas yang tak pernah dinyatakan, dan seperti SPEC-479 keduanya jatuh ke **bentuk kode**. Yang pertama, `choice` adalah satu `string` dan kolomnya sepasang skalar — peminta yang opsinya tak saling eksklusif hanya bisa menuang jawabannya ke prosa (membatalkan yang dibangun ADR-0098) atau memanggil ulang `POST /lead/decisions`, satu proses `claude -p` per panggilan di atas gerbang berkapasitas 2. Yang kedua terukur in-vivo pada **claude 2.1.220**: dialog `AskUserQuestion` ber-`multiSelect` adalah widget yang **berbeda**, dan empat perbedaannya masing-masing cukup untuk merusak jalur SPEC-452/474 — (a) tiap label diawali kotak centang sehingga `"[ ] Type something"` tak lagi cocok `PLACEHOLDER` → `freeIndex === null` → `sendToPane` jatuh ke jalur prosa+`Enter`; (b) **digit MEN-TOGGLE** (`b = toggleValue` di biner), kebalikan penuh dari single-select yang memilih seketika; (c) ada tombol kirim **tanpa nomor** (`Submit`, atau **`Next`** bila pertanyaannya belum yang terakhir), dan karena tombol itu ada `Enter` di baris opsi men-toggle baris tersorot alih-alih mengirim → jalur lama men-toggle **opsi 1** lalu berhenti: layar tak maju, marker tak dikosongkan, `MAX_CHAIN_STEPS` habis, `failures` naik — persis gejala "hanya bisa dipilih satu" + hang yang dilaporkan; (d) kolom bebas hanya bisa dicapai lewat **navigasi**, dan panah pun **satu keystroke per `send-keys`** (terukur: empat panah dalam satu pemanggilan = satu perpindahan — jebakan burst ADR-0085 ternyata tak berhenti di teks). Keputusannya empat. **(1)** Jawaban **selalu daftar** di penyimpanan (`choices Json?` + `select Json?`), `choice`/`choiceIndex` tinggal turunan `choices[0]`, dan `toDecisionView` **menurunkan** `choices` dari kolom lama untuk baris pra-migrasi — itulah yang membuat riwayat lama terbaca sesudah perubahan skema. `resolveChoice` tak disentuh; `resolveChoices` memanggilnya per item (satu definisi pencocokan — menyalinnya kelas bug SPEC-431/448/475), dan `optionActionHint` hanya berlaku saat pilihannya **tepat satu**. **(2)** `LeadFlow` menjadikan alur sebuah entitas ber-status `menunggu`/`sebagian`/`selesai`/`dibatalkan`, dibuat di `decide()` — choke point tunggal yang sama yang sudah memegang gerbang SPEC-479. **Setiap** keputusan punya alur; yang tak berantai ditutup seketika, yang ber-`chain` terbuka sampai `POST /lead/flows/:id/submit`, dan `flowId` ke alur **tertutup** ditolak **409** — bentuk teknis dari "tak bisa menyisipkan pertanyaan ke rantai yang sudah di-submit". Alur tak pernah menunggu manusia: endpoint tetap **sinkron**, ADR-0091 utuh. **(3)** `lead.flowTtlMin` (default 60, kolom `Json` → tanpa migration) memberi tiap alur `expiresAt`, dan penyapunya **menumpang tick engine lead yang sudah ada** — ADR-0024 melarang timer baru. **(4)** Dialog multi dijawab dengan **mencentang**: toggle per opsi (satu karakter, lalu **dibuktikan** kotaknya berubah) → navigasi ke kolom bebas (satu panah per pemanggilan, dibuktikan lewat posisi `❯`) → prosa ber-`goalChunks` → navigasi ke tombol kirim → `Enter`; fail-closed di tiap langkah, dan `sendToPane` karena itu menerima `choices` sebagai **data**, yang sekalian menyembuhkan pintu override operator. Validasi berlapis dua: bentuk `select` yang mustahil dipenuhi ditolak **400** di pintu masuk, sementara jumlah pilihan di luar `min`/`max` **membatalkan seluruh pilihan** (bukan memangkasnya — mengambil dua dari tiga secara sewenang-wenang persis tebakan yang ADR-0098 hapus) tanpa menulis ulang `kind` (idempotensi denyut, SPEC-432). **Gotcha paling mahal:** `dialogKey` untuk layar multi **wajib membuang penanda `☐/☒` tab strip** — mencentang satu opsi sudah membalik tab yang sedang tampil menjadi `☒` tanpa satu pun pertanyaan berpindah, dan kunci yang ikut berubah membaca layar yang **macet** sebagai layar yang **maju**, cacat yang sama persis yang SPEC-474 tutup untuk label kolom bebas lewat pintu baru. **Alternatif yang ditolak:** alur opt-in (permukaan yang hanya kadang punya keadaan selalu harus ditanya dua kali), alur asinkron yang menunggu manusia (membalik ADR-0091), memangkas pilihan yang melebihi `max` (menukar kesalahan terlihat dengan kesalahan tak terlihat), menjawab dialog multi cukup lewat kolom bebas (maksudnya sampai, kotaknya tetap kosong), dan memasukkan `LeadFlow` ke `WEBHOOK_ENTITIES` (katalog itu selektif — `SchedulerQueueItem` juga di luar).
- [0101 — Form Custom Agent berbasis katalog: `runtime` sebagai penyaring, katalog tool turunan konfigurasi MCP](0101-form-custom-agent-katalog-runtime.md) — **memperluas 0094 & 0074**, **mengikuti 0045 & 0065**, **tidak menyentuh 0037**, **tidak mencabut apa pun** (SPEC-484): form Custom Agent lahir bersama ADR-0094 dengan tiga field bebas-ketik untuk data yang sumbernya pasti, dan biayanya bukan estetika melainkan kelas kegagalan yang **sudah diukur di ADR-0094 sendiri** — M4: nama tool tak dikenal **dibuang claude tanpa satu pun pesan**, jadi `read, bash` huruf kecil melahirkan agen **tanpa alat apa pun**, exit 0, sesi jalan, nol keluhan. Field keempat belum ada sama sekali: hanoman punya **dua** mesin sesi yang materialisasinya sengaja berbeda (claude → subagent `--agents`; codex → roster prosa inline), tapi persona yang ditulis untuk salah satunya tetap disodorkan ke keduanya. Keputusan intinya **apa arti `runtime`**: ia **PENYARING**, bukan pemilih biner — ia menyaring apa yang masuk roster sesi, bukan proses mana yang dijalankan, sebab melahirkan codex dari dalam sesi claude adalah **titik spawn ketiga** dan SPEC-448 membuktikan tiap titik spawn membayar ulang seluruh pelajarannya. Kolomnya **nullable tanpa default** dan `null` berarti **"ikut sesi induk"**, jadi migration tak perlu backfill dan setiap baris lama berperilaku persis seperti sebelumnya; alternatif "wajib claude|codex + backfill ke claude" **ditolak** karena rilisnya akan mencabut seluruh roster dari sesi codex **tanpa satu pun operator memintanya** — perubahan perilaku yang tak diminta, dibungkus sebagai perubahan form. Penyaringnya hidup di `agentDefsFor(projectId, agent)` di belakang `registerCustomAgentSource`, jadi `pty.ts` tetap nol dependensi DB dan `createSession` tetap pintu satu-satunya (ADR-0094 keputusan 7). Katalog tool = `DEFAULT_AGENT_TOOLS` + **satu entri per SERVER MCP** (`mcp__<server>__*`) + pintasan `*`: nama tool MCP yang sebenarnya hanya bisa diketahui dengan **menyambung** ke servernya (= melahirkan proses, arah yang ditolak ADR-0094), sementara nama **server**-nya bisa dibaca dari tiga berkas konfigurasi (`~/.claude.json` global + per-path, `<repoDir>/.mcp.json`, `~/.codex/config.toml`) — semuanya **gagal-terbuka**. Katalog bawaannya **persis** `DEFAULT_AGENT_TOOLS`, bukan daftar kedua yang lebih panjang: menawarkan nama yang belum diukur (M4 membuang `TodoWrite` senyap) berarti menawarkan pilihan yang **tidak melakukan apa-apa** — kegagalan yang sama, hanya dari sisi UI. `*` disimpan sebagai `tools:["*"]` dan **di-expand sebelum materialisasi**, karena tiga nilai wajib tetap berbeda (`null` = pakai default · `[]` = sengaja tanpa tool · `["*"]` = semua): meneruskan `"*"` apa adanya membuat claude membuangnya senyap, sementara menerjemahkannya jadi "kosongkan `tools`" membuat agen mewarisi **seluruh** tool termasuk `Task` dan **lapis 2 anti-loop lenyap tanpa jejak** (gotcha 5 ADR-0094) — ekspansinya karena itu di `agentDefsFor()`, dan `runner/src/custom-agents.ts` tak pernah melihat `"*"`. Validasi katalog **KERAS** (400 yang menyebut nilainya), dengan satu klausa yang membuatnya bisa hidup: **hanya field yang ADA di payload** yang divalidasi, sehingga `PATCH {enabled:false}` pada baris warisan ber-`model` asing tetap 200 — tanpa klausa itu gerbangnya mengunci saklar aktif/nonaktif setiap baris lama; pengecualiannya `model` **ikut** divalidasi saat hanya `runtime` yang berubah, sebab menukar runtime bisa membuat model tersimpan jadi tak sah. Daftar **mention** sengaja tak ikut endpoint katalog — ia sudah hidup di `GET /custom-agents?projectId=` lengkap dengan aturan project-menimpa-global, dan dua sumber untuk satu daftar adalah cara dua daftar mulai berbeda. **Enam gotcha:** `runtime` wajib masuk `FIELDS.customAgent` (kolom terlewat menyeberang sebagai **default palsu tanpa error**, kelas ADR-0090/0093); `agent` yang dipakai penyaring wajib `agentForDefs` milik `createSession`, bukan `Setting.agent` (membaca yang salah mengulang SPEC-377 dalam bentuk baru — sesi bisa lahir dengan agen override per-request); `"*"` bercampur nama lain **ditolak**, bukan digabung; validasi model memakai runtime **efektif** (`payload.runtime` bila ada, selain itu nilai baris) — `?? null` membuat setiap `PATCH {model}` pada agen codex divalidasi terhadap gabungan katalog dan lolos untuk model claude; sub-tabel TOML **bukan server** (terukur saat smoke: `[mcp_servers.context7.http_headers]` melahirkan "server" palsu `context7.http_headers` → entri `mcp__context7.http_headers__*` yang tak pernah bisa ada, 10 → 8 sesudah diperbaiki — kelas kegagalan yang sama, hanya lahir dari sisi hanoman); dan migration ditulis tangan lalu `migrate deploy`, sah di SQLite justru karena kolomnya **nullable tanpa default**. Konsekuensi yang diterima sadar: definisi sah di mesin rekan (server MCP berbeda) **tak bisa disimpan ulang** di sini sampai chip invalidnya dibuang — alternatif "validasi lunak, terima lalu tandai" ditolak operator karena nilai salah ketik akan tetap tersimpan dan gejalanya tetap muncul saat sesi berjalan, yakni bug yang sama dengan lencana.
- [0100 — Webhook keluar: tap Prisma sebagai satu sumber peristiwa, amplop ber-versi, antrean SQLite](0100-webhook-keluar-peristiwa.md) — **memperluas 0049 & 0097**, **tidak menyentuh 0024/0037/0039/0072/0079/0096** (SPEC-481): sistem lain tak punya cara mengetahui apa yang terjadi di hanoman selain memanggil `/api` berkala, dan akibatnya terlihat di repo ini sendiri — gateway Telegram (ADR-0096) adalah **satu subsistem penuh** yang lahir karena tak ada kanal peristiwa keluar. Yang dicabut ADR-0024 dulu adalah webhook **masuk** berikut message queue-nya; ini kebalikannya, satu arah keluar, tanpa menghidupkan apa pun yang dibuang di sana. Keputusan intinya **di mana peristiwa diambil**: satu Prisma client extension di `db.ts` (`services/webhooks/tap.ts`) membungkus `create`/`update`/`upsert`/`delete`/`updateMany`/`deleteMany` untuk model yang dienumerasi katalog — **nol perubahan di call site**. Alasannya rekam jejak, bukan keanggunan: hanoman sudah **tiga kali** membayar kelas bug "satu definisi, N call site" (SPEC-431 predikat, SPEC-448 env spawn, SPEC-475 efek samping), dan SPEC-475 mencatat bahwa yang paling licin adalah divergensi pada **efek samping** justru karena ia tak punya tipe pemaksa seperti `GovernorDeps.blockers` — sementara "pancarkan peristiwa" adalah efek samping murni. Katalog **`WEBHOOK_ENTITIES`** (`@hanoman/shared`) menyetir tap **dan** halaman dokumentasi in-app, jadi menambah peristiwa = menambah satu entri dan dokumentasi ikut bergerak; **allowlist field** di dalamnya sekaligus pagar data sensitif dan kontrak payload, dijaga test DMMF (cermin `PG_ORDER`) karena pelanggarannya gagal SENYAP — payload jadi kosong tanpa satu pun error. Antreannya `WebhookDelivery` (tabel SQLite merangkap **antrean dan riwayat**) dikuras `setInterval` dari `server.ts`, pola governor ADR-0072 & outbox ADR-0096; `payload` disimpan **per baris** karena retry wajib mengirim byte yang persis sama (id peristiwa stabil ⇒ idempotensi penerima berlaku) dan riwayat harus memperlihatkan apa yang benar-benar dikirim, bukan keadaan hari ini. Amplop **ber-versi sejak awal** (`hanoman.webhook/1` + `apiVersion` per endpoint) dan nomor percobaan hidup di **header**, sebab badan yang berubah tiap percobaan mematahkan janji byte-identik itu; peristiwa turunan **menggantikan** (`spec.stage_changed` alih-alih `spec.updated`), dan **diff kosong tak melahirkan apa pun** — tanpa aturan itu overlay stage-live `liveSpecs` (menulis tiap `GET /specs`) dan bump `version` mesin sync jadi banjir peristiwa hampa. Kontraknya **at-least-once**: baris `sending` yang tertinggal crash **DIULANG** saat boot, sengaja **berlawanan** dengan `TelegramOutbox` yang memilih `uncertain` — di sana kembarannya pesan ganda ke manusia, di sini penerima memegang id stabil dan diwajibkan idempoten. Pengelolaannya **COOKIE_ONLY** (preseden `/telegram/{settings,test,credentials}`): permukaan ini memegang secret penandatanganan **dan** menentukan ke mana data workspace mengalir keluar; secret 32 byte disimpan terenkripsi lewat `secret-box.ts` dan ditampilkan **sekali** (pola AgentToken). SSRF dua lapis dengan pertanyaan berbeda — `checkUrlShape` saat **simpan** (bentuk + IP literal + `localhost`, **sengaja tanpa DNS**: resolusi jaringan di jalur tulis CRUD membuat pendaftaran gagal saat DNS mati, dan fail-closed di sana berarti operator tak bisa mendaftar apa pun offline) dan `checkDestination` saat **setiap percobaan kirim**, yang memang gerbang sebenarnya. **Enam konsekuensi yang diterima sadar:** cascade delete tingkat-DB tak terlihat (dilaporkan sebagai `data.cascade` pada `project.deleted`); `$executeRaw`/`createMany` di luar jangkauan, dijaga test penjaga karena pelanggarannya senyap; satu pre-read per tulisan model terlacak **hanya** saat ada endpoint aktif (nol endpoint = satu boolean); jendela DNS rebinding dipersempit tapi **tak ditutup**, dan itu ditulis apa adanya di halaman docs; notifikasi bertipe `webhook` **tak** difan-out supaya kegagalan satu endpoint tak mengirim lalu lintas ke endpoint lain; dan klien Prisma yang diekspor kini ber-extension sehingga tak lagi assignable ke `PrismaClient`/`Prisma.TransactionClient` — `db.ts` mengekspor alias `Db`/`DbTx` yang **diturunkan dari nilai nyatanya** agar menambah atau mencabut extension kelak tak menuntut menyunting tanda tangan mana pun. **Alternatif yang ditolak:** emit eksplisit di call site (menghidupkan kembali kelas bug SPEC-475), polling diff periodik (changefeed kedua + kehilangan perubahan yang dibatalkan dalam satu interval), dan menumpang `SyncLog` — ia sudah berbentuk changefeed ber-snapshot tapi **role-dependent** (peran client memakai `enqueueOutbox` dan tak menulis changefeed sama sekali, jadi `before`/`after` hilang justru di separuh topologi) sekaligus buta terhadap `SessionHistory`/`LeadDecision`/`Notification`.
- [0099 — MCP server hanoman: subcommand stdio yang jadi klien REST, katalog tool di `shared`](0099-mcp-server-hanoman.md) — **memperluas 0065**, **mengikuti 0087/0038**, **tidak menyentuh 0037 & 0024**, **tidak mencabut apa pun** (SPEC-482): permukaan hanoman sudah REST penuh dan jalur auth agen sudah ada sejak ADR-0065, tapi tak pernah ada **pembungkus yang bisa dipakai agen mana pun** — setiap klien AI menulis ulang wrappernya sendiri. Buktinya ada di wrapper yang benar-benar dipakai: skill `~/.claude/skills/hanoman` = helper shell `hnm` + `api-reference.md` **240 baris** yang separuhnya bukan dokumentasi fitur melainkan **jebakan yang hanya ketahuan lewat 400/403** (`payload` wajib cocok dengan `source`, tiga bentuk; `startable` **hanya** menerima string `"true"` dan nilai lain diabaikan **senyap** sambil mengembalikan seluruh backlog termasuk `done`; `q` tak menyentuh `payload`; route cookie-only selalu 403; token **per-instance** sehingga instance keliru menjawab **401 telanjang** yang tak terbedakan dari master switch mati) — dan dokumen itu sendiri sudah **basi tanpa suara**: masih memuat domain `errors` dan source `cross-audit` yang dicabut ADR-0092. Itulah argumen intinya: **skema tool tak bisa basi diam-diam, karena ia dites.** Keputusannya **`hanoman mcp`** — subcommand stdio di CLI yang sudah ada, berperan sebagai **klien REST**, sehingga gate `onRequest` tetap satu-satunya otorisasi dan route cookie-only tak terjangkau **secara struktural**, bukan karena diingat; stdio karena dukungannya merata; satu artefak distribusi karena `hanoman-sdk` sudah membuktikan paket kedua melenceng versinya (ADR-0092). Katalog 17 tool hidup di **`@hanoman/shared`** sebagai data murni yang dipakai runtime CLI **dan** panel Settings — daftar capability yang harus dicentang manusia tak bisa drift dari yang dituntut tool — dan diikat ke gate oleh uji kontrak di **server** (`capabilityForRoute(sampleMethod, samplePath) === capability`, plus larangan cookie-only/`/vps`/`/terminal` non-GET). **Tool yang mengeksekusi sengaja tak ada:** `POST /terminal/sessions` (RCE efektif) dan `/vps*` (remote exec), begitu pula `integrate`, `DELETE /specs/:id`, dan `PATCH stage`; menyediakannya kelak menuntut opt-in terpisah + penandaan berbahaya. Mode baca-saja **menghilangkan** keempat tool tulis dari `tools/list` — yang tak terlihat tak bisa dicoba. **Gagal-lunak saat start, jelaskan saat panggil**, karena klien MCP menyembunyikan stderr sehingga proses yang mati hanya tampak "server gagal" tanpa sebab; `hanoman_about` menjawab tanpa token. `MCP_TOOL_SCHEMA_VERSION = 1`, aditif-dalam-versi, dijaga **test snapshot** katalog. **Delapan gotcha, tiga yang mengikat:** stdout **milik JSON-RPC** — satu byte diagnostik di sana merusak protokol, jadi perintah `mcp` tak pernah memanggil `ctx.stdout`; `allOf`/`if`/`then` di JSON Schema **ditegakkan validator SDK** (terukur: `source:"qa"` + payload brief ditolak **di klien**, handler tak pernah jalan, 400 tak pernah lahir — itulah "dibimbing ke panggilan yang sah alih-alih menemukannya lewat 400" yang benar-benar terjadi); dan **401 telanjang tak bisa dibedakan** antara host salah / master switch mati / token dicabut, sehingga probe `GET /api/health` (endpoint PUBLIK, tanpa auth) sekali-lalu-cache adalah satu-satunya yang memisahkan "host salah" dari "token salah". Sisanya: `GET /specs/:id` **tidak ada** sehingga `hanoman_backlog_get` mencocokkan id **persis** atas hasil `q` yang substring (`SPEC-48` → `SPEC-480…489`); `startable` diekspos **boolean** dan `false` **menghilangkan** parameternya; token **tak pernah dari flag** (ARGV terbaca `ps` — kelas SPEC-402), `--token` justru melahirkan keluhan tanpa mengutip nilainya; redaksi di **satu titik keluar** (SPEC-472 membuktikan satu call site terlewat sudah cukup); dan pemotongan **wajib tetap JSON sah** — JSON terpotong di tengah dibaca agen sebagai galat parsing, bukan batas ukuran. Tanpa endpoint baru, tanpa skema, tanpa migration; bundle CLI tumbuh ±750 KB tapi `RUNTIME_DEPS` paket npm tidak bertambah.
- [0098 — Putusan lead ringkas & terstruktur: `choice` tervalidasi, `missing`, batas panjang saat pengiriman](0098-putusan-lead-ringkas-terstruktur.md) — **mengamandemen 0091 (AC-1 & AC-22)**, **memperluas 0085**, **tidak menyentuh 0037** (SPEC-480): `DecideRequest.options` sudah dipakai **empat** pemanggil — pintu deteksi dialog (SPEC-452) dan tiga pintu denyut — tapi verdict lead tak pernah punya field yang menjawab **"opsi yang mana"**. Label opsi denyut sengaja diawali nama tindakan (`"integrate-main — …"`), dan itu satu-satunya jembatan antara pilihan dan eksekusi: berupa **harapan** bahwa prosa `decision` dan field `action` sepakat. Lead yang memilih opsi 1 dalam kalimatnya lalu membiarkan `action` pada default `"none"` melahirkan baris `berlaku` yang **tak berefek apa pun** dan tak terbaca sebagai kesalahan oleh siapa pun; `orderReadyWork` bahkan mem-`split` prosanya dengan regex. Keputusannya: **`choice`** (string bebas — pilihan karangan harus BISA MASUK supaya ditolak-dan-dicatat, alasan yang sama dengan `action`) diselesaikan **`resolveChoice`** yang **fail-closed** (nomor · nomor+label yang sepakat · teks persis · kepala label · awalan unik; ambigu → `null`, karena SPEC-452 sudah mengukur ongkos pencocokan yang "kelihatan benar"); di luar daftar → baris tetap lahir + `DITOLAK:` + `weighty`, **`kind` TIDAK ditulis ulang** (mengganti `kind` merusak idempotensi denyut — SPEC-432); `action` boleh **diturunkan** dari `optionActionHint` **hanya saat lead diam**, dan pertentangan → `none` + `KONFLIK:` + notifikasi, tak pernah ditebak. Ringkas ditegakkan **dua lapis**: prompt menyebut `LEAD_DECISION_MAX = 240` / `LEAD_REASON_MAX = 480` **dengan angkanya** plus larangan eksplisit (ringkasan ulang konteks · latar belakang · alternatif tak diminta), sementara `clampProse` memangkas **hanya yang dikirim** — **jejak menyimpan prosa lead UTUH**, karena jejak adalah tempat orang mencari kenapa sebuah putusan diambil. **`missing`** memberi AC-22 satu pengecualian bernama: bukan "bukti tipis" (itu `confidence: "ragu"` yang sudah ada) melainkan fakta konkret yang tak ada di repo — terisi ⇒ `ragu` dipaksa ⇒ weighty ⇒ operator dipanggil dengan pertanyaan yang **presisi**, bukan dengan diam; `decision` tetap wajib, dan itulah kompatibilitas mundurnya. Empat kolom aditif nullable (`choice`/`choiceIndex`/`options`/`missing`); `options` ikut disimpan karena `question` tersimpan sedangkan menunya tidak — tanpa itu "lead memilih opsi 2" tak bisa diverifikasi enam jam kemudian. **Lima gotcha:** `clampProse` melipat spasi bukan demi rapi melainkan karena satu baris baru yang lolos ke pane adalah `Enter` yang mengirim jawaban separuh jadi (kelas SPEC-452); catatan `DITOLAK`/`KONFLIK` ditempel **sesudah** pemangkasan supaya bagian yang paling perlu dibaca tak terpotong; adopsi `action` sah **hanya** karena label opsi dirakit pemanggil — untuk label bebas hint-nya `null`; saluran `takeDelivery` hidup di memori dan berumur satu ketikan sehingga fallback ke `answer` wajib (di `detect.ts` ia disuntikkan sebagai dep `delivery`, prod tetap satu definisi); dan `LeadScreen` membaca `options`/`missing` dengan `?? []` sebab dashboard bisa lebih baru daripada server yang dilayaninya (ADR-0087) dan baris berbentuk lama akan meruntuhkan **seluruh** panel.
- [0097 — Kredensial Telegram di Settings: entri config terenkripsi, bukan `.env`](0097-kredensial-telegram-di-settings-terenkripsi.md) — **mengamandemen 0096**, **memperluas 0049 & 0065**, tak mencabut apa pun (SPEC-477): ADR-0096 menaruh bot token, allowlist, dan AgentToken gateway di environment dan membacanya **sekali saat boot**, sehingga setiap perubahan menuntut edit berkas di server + restart proses — kartu Settings-nya bahkan menutup diri sendiri (*"credential disimpan di env, bukan database"*). Keempat nilai kini menjadi entri **`CONFIG_REGISTRY` grup `telegram`** alih-alih store kredensial kedua, karena resolver ADR-0049 (**DB → env → default** + `sourceOf()`) sudah **persis** semantik "DB kosong → pakai `.env` → tandai deprecated" yang diminta: fallback dan penanda deprecated didapat tanpa satu baris kode fallback pun, dan `.env` lama tetap bekerja penuh. Dua penghalang ditutup sekalian. **(a)** `RuntimeConfig.value` plaintext → `services/secret-box.ts` (AES-256-GCM, `node:crypto`, tanpa dependency baru) mengenkripsi **semua** entri `kind: "secret"` — bukan hanya Telegram, karena dua aturan encoding untuk `kind` yang sama adalah kebingungan permanen; kunci 32 byte di `<HANOMAN_HOME>/secret.key` mode `0600` **dibuat otomatis** (override `HANOMAN_SECRET_KEY` opsional — mewajibkannya akan menghidupkan lagi ketergantungan `.env` yang justru dicabut). Batasnya di `setConfig`/`loadConfig` sehingga **cache memegang plaintext** dan tak satu pun pemakai `effectiveStr`/`rawDbValue` berubah; baris tanpa prefix `enc:v1:` = plaintext lama, dikembalikan apa adanya dan naik kelas saat ditulis ulang → **tanpa migration**. **(b)** `capabilityForRoute` memetakan `/config` ke `settings:write`, capability yang justru **wajib** dimiliki AgentToken gateway Telegram (ADR-0096 §2) — jadi sesi operator bisa menulis ulang kredensialnya sendiri lewat percakapan; ditutup pagar kategori **`credential` = cookie-only** di handler `PUT`/`DELETE /config` (peta route struktural tak bisa melihat `body.key`) plus tiga endpoint baru `/telegram/{settings,test,credentials}` yang **COOKIE_ONLY**, sementara sub-path kerja sesi (`status`/`chats`/`replies`/`audit`) tak berubah. Berlaku-tanpa-restart lewat `reloadTelegramGateway()` dari `applyConfigSideEffect` + `PUT /settings` (hanya bila blok `telegram` berubah). **Sepuluh gotcha**, tiga yang mengikat: `loadConfig()` **wajib** mendahului `installTelegramGateway` — urutan lama membuat gateway lahir dengan cache kosong dan diam-diam jatuh ke env, kegagalan **senyap yang tampak benar**; chat id channel/supergroup **negatif** sehingga `^\d+$` menolak persis kasus "Channel ID" yang diminta (allowlist user id tetap non-negatif — dua pola, jangan disatukan); dan Test Connection memakai **klien sekali pakai** karena klien gateway memegang `AbortController` loop `getUpdates`-nya, sehingga menumpang di sana menukar "uji koneksi" dengan "putuskan polling". Bot token **tetap tak pernah masuk sesi** (ADR-0096 gotcha 4 utuh) — yang berubah hanya dari mana gateway membacanya. ADR-0037 & ADR-0086 utuh.
- [0096 — Telegram gateway: transport ke session operator tmux persisten, bukan runtime agen](0096-telegram-gateway-session-operator-persisten.md) — **memperluas 0016/0065/0079/0094**, **mengikuti 0024/0039/0086**, **tidak menghidupkan kembali worker/headless/tool bus/Redis** (SPEC-476): satu private chat/user allowlisted diikat durable ke **satu** session operator tmux deterministik; natural text, command, dan callback masuk pane yang sama, sementara action produk tetap melalui `/api` ber-`AgentToken`, correlation, capability, dan audit. Transportnya satu loop `getUpdates` in-process dari `server.ts` (webhook ditolak untuk MVP); offset, dedupe, binding, curated memory, summary, confirmation, outbox, dan audit adalah model SQLite **LOCAL-only**. Batas DB↔tmux/Telegram diputuskan **at-most-once fail-closed**: `received→dispatching` / `pending→sending` sebelum efek eksternal, sisa batas crash menjadi `uncertain` dan tak diretry otomatis — crash boleh meminta operator mengulang sadar, update yang sama tak pernah masuk pane dua kali. Output hanya amplop reply eksplisit; raw `capture-pane` dilarang menjadi chat karena teks tanpa ANSI pun dapat memuat reasoning/command echo/secret. Bot token hanya transport env dan **tak pernah masuk session**; agent token masuk env session, DB hanya hash existing. Aksi sulit dibatalkan memakai confirmation inline single-use yang menjadi kondisi tambahan khusus identitas token gateway; capability + pagar route existing tetap menang. Personality memakai katalog `CustomAgent`, memory/ringkasan dikurasi session yang sama (bukan LLM kedua), dan agent/model mewarisi `sessionAgentDefaults()` sehingga claude/codex tidak punya jalur setting terpisah.
- [0095 — Tarik issue GitHub ke backlog: record lokal `GithubIssue`, dua jalur ambil, baca-saja](0095-tarik-issue-github-ke-backlog.md) — **mengikuti 0062/0045/0094**, **menyentuh 0065/0049**, **menegakkan 0015**, **tidak menghidupkan kembali 0006**, **tidak menyentuh 0024**, **tidak mencabut apa pun** (SPEC-471): hanoman punya empat kanal masuk yang melahirkan backlog (manual · breakdown PRD 0069 · tiket Help Center 0062 · eskalasi audit 0076) yang semuanya bermuara ke **tiga** call site `prisma.spec.create` — dan kanal tempat rekan kerja developer sesungguhnya menulis pekerjaan bukan salah satunya: `grep` `api.github.com` di seluruh `server/src`+`src/src`+`shared/src`+`cli/src`+`runner/src` **kosong**. Kemampuan membacanya hilang sebagai **efek samping** ADR-0024 yang mencabut webhook dan ikut membuang `octokit` (ADR-0006 jadi *de-facto obsolete*); tiga kanal yang lahir sesudahnya melayani **audiens lain**. Terukur 2026-08-01: **9 issue** dari dua rekan kerja, badan 2 108–3 942 byte, tertua menganggur **36 j 37 m**, vs **284 `Spec`** — nol dari issue; kontrol negatif membuktikan hambatannya murni sisi-hanoman (`gh` terautentikasi scope `repo`, kuota 4986/5000, sembilan issue kembali < 1 detik). **Probe yang menentukan keputusan, bukan dokumentasi:** di GitHub setiap pull request *adalah* issue, jadi REST `/repos/{slug}/issues` memuat keduanya sementara `gh issue list` tidak — **14 dari 30** di `cli/cli`, dan di `zamaludin/kirimchat-multi` yang issue-nya **DIMATIKAN** REST menjawab **HTTP 200 dengan 71 item, 71-71-nya PR** (gh: exit 1). Karena itu jalur REST **wajib** membuang tiap item ber-kunci `pull_request` dan membaca `has_issues` di `/repos/{slug}` lebih dulu — satu-satunya pembeda "dimatikan" vs "kosong". Konsekuensi paling halus: **kegagalan `gh` adalah jawaban OTORITATIF, bukan alasan fallback** — fallback hanya sah saat `gh` tak bisa dieksekusi (ENOENT) atau tak terautentikasi, sebab jatuh ke REST karena gh bilang "issues dimatikan" justru **memproduksi** 71 backlog palsu itu. Keputusannya: entitas **`GithubIssue`** disync dengan **id deterministik `"<projectId>:<owner>/<repo>#<n>"`** (alasan 0094: dua mesin bertemu sebagai SATU baris) dan `specId` **tanpa FK** (cermin `Ticket.specId`, kelas SPEC-382); **dua jalur ambil satu bentuk** dengan `GITHUB_TOKEN` melayani keduanya (terukur: `GH_TOKEN` di env **mengalahkan** keyring, jadi hub VPS tanpa keyring terlayani satu jalur kode); **`pullIssues` tak pernah menyentuh `status`/`specId`** saat memperbarui — tanpa itu issue yang sudah diterima kembali `new` dan accept berikutnya melahirkan `Spec` kedua; resolusi repo **`gitRemote ?? origin(repoDir)`** karena sweep 8 project menunjukkan 4 ber-host GitHub tapi hanya **2** punya kolomnya terisi (`inkara` bahkan punya `gitRemote` **tanpa `repoDir`**, `erp-tumbuh-ai` GitLab → ditolak **menyebut hostnya**); dan **hanoman TIDAK PERNAH menulis ke GitHub** — batas sadar, karena gerbang "selesai" sudah salah baca tiga kali (SPEC-402/433/451) dan efek keluar ke repo publik menunggu gerbang yang lebih matang. **Gotcha lain:** `gh` default `--limit` **30** (memotong senyap, jadi selalu eksplisit); default label→source di sini **`qa`**, bukan `brief` seperti SPEC-291 — kesembilan issue nyata tak berlabel sementara isinya laporan cacat, jadi flow yang menyelidiki dulu adalah default yang aman; `zBriefPayload` **mewajibkan `priority`** (`zQaPayload` tidak) sehingga payload brief tanpa itu ditolak di boundary mana pun yang memvalidasi; dan `"githubIssue"` wajib ikut seluruh peta di `sync.ts` **termasuk `status`/`specId`** di `FIELDS`, karena `upsert` yang tak menyebut kolom ber-default tetap berhasil dan mendaratkan default palsu tanpa satu pun error (kelas ADR-0090/0093/0094).
- [0094 — Custom agent: katalog di DB, materialisasi native per agen, anti-loop berlapis](0094-custom-agent-katalog-materialisasi-native.md) — **memperluas 0074**, **mengikuti 0045/0002**, **menyentuh 0065**, **tidak menyentuh 0037**, **tidak menghidupkan kembali 0024** (SPEC-450): setiap sesi hanoman selalu berisi **satu agen generik** — tak ada cara menyatakan persona yang bisa dipilih ulang, dibagi antar project, dan terlihat di dashboard. Kedua CLI punya konsep agen custom tapi **tidak setara**, dan perbedaannya baru terbaca setelah **diukur**: `claude --agents '<json>'` ada & bekerja (agen muncul sebagai subagent sungguhan), delegasi antar-subagent **bekerja** bila pemanggilnya punya `Task` di `tools`; sedangkan codex 0.146 — meski membawa `multi_agent`, `spawn_agent`, dan `AgentRoleToml{config_file,nickname_candidates,…}` di binernya — **menerima kunci `-c` yang tak dikenal secara diam-diam** (`agent_roles.x.bogus_field` → exit 0), jadi "konfigurasinya masuk tanpa error" tak membuktikan apa pun dan `agent_roles` tak bisa jadi fondasi. Pertanyaan kedua dijawab `$GIT_DIR/info/exclude` **yang diabaikan git di linked worktree** (hanya common dir yang dibaca): konvensi `.claude/agents/<name>.md` berarti berkas yang **pasti** muncul di `git status`, jadi menukar "definisi agen" dengan "diff worktree tak pernah bersih" — atau menyunting `.git/info/exclude` milik checkout operator. Keputusannya: entitas **`CustomAgent`** disync dengan `projectId` nullable sebagai scope (project menimpa global), **`id` deterministik `"<scope>:<name>"`** karena id acak membuat dua mesin melahirkan dua baris yang lalu bertemu di satu objek JSON **berkunci nama** dan salah satunya hilang tanpa jejak, **`name` immutable** karena changefeed tak punya operasi hapus, **nol berkas ditulis ke mana pun** (claude → `--agents "$(cat <file>)"` lewat berkas tmpdir seperti prompt SPEC-223 karena tmux membatasi satu command ±16 KB; codex → blok **roster** ditempel ke akhir prompt sesi, agen mengadopsi peran **inline** sehingga risiko loop di codex **struktural nol**). Anti-loop **tiga lapis** dan yang menjamin adalah dua lapis pertama: graf mention wajib **asiklik** (409 + jalur siklusnya, pola `dependsOn`), lalu **kapabilitas di argv** — `Task` diturunkan dari `mentions`, bukan dari ketikan operator, jadi agen daun **tak punya alat** untuk memanggil siapa pun — lalu anggaran hop `MENTION_MAX_HOPS=3` di prosa (pelajaran SPEC-432: agen berbatas yang tak diberi tahu batasnya membakar seluruh anggaran tanpa hasil). Wiring di titik cekik **`createSession`** lewat `registerCustomAgentSource` (cermin `registerSessionHooks`/`registerSchedulerSource`) sehingga tak ada call site yang bisa lupa memasangnya — kelas bug SPEC-431/ADR-0093 — dengan cache **sinkron** (Prisma tidak) yang gagal-baca → daftar kosong, karena katalog agen tak boleh menggagalkan kelahiran sesi. **Tujuh gotcha:** ketiga permukaan **gagal-senyap** sehingga verifikasi berbasis exit code lulus palsu di ketiganya (JSON rusak → exit 0 nol agen; nama tool tak dikenal dibuang tanpa pesan; codex diam untuk kunci asing) — verifikasi hidup wajib **menanyai agen apa yang benar-benar ia miliki**, kelas jebakan `paneText.includes("/goal")` ADR-0085; memeriksa graf global saja **tidak cukup** karena agen project bisa menimpa nama global (`G→H(project)→G`); `@@unique([projectId,name])` **tidak** mencegah dua agen global bernama sama (NULL saling berbeda di indeks unik SQLite) — yang mencegahnya PK deterministik; `--agents` **tak boleh** lewat `.map(sq)` seperti flag lain atau agen menerima literal `$(cat …)` sebagai definisi; `Task` yang diketik operator **wajib dicabut** saat `mentions` kosong dan `tools` kosong juga tak boleh (agen tanpa `tools` mewarisi semuanya termasuk `Task`); nama tool yang dibuang senyap **aman** untuk lapis 2 karena membuang hanya mengurangi kemampuan; dan `"customAgent"` wajib ikut `PG_ORDER` + seluruh kolomnya di `FIELDS.customAgent`, karena `upsert` yang tak menyebut kolom ber-default tetap berhasil
- [0093 — Dependency antar-backlog: kolom `dependsOn` + gerbang "selesai & ter-merge" di dua titik](0093-dependency-antar-backlog.md) — **mempersempit 0072**, **mengikuti 0018/0019**, **menyentuh 0045**, **tidak menyentuh 0037** (SPEC-447): backlog tak pernah punya konsep urutan wajib — checker mengantrekan semua item siap-kerja, governor menguras sampai `maxConcurrent` (urutan antrean bukan gerbang, jadi penataan lead ADR-0091 pun tak menahan apa pun), dan Start manual tak bertanya sama sekali; ADR-0069 bahkan menuliskan asumsi lamanya ("breakdown by-construction independen"). Yang membuatnya mahal bukan urutan yang berantakan melainkan **ADR-0002**: sesi lahir `--detach` dari `branchFrom`, jadi dependent yang lahir sebelum dependency-nya ter-merge **secara fisik tak memuat pekerjaan itu**. Kolom `Spec.dependsOn` (`Json?`, array id, satu project) + predikat siap = `stage=done` **DAN** commit ada di basis, dengan merged-ness **diturunkan git** (`merge-base --is-ancestor` terhadap `branchFrom ?? "HEAD"` — ref yang sama persis yang dipakai `addWorktree`), jadi merge yang dilakukan di luar hanoman tetap terbaca dan tak ada kolom "sudah di-merge" yang bisa basi. Satu resolver `services/spec-deps.ts` melayani TIGA pembaca (gerbang `startSpecSession`, gerbang governor, dekorasi `liveSpecs`) — menyalin predikatnya adalah kelas bug SPEC-431. **Gerbang keduanya sengaja tak simetris:** `startSpecSession` melempar `kind:"blocked"` (sesudah cek pane hidup — re-attach tak boleh ikut ditolak — dan sebelum `killSession`/worktree), governor cuma **melewati** dengan `note` dan baris tetap `queued` karena pemblokirnya akan selesai sementara `enqueue` ber-`update:{}` tak bisa menghidupkan baris yang sudah ditutup. `force` **hanya** di jalur manusia (409 tanpa itu); otomasi tak punya jalan paksa. `dependsOn` sengaja **di luar** gerbang edit SPEC-186 (ia menggerbangi peluncuran BERIKUTNYA, bukan konten sesi berjalan) dan `DELETE /specs/:id` **mencabutnya dari dependent** — tanpa itu menghapus satu item mengunci tetangganya selamanya dengan alasan `missing` yang tak bisa diperbaiki dari UI. **Empat gotcha:** dependency yang tak punya **jejak kerja** sama sekali adalah **SIAP** (hanoman tak pernah membuatkan worktree untuknya — 27 item semacam itu terukur di SPEC-431; membacanya "belum" mengunci backlog lama selamanya); git yang tak bisa menjawab dibaca **belum merged** (fail-closed, tapi terlihat sebagai `unmerged`); `"dependsOn"` **wajib** di `FIELDS.spec` atau client meluncurkan pekerjaan yang di hub terblokir (kelas bug `createdAt` ADR-0090, sama senyapnya); dan `GovernorDeps.blockers` **wajib bukan opsional** supaya gerbang otomasi tak bisa lupa dipasang. — **Diamandemen SPEC-475 (2026-08-01), gotcha 1 dipersempit, keputusannya utuh:** ukuran "jejak kerja" tak boleh kolom `headSha` sendirian. Kolom itu sampai saat itu hanya ditulis `DELETE /terminal/sessions/:id`, sementara penyelesaian **otonom** tak pernah melewatinya (pane sesi sukses tak mati sendiri SPEC-433; `integrate-main` lead melepas pane lewat `killSession` langsung SPEC-451; item yang di-Start manual tak punya baris antrean sehingga `reconcile` tak menyentuhnya) → kosong pada **159 dari 210** item `done` ber-worktree (**~76 %**), sehingga alasan `unmerged` **tak pernah menyala sekali pun** di produksi (**0** dari 56 baris antrean) dan rantai `raciklaba.id` lahir **6 detik** sesudah dependency-nya `done` di atas `baseSha` yang **identik** dengan milik dependency itu — ±8,5 jam sebelum merge-nya. Perbaikannya dua lapis, masing-masing sendirian menukar bug ini dengan kebalikannya: penulis bersama `recordHeadSha()` (`services/spec-head.ts`) di **ketiga** jalur persist `stage="done"` (pengulangan ketiga pola SPEC-431/448, kali ini pada **efek samping** yang tak punya tipe pemaksa), dan pembaca yang mencari ujung kerja `headSha` **?? tip branch sesi** (`hanoman/<sessionIdForSpec>`, ADR-0032) sehingga branch yang dihapus karena ter-merge (SPEC-360) tetap terbaca siap. Squash-merge sengaja terbaca `unmerged` (fail-closed gotcha 2; `force` jalur manusia adalah jalan keluarnya).
- [0092 — Cabut error monitoring, `hanoman-sdk`, dan cross-audit (pemantauan pindah ke Uptrace)](0092-cabut-error-monitoring-sdk-cross-audit.md) — **mencabut 0060/0063/0070/0075**, **mengamandemen 0066**, **tidak menyentuh** 0062/0065/0072/0076 (SPEC-384): pemantauan error produksi sudah pindah ke Uptrace, jadi jalur error milik hanoman sendiri — SDK npm, ingest ber-DSN, grouping, symbolication, eskalasi, source scheduler `errors` — dicabut utuh berikut tabel (`ErrorGroup`/`ErrorEvent`/`SourceMapArtifact`), kolom (`Project.ingestKeyHash`/`ingestKeyPrefix`), endpoint, layar, dan docs-nya. **Cross-audit ikut** karena `GET /api/audit/logs` HANYA membaca `ErrorEvent`/`ErrorGroup` dan seluruh mekanisme kunci `hnm_xa_…` ada semata untuk menggerbanginya: tanpa sumbu error, flow itu tak lagi berbeda dari `audit` biasa, jadi `ProjectLink`, kartu Integrasi, dan prompt 90 barisnya jadi duplikat berbiaya. **Penyimpangan konvensi yang disengaja:** berkas keempat ADR yang dicabut **dihapus**, bukan ditandai, atas perintah manusia yang eksplisit — nomornya tetap tak dipakai ulang. **Pengecualian di dalam pengecualian:** 0066 memuat DUA keputusan (errors *dan* tiket + pemicu sync manual) dan yang kedua masih hidup, jadi ia ditulis ulang tanpa bagian errors alih-alih dihapus. **Gotcha yang paling mahal:** byte source-map hidup di `uploadDir()` yang SAMA dengan lampiran tiket dengan nama opaque `<uuid>.map` — menghapus direktorinya ikut menghapus lampiran tiket yang masih dipakai, jadi pembersihan byte WAJIB memakai daftar `storageKey` yang dibaca SEBELUM tabelnya di-drop (sesudah itu daftarnya tak bisa direkonstruksi dari apa pun); karena migration SQL tak menyentuh filesystem, langkah itu jadi runbook manual. Gotcha kedua: pengecualian gate yatim TAK BISA diamati dari luar — `setNotFoundHandler` ada di app root sementara hook gate hidup di scope `/api`, jadi path tanpa route dijawab handler root dan prefix bypass yang tertinggal menghasilkan 404 yang identik dengan keadaan benar; penjaganya adalah ketiadaan route-nya. Migration `20260731180000` juga membereskan baris ber-nilai enum yang dicabut (`Notification type='error'`, jejak feed `errorGroup`, antrean `source='errors'`) dan **menormalkan** `Spec.source='cross-audit'` → `audit` alih-alih menghapusnya — backlog item-nya pekerjaan nyata. `hanoman-sdk` masih terbit di npm: mencabutnya dari registry (unpublish → deprecate) adalah tindakan manusia.
- [0091 — hanoman-lead: agen pemimpin di atas agen; manusia jadi pembatal, bukan gerbang](0091-hanoman-lead-agen-pemimpin.md) — **§5 & §2 pintu #2 diamandemen 0146 (SPEC-909): pintu deteksi dipicu event hook, bukan denyut 5 detik** · **mengamandemen 0035**, memperluas **0072** & **0065**; **tidak** mencabut 0037/0024/0039 (SPEC-409, mewujudkan PRD `docs/prd/orchestrator-hanoman.md`): mekanisme "sesi menunggu keputusan" sudah lengkap sejak SPEC-184/196 — yang tak pernah ada adalah **yang menjawabnya selain manusia**. Karena itu ADR ini mengganti PIHAK yang menjawab, bukan mekanisme berhentinya. Prinsip produk bergeser dari *"manusia terakhir yang memutuskan"* ke **"manusia terakhir yang bisa membatalkan"** — tapi **hanya untuk project yang di-opt-in** (`Project.leadOptIn`, cermin `schedulerOptIn`); default mati, dan selama master switch `Setting.lead.enabled` mati hanoman berperilaku persis seperti sebelumnya. Lead adalah **agen** yang dipanggil sekali-jalan non-interaktif (`claude -p`/`codex exec`) dengan keluaran satu blok ```json — dan itu **bukan** menghidupkan run headless yang dicabut **0024**: yang dicabut adalah MENGERJAKAN pekerjaan lewat CLI bertahap, sementara lead cuma penasihat berumur pendek yang tak menyentuh worktree sesi manapun. **Tiga pintu, satu otak** (`decide.ts`): kontrak eksplisit `POST /api/lead/decisions` (agen internal & eksternal ber-`AgentToken`), deteksi otomatis (baca pane sesi ber-marker → ketik jawabannya, sesi tak perlu tahu kontrak apa pun), dan denyut proaktif (urutan kerja · tabrakan area · tindak lanjut sesi selesai). Urutan wajibnya **bukti → putusan → saring rujukan → gerbang tindakan → TULIS JEJAK → notifikasi**, dan alasan satu-satunya jalur adalah AC-2: jejak harus ada sebelum jawaban sampai ke peminta. **Batas kerasnya ada di permukaan tindakan LEAD** (`LEAD_ACTIONS` = allowlist tertutup, konstanta modul **bukan konfigurasi** — AC-31 berbunyi "termasuk saat operator memintanya lewat konfigurasi"), **bukan** hook deny pada sesi pekerja: **0037 tetap utuh**. Konsekuensi yang mengikat: "ulangi dari nol" **mustahil** bagi lead karena itu menuntut menghapus worktree yang terkunci — `stop-session` memanggil `killSession()` LANGSUNG, bukan `DELETE /terminal/sessions/:id` yang memang menghapus worktree (SPEC-362). Jejaknya model baru `LeadDecision` + migration tulis tangan, LOCAL-only (cermin `SessionHistory`), berlawanan arah dengan **0011/0018** dengan alasan yang sama seperti **0090**: pertanyaan sesi yang sudah mati tak bisa dihitung ulang dari mana pun; `trail.ts` sengaja **tak punya fungsi hapus** (cara termurah menegakkan AC-32). Denyut = `setInterval` in-process (tanpa queue/worker/cron — **0024** utuh), jejak lewat HTTP polling (tanpa kanal WS baru — **0039** utuh), urutan diserahkan ke antrean & governor yang sudah ada (**0072**, bukan antrean kedua). Keempat belas Open Question PRD ditutup di ADR-nya. **Enam gotcha:** penghitung jawaban otomatis TAK BOLEH di-reset saat marker kosong (marker memang kosong sesaat sesudah lead mengetik — hook `UserPromptSubmit` menjalankan `: >` — dan reset di sana membuat pagar AC-11 tak pernah tercapai: loop tanpa ujung yang tampak sehat); idempotensi denyut lewat **jejak** bukan `Set` memori (pane mati bertahan berhari-hari, dan `Set` kosong justru sesudah restart); `capabilityForRoute` wajib memetakan `lead` **menurut method** (kelas bug SPEC-405); `zLeadVerdict.action` sengaja `string` bukan enum supaya "deploy" bisa MASUK lalu ditolak-dan-dicatat (AC-33) alih-alih lenyap sebagai keluaran rusak; jawaban ke pane dipotong `goalChunks` karena burst ≥1024 char jadi `[Pasted Content]` SENYAP (**0085**); dan rujukan disaring terhadap repo (path absolut & `..` ditolak — jejak dibaca operator sebagai bukti)
- [0090 — Stempel waktu backlog: `Spec.createdAt` & `startedAt` sebagai kolom](0090-stempel-waktu-backlog-created-started.md) — **memperluas 0038** (SPEC-408): permintaannya tampak seperti pekerjaan UI ("tambah filter date range di backlog"), ternyata **datanya tak pernah ada**. `Spec` hanya punya `updatedAt`; "dikerjakan" bahkan bukan tanggal melainkan kondisi boolean turunan `baseSha !== null` yang dipakai scheduler untuk memilih item belum-mulai. Jadi dua kolom baru: `createdAt` (NOT NULL, `@default(now())`, **tak pernah ditulis route** — "dibuat" jadi fakta yang tak bisa diedit operator) dan `startedAt` (nullable). Arahnya **berlawanan dengan 0018/0019** dan itu disengaja: aturan sebenarnya bukan "selalu turunkan" melainkan *bisakah nilainya dihitung ulang dari sumber lain* — coverage bisa (filesystem), diff bisa (git), waktu lahirnya sebuah baris **tidak bisa**. `startedAt` ditulis di **titik cekik yang sama dengan `baseSha`** (`session-launch.ts`, cabang `if (!resume)`) sehingga ia mewarisi keputusan **0084** secara gratis dan tak bisa drift darinya: melanjutkan sesi bukan memulai ulang, jadi maknanya **mulai pertama**, bukan sentuhan terakhir — item yang dikerjakan berhari-hari tak berpindah bucket tanggal tiap kali sesinya dibuka lagi. `updatedAt` **ditolak** sebagai proksi keduanya karena mesin sync menggerakkannya (`publishLocal`/`backfillFeed` mem-bump `version` → `@updatedAt` ikut) dan overlay stage-live menulis kemajuan tiap `GET /specs` dibaca — item yang tak pernah dibuka bisa tampak baru saja diperbarui, dan filter di atasnya akan berbohong dengan tenang. Baris lama di-backfill dari `updatedAt` (`startedAt` hanya bila `baseSha` ada): **aproksimasi yang dinyatakan terbuka**, batas ATAS dari waktu sebenarnya; alternatif "waktu migration dijalankan" ditolak karena itu satu-satunya jawaban yang dijamin salah untuk *setiap* baris. `dateField=started` **membuang** item ber-`startedAt` null — item tanpa tanggal tak bisa berada di dalam rentang tanggal, dan meloloskannya membuat "dikerjakan bulan Juli" mengembalikan yang tak pernah dikerjakan. Filternya tetap sebidang dengan `q`/`stage`/`priority`/`startable` di layer response (**0038 utuh**), lewat modul murni `services/date-range.ts`; **tanpa index baru** karena query planner tak pernah melihatnya. **Tiga gotcha:** (i) SQLite melarang `ADD COLUMN … DEFAULT CURRENT_TIMESTAMP` → migration wajib redefinisi tabel, dan klausa `SELECT`-nya justru satu-satunya tempat backfill bisa terjadi sekali jalan (karena itu ditulis tangan, bukan `migrate dev`); (ii) `new Date("2026-07-31")` = tengah malam **UTC** — sebagai batas `to` ia membuang hampir seluruh hari itu di WIB tanpa error apa pun, jadi parsing dilakukan komponen-per-komponen di zona lokal, lengkap dengan uji-balik terhadap input karena konstruktor komponen **menggulir** (`2026-02-30` → `null`, bukan diam-diam 2 Maret); (iii) tanpa `FIELDS.spec` + `DATE_FIELDS.spec`, `upsert` yang tak menyebut `createdAt` **tetap berhasil** memakai `now()` mesin penerima — tak ada yang gagal, tanggal pembuatan sekadar berbeda di tiap client.
- [0089 — Backlog goal: source & flow `goal` dua fase, mode goal dipaksa](0089-backlog-goal-flow-dua-fase.md) — **memperluas 0073** (SPEC-407): mode goal berhenti jadi knob di atas pipeline `feature` dan mendapat bentuk backlog-nya sendiri. Sebelumnya sesi "goal" tetap menjalankan Brainstorm → Objective → Spec → Plan → Execute karena flow diturunkan dari source, dan keempat source yang ada semuanya bermuara ke pipeline berfase — jadi ia menulis design doc & plan berkotak sebelum menyentuh pekerjaan yang diminta. Kini `source: "goal"` → `flow: "goal"` = **`["Goal", "Verifikasi"]`**: `Goal` mengerjakan, `Verifikasi` membuktikan. Fase kedua ada karena **pintu keluar butuh tempat**; nol fase ditolak karena membuat board buta (stage tak pernah bergerak), satu fase ditolak karena "selesai" jadi klaim agen alih-alih baris fase yang menyusul verifikasi. `Goal` **aktif** sudah memetakan ke `executing` (aturan yang sama sudah dipakai `Execute` aktif) — tanpa itu sesi goal yang jalan tampak `brainstorming`, persis fase yang dihapus flow ini. Payload-nya bentuk **ketiga** (`zGoalPayload {goal, done, constraints, priority}`), bukan reuse `zBriefPayload` dengan `context` yang diam-diam berarti goal: bentuk payload di repo ini justru yang mengikat source, jadi `superRefine` SPEC-197 kini **tiga-arah** (`qa` ↔ `severity`, `goal` ↔ `goal`, selain itu brief). **Mode goal DIPAKSA aktif** (`opts.goal:false` tak bisa mematikannya) dan template global `Setting.goal.condition` **DILEWATI** — ia generik untuk semua sesi, sedangkan item goal membawa kondisinya sendiri; override per-sesi tetap paling tinggi. Kondisi bawaannya menuntut tiga bukti segar: goal tercapai, baris fase `Goal → Verifikasi`, dan `git push` sukses (dua terakhir bukan hiasan — tanpa baris fase board tak pernah melihat item selesai, tanpa push hasilnya hilang bersama worktree). **Gotcha wajib:** gerbang klausa scope verifikasi pindah dari "punya fase `Execute`" ke predikat **`writesCode(flow)`** — sesi goal menulis kode meski pipeline-nya tak punya `Execute`, dan melewatkannya membuatnya jatuh ke DoD repo target (suite penuh), persis lubang ADR-0080. Prompt-nya builder terpisah (`startGoalPrompt`), dan `resumeClause` hanya menyebut plan untuk pipeline ber-fase `Plan` — menyuruh sesi goal mencari plan mengundangnya membuat satu. Tanpa migration/endpoint/model baru; ADR-0029 & ADR-0037 utuh.
- [0088 — Tombol update dari dashboard: server keluar, supervisor memasang](0088-tombol-update-npm-restart-tersupervisi.md) — **mengamandemen 0048** (memenuhi syarat yang ADR itu sendiri tetapkan: "butuh ADR baru + supervisor") dan **membalik satu alternatif yang ditolak 0087**, bersandar pada **0016**, mempersempit permukaan **0065** (SPEC-405): `POST /api/update/apply` sah, tapi **server tetap tak memasang apa pun** — ia hanya keluar dengan `UPDATE_RESTART_EXIT = 75`, dan CLI parent `hanoman start` (yang sejak 0087 memang sudah men-spawn server sebagai proses ANAK) yang menjalankan `npm i -g hanoman@latest` → `prisma generate` → `migrate deploy` → spawn lagi dari path yang sama (isinya sudah tertimpa `npm i -g` di tempat). **Supervised-only**: digerbangi `process.env.HANOMAN_SUPERVISOR === "1"` yang HANYA disuntik `serverEnv()` di `cli/src/commands/start.ts`, diekspor sebagai `UpdateStatus.canApply` — dibaca dari `process.env` LANGSUNG, bukan `effectiveBool()` (yang membaca cache config DB lebih dulu, sehingga siapa pun yang bisa menulis config bisa mengaku disupervisi). **Dua langkah, satu endpoint**: tanpa `confirm` ia dry-run `409 confirm-required` + jumlah sesi hidup yang dihitung SAAT ITU (bukan dari frame siar `update` yang di-recompute tiap 300 tick — angka basi pada dialog risiko lebih buruk daripada tak ada angka); sesi hidup tak memblokir apa pun di server. Premis penolakan 0087 ("akan memutus sesi tmux") **tidak akurat**: `pty.ts` memakai `tmux new-session -d`, tmux adalah daemon — yang putus hanya jembatan `tmux attach` + WebSocket, dan klien sudah reconnect ber-backoff. **Install gagal tidak fatal** (respawn versi lama + cetak alasan); **migrasi gagal fatal** (bundle baru di atas skema lama = menukar downtime dengan kesalahan data); jatah `MAX_UPDATE_RESTARTS = 5` dengan alasan **dicetak** saat habis. **Lubang capability ditutup bersamaan**: `capabilityForRoute` dulu memetakan `update`/`limits`/`events`/`fs`/`health` ke `GLOBAL_READ` **tanpa melihat method**, jadi `POST /update/apply` akan lolos untuk **setiap** agent token — kini `GLOBAL_READ` hanya untuk method baca, selain itu `COOKIE_ONLY` → 403. **Gotcha:** `prisma generate` dijalankan **tanpa cek dulu** karena `@prisma/client` sudah ter-cache di proses supervisor sejak boot (pemeriksaan kedua menjawab "siap" memakai modul LAMA — kelas jebakan `existsSync` di 0087); listener sinyal wajib `process.off` tiap putaran; `confirm` wajib **boolean** (`"ya"` truthy = kehilangan langkah konfirmasi tanpa sadar). Konsekuensi yang diterima sadar: proses CLI supervisor sendiri tetap kode lama sampai `hanoman` dijalankan ulang manusia. Tanpa skema/migration/knob baru
- [0087 — Distribusi hanoman sebagai paket npm global: satu perintah `hanoman`, update dari registry](0087-distribusi-npm-global-satu-perintah.md) — **bergantung pada 0086**, **mengubah mekanisme 0048 tanpa membalik keputusannya**, memakai pola paket publik **0063**, **TIDAK menyentuh** 0016/0028/0037 (SPEC-398): paket npm publik bernama `hanoman`; **`hanoman` telanjang = `start`** (help pindah ke `--help`) yang me-resolve home & DB → `prisma migrate deploy` → **men-spawn** `node dist/server.js` sebagai proses ANAK (bukan import — sinyal/exit code/flag node bersih, sesi tmux selamat dari restart); `resolveLayout()` mengenali **dua** layout (paket npm: `dist/`+`prisma/`+`web/` bersebelahan · checkout repo: `cli/dist`+`server/prisma`+`src/dist`) sehingga mengembangkan dari checkout dan menjalankan yang terpasang bukan dua kode berbeda; aset dashboard ikut di `web/` lewat `pickWebDir()` (`HANOMAN_WEB_DIR` → `<dist>/../web` → `<dist>/../../src/dist`, absen = peringatan bukan fatal: API jalan, dashboard tidak); `prisma` CLI ikut jadi **dependency runtime** (±40 MB, ganti dari mengarang runner migrasi sendiri); staging rilis terpisah `dist-npm/` dirakit `hanoman __pack`, workspace **tidak** dipublikasikan, `pnpm release` = build+pack+`npm pack --dry-run` dan **`npm publish` tetap tindakan manusia**; identitas versi pindah dari **SHA git ke semver** (satu sumber `version` root → `dist/build-info.json`), `services/update.ts` membaca `GET <registry>/hanoman/latest` + `compareSemver()`. **Kenapa update di CLI, bukan endpoint:** server adalah yang akan ditimpa — `POST /api/update/apply` memasang paket baru di bawah proses yang mengeksekusinya lalu harus mematikan dirinya sendiri, memutus sesi tmux yang tak pernah minta diputus; jadi **0048 utuh** (dashboard mendeteksi, tak pernah memasang), yang berubah cuma sumber kebenarannya — `git fetch` tak punya arti di instalasi `npm i -g` (tak ada repo git di sana). Jaringan tetap di **satu** tempat, tetap ter-gate `HANOMAN_UPDATE_FETCH` (test memaksa `0`), TTL 5 mnt, gagal → `unavailable` (tak pernah melempar). `hanoman doctor` melaporkan prasyarat yang npm **tak bisa bawa** (node ≥ 20 · `git` · `tmux` · `claude`/`codex` minimal satu · izin tulis home · aset web) dengan exit code — memasangnya diam-diam berarti hanoman memasang perangkat lunak yang tak diminta, menyembunyikannya berarti kegagalan muncul jauh nanti di pane tmux yang tak dibaca siapa pun. **Gotcha pemblokir:** `@prisma/client` dari npm adalah **STUB** — kodenya baru ada sesudah `prisma generate`, dan gejalanya di `npm i -g` nyata adalah migrasi **berhasil** lalu server mati seketika `@prisma/client did not initialize yet`; dua lapis penangkal sengaja dipasang keduanya (`postinstall: prisma generate … || true` non-fatal + pemeriksaan ulang `ensurePrismaClient` saat start, karena postinstall **bisa dilewati** lewat `--ignore-scripts`/sebagian CI/sebagian setup npm global), dan pemeriksaannya **langsung** — mengonstruksi `PrismaClient` lalu menangkap kegagalannya — bukan `existsSync`: berkas stub `default.js` memang **ADA** justru saat client belum di-generate, jadi cek berkas selalu berkata "sudah siap" (kelas jebakan yang sama dengan verifikasi lulus-palsu di 0085). Konsekuensi: `HANOMAN_HOME` jadi **akar data** (DB · key SSH · transkrip) sehingga dua instance dipisah `HANOMAN_HOME`+`--port`, bukan dua database; ±100 MB terpasang (didominasi prisma CLI+engine & node-pty); runbook VPS berubah jadi `npm i -g hanoman@latest` + restart; **publish belum terjadi** (nama `hanoman` diverifikasi 404/bebas per 2026-07-30). Daftar yang bisa salah **tanpa suara** dijaga test murni: `RUNTIME_DEPS` (harus memuat setiap `--external:` build server + `prisma` + `pg` — dependency terlewat tidak menggagalkan build, ia membuat paket terbit lalu mati saat `import`) dan `REQUIRED_ARTIFACTS` (tujuh berkas — web/migrasi yang lupa ikut menghasilkan paket yang "berhasil dirakit" dan rusak saat dipakai)
> **Diamandemen 2026-07-30** (tanpa ADR baru): butir 6 ADR-0087 ("`npm publish` tetap tindakan manusia") **diamandemen, bukan dicabut** — publish dijalankan `.github/workflows/release.yml` pada tag `v*` lewat **trusted publishing npm (OIDC)**, sehingga tak ada kredensial penerbit yang pernah ada di mesin mana pun. Alternatifnya ditolak dengan alasan konkret: publish non-interaktif tanpa OIDC menuntut **Granular Access Token ber-"bypass 2FA"**, dan token itu hidup di `~/.npmrc` — berkas yang bisa dibaca proses mana pun di mesin itu, **termasuk sesi agen**, dan yang bisa menerbitkan paket apa pun milik akun itu (docs npm sendiri menyarankan menghapus GAT-bypass dan pindah ke trust relationship). **Di mana gerbang manusianya, dengan jujur:** ia pindah dari "mengetik `npm publish`" ke "mendorong tag `v*`" — dan itu **bukan** jaminan yang sama kuat, karena agen yang punya akses push juga bisa membuat tag; yang benar-benar menggerbangi adalah **GitHub Environment `release`** dengan *Required reviewers*, dan tanpa reviewer environment itu tak memagari apa pun. Tiga pagar yang tak bergantung penilaian siapa pun: trigger **hanya** tag `v*`; tag yang tak cocok dengan `version` root **menggagalkan run sebelum menyentuh registry** (nomor versi yang salah terbit tak bisa dipakai ulang); dan tarball hasil rakitan **dipasang lalu dijalankan** (`hanoman --version` == versi tag) sebelum publish — `hanoman doctor` sengaja TIDAK dipakai di CI karena ia menuntut tmux & CLI agen yang memang tak ada di runner, jadi gagalnya tak relevan. **Konsekuensi berantai wajib:** `packageJsonFor()` kini menyertakan `repository.url`; trusted publishing **dan** `--provenance` membandingkannya dengan repo pembangun **persis**, tanpanya publish ditolak — dan kegagalannya hanya muncul di CI, jauh dari tempat nilainya ditulis, jadi ia dipagari test `cli/test/pack.test.ts`. **Urutan yang tak bisa dibalik:** trusted publisher dikonfigurasi **per paket** dan GAT hanya bisa di-scope ke paket yang **sudah ada**, jadi `0.1.0` tetap harus terbit sekali secara interaktif sebelum otomasi ini bisa dipercaya. Runbook: [operations/release-npm](../operations/release-npm.md). `pnpm release` sendiri **tetap** tak pernah memanggil `npm publish`.
- [0086 — SQLite satu-satunya provider: DB embedded di `~/.hanoman`, Docker dicabut](0086-sqlite-satu-satunya-provider.md) — **melengkapi 0087** (yang tanpa ini tak mungkin — paket npm tak bisa membawa Postgres), **menuntaskan pencabutan 0005** yang sudah kehilangan worker & broker-nya di **0024** tetapi meninggalkan Postgres-nya, **TIDAK menyentuh** 0011/0018/0021 (SPEC-398): Postgres ada karena 0005 butuh antrean durable + worker; sejak 0024 mencabut keduanya DB hanoman hanya menyimpan **state aplikasi** (26 model) tanpa satu pun konsumen yang menuntut fitur Postgres — yang tersisa hanya biayanya (Docker jadi prasyarat, instalasi enam langkah, dan **satu Postgres dipakai bersama semua worktree** yang jadi akar dua kelas gagal palsu berumur: sesi tetangga men-*truncate* `hanoman_test` di tengah run orang lain, dan `hanoman_test` menuntut `migrate deploy` sendiri sehingga lupa memberi ~24 gagal `P2022` yang tampak seperti regresi skema). **Kelayakan diukur, bukan diasumsikan:** raw SQL **nol** · `@db.*` **nol** · scalar list **nol** · `Decimal`/`Bytes` **nol** · `@map`/`@@map` **nol** · `Json` **14 kolom** (menuntut Prisma ≥ 6.2, repo di 5.18) · `mode:"insensitive"` **4 pemakaian** (semua di `services/session-history.ts`) → hanya dua hambatan nyata. Keputusan: Prisma **5.18 → 6.19** (bukan 7 — Prisma 7 mewajibkan driver adapter, lapisan baru demi nol kebutuhan di sini); **32 migrasi Postgres diganti satu init SQLite** (tak bisa di-replay di SQLite; jalan pindah data bukan riwayat migrasi tapi `hanoman migrate-from-postgres`); `mode:"insensitive"` dibuang (`LIKE` SQLite sudah case-insensitive ASCII — non-ASCII jadi case-sensitive, **satu-satunya regresi semantik**, diterima sadar); `HANOMAN_HOME` (default `~/.hanoman`) jadi lokasi data lewat tiga fungsi murni di `runner/src/paths.ts` — di **runner** karena `server` akan memaksa CLI bergantung pada paket server sementara `shared` ikut dibundel Vite ke browser; **`DATABASE_URL` non-`file:` MELEMPAR** dengan pesan yang menyebut perintah migrasinya (instance lama harus gagal berisik, bukan menyajikan DB kosong yang tampak seperti kehilangan data); DB test jadi **berkas per checkout** (`<db>.test.db`) yang dimigrasi otomatis `server/test/global-setup.ts`. **`resolveDbUrl` meniru aturan path Prisma** (relatif ke direktori `schema.prisma`, **bukan** cwd) — kalau CLI dan runtime beraturan beda keduanya menunjuk dua berkas: migrasi jalan di satu, server membaca yang lain, gejalanya tabel "hilang" tanpa satu pun error yang menyebut path. Konsekuensi: nol proses eksternal (`docker-compose.yml` dihapus); dua kelas gagal palsu hilang di akarnya **tetapi `--no-file-parallelism` tetap wajib** — berkas test satu paket server masih berbagi **satu** berkas DB yang di-seed ulang tiap berkas (butir 0085 utuh); instance lama **wajib** dimigrasi (`migrate-from-postgres`, 26 model urutan FK, `createMany` per 200) karena hub produksi menyimpan akun rekan & tiket nyata; urutan FK dijaga test terhadap **DMMF Prisma** (tiap model tepat sekali, dan sesudah semua model yang di-refer relasi wajibnya) sehingga menambah model tanpa memperbarui urutan → test merah, bukan migrasi gagal di tengah pada data orang; concurrency SQLite bukan masalah dan itu terbukti dari bentuk produknya (satu proses Fastify, tanpa worker, beban tulis sebesar interaksi manusia + poll 500 ms; pekerjaan berat ada di sesi tmux yang tak menyimpan baris `Run`). **Dua gotcha terukur:** `require.resolve("prisma")` **tidak** memberi CLI-nya — peta `exports` prisma 6.19 memetakan `"."` ke `./build/types.js` yang **tidak ada** di tarball, jadi resolusi bare-specifier gagal `MODULE_NOT_FOUND` alih-alih memberi `build/index.js`; `prismaCliPath()` mencoba subpath resmi `./build/index.js` **dan** `./package.json`. Dan **dry-run tak boleh menyentuh target sama sekali** — versi awal masih memanggil `count()` untuk memeriksa target kosong, dan terhadap berkas SQLite yang belum dimigrasi itu gagal `The table \`main.Project\` does not exist` persis pada perintah yang gunanya melihat-lihat dengan aman; dry-run adalah pertanyaan tentang **sumber**, ditegakkan `migrationSteps()` sebagai fungsi murni yang mematikan ketiga langkah target sekaligus
> **Diamandemen 2026-07-30** (tanpa ADR baru): butir 5 ADR-0086 (`DATABASE_URL` non-`file:` **MELEMPAR**) terbukti **salah untuk instalasi npm global** — `hanoman@0.1.0` menolak boot di mesin yang punya `DATABASE_URL=postgresql://…` untuk **project lain**, sambil menyuruh operator menjalankan `migrate-from-postgres` atas DB yang bukan miliknya. Akarnya **kategori, bukan konfigurasi**: `DATABASE_URL` salah satu nama env var paling umum yang ada (Rails/Django/Heroku/Prisma) dan CLI **global** mewarisi shell apa pun, jadi ia tak boleh mengklaim nama generik itu lalu mati saat menemukan nilai milik orang lain. Presedensi baru: **`HANOMAN_DATABASE_URL` → `DATABASE_URL` → `<home>/hanoman.db`** — hard-fail **pindah** ke knob milik hanoman sendiri (di situ niatnya eksplisit), sementara `DATABASE_URL` non-`file:` **diabaikan**. Semangat butir 5 dijaga karena pengabaiannya **tidak senyap**: `dbUrlNotice()` (murni, dipisah dari `resolveDbUrl` agar fungsi itu tetap bebas I/O) dicetak oleh `hanoman start`, `doctor`, dan boot server langsung, membawa **kedua** jalan keluar (`migrate-from-postgres` bila datanya memang punyamu · `HANOMAN_DATABASE_URL=file:…` bila mau menunjuk berkas tertentu) dan mencetak **hanya skemanya**, bukan URL-nya, karena URL DB biasanya memuat kredensial. Pertukaran diterima sadar atas dasar asimetri: instalasi npm baru yang mati total karena var project lain **sangat mungkin** dan membuat produk tak bisa dipakai sejak perintah pertama, sedangkan upgrade-in-place hanya berlaku bagi instance yang bisa dihitung, datanya **masih utuh di Postgres**, dan pulihnya satu perintah. Terbit di `0.1.1`, bersama lisensi **MIT** yang menggantikan `UNLICENSED` (0.1.0 terbit dengan kontradiksi: paket publik untuk `npm i -g` tapi "tak ada izin pakai" — versi terbit tak bisa diperbaiki, jadi `license` & berkas `LICENSE` kini dipagari test).
- [0085 — Mode goal codex memakai goal native codex, berdampingan dengan gate sh](0085-mode-goal-codex-native.md) — **mengamandemen 0074** butir (b), memberi codex jalur kedua **0073** yang sampai kini hanya dimiliki claude, **TIDAK menyentuh** 0029/0037 (SPEC-397): kalimat "`armGoalInTui` (`/goal`) tetap khusus claude; codex tak punya padanan terverifikasi" **dicabut** — benar di codex-cli 0.142.5, salah di **0.146.0** yang punya mode goal **native** (`codex features list` → `goals stable true`; tabel `thread_goals` di `$CODEX_HOME/goals_1.sqlite`; tool `create_goal`/`update_goal`/`get_goal`; status line `Pursuing goal (Ns)` · `Goal achieved (Ns)` · `Goal unmet`). Terverifikasi end-to-end: `/goal <objektif>` diketik ke pane → `Goal active`, codex **melanjutkan sendiri sesudah turn berakhir** sampai objektif tercapai; diterima **juga saat turn sedang jalan** (penting — sesi hanoman lahir membawa prompt positional, jadi arming selalu di tengah turn); objektif 4000 karakter masuk utuh (`GOAL_MAX` tak perlu diturunkan); 1,3 dtk sesudah spawn sudah cukup. Gate sh `type="command"` **tetap terpasang & masih menembak di 0.146** — jadi jalur native ditambahkan tanpa mencabut apa pun, karena gate sh satu-satunya yang benar-benar **membaca** berkas fase & kotak `- [ ]` (cermin 0029) sementara goal native menilai dengan prosa. Konsekuensi yang diterima sadar (sama seperti claude sejak 0073): satu percobaan berhenti dievaluasi **dua kali**; keduanya berpagar — gate sh oleh `GOAL_MAX_BLOCKS=25`, goal native oleh akunting budget codex (`Goal unmet`). **Gotcha wajib:** TUI codex mengubah masukan yang datang dalam satu burst **≥ 1024 karakter** jadi `[Pasted Content N chars]`, dan begitu itu terjadi slash-dispatch **tak jalan** — `/goal` terkirim sebagai **pesan chat biasa tanpa error, tanpa goal, tanpa jejak**; deteksinya **per-burst PTY, bukan per-invokasi `send-keys`** (terukur: 4×500 char TANPA jeda → `[Pasted Content 1500 chars]`), jadi memotong keystroke tanpa jeda tak menyelesaikan apa pun → `goalChunks()` mengirim potongan **500** (margin: 2×500 = 1000 < 1024) ber-jeda 50 ms, untuk **kedua** agen karena jebakan yang sama laten di claude. **Jebakan test:** verifikasi lama `paneText.includes("/goal")` **lulus palsu** persis untuk degradasi paste itu (pane memang memuat `/goal …` — sebagai pesan chat), jadi codex diverifikasi lewat penanda runtime goal-nya sendiri; verifikasi claude sengaja tak disentuh (tak ada bukti terukur soal penandanya). **Tiga jebakan test yang terukur saat mengeksekusinya:** fixture agen palsu bermode tty **kanonikal** tak bisa menerima baris ~1,3 KB+ dengan andal (900–1200 selalu sampai, 1300–1500 kadang-kadang — `MAX_CANON` 1024 kebetulan berdekatan dengan batas paste codex yang juga 1024, jadi sangat mudah disalahatribusikan ke produk; codex sungguhan bermode raw dan menerima 4000), jadi test end-to-end memakai 900 karakter dan properti "≥ 1024" dijaga unit test `goalChunks`; `vitest --changed` di tingkat root WAJIB disertai **`--no-file-parallelism`** (run root tak menghormati `fileParallelism: false` milik project server, dan test server berbagi satu Postgres — set `--changed` yang SAMA: **181 gagal / 736** paralel vs **736 lulus** serial, dengan bentuk kegagalan yang menyesatkan seperti regresi sync; `CLAUDE.md`/`AGENTS.md` diperbarui ikut ADR ini); dan vitest **tak menjalankan typecheck** (field `GoalArmOpts` baru diam-diam diabaikan saat runtime → kegagalan TDD bersifat perilaku, error tipe baru muncul di `pnpm --filter ./server typecheck`) sementara `-t` menyaring **nama test** — menyaring dengan id sesi membalas `35 skipped`, nol test berjalan, tanpa kegagalan, dan nama project vitest adalah nama **paket** (`@hanoman/server`), bukan direktori. Tanpa skema/migration/endpoint/knob baru
- [0084 — Sesi backlog dilanjutkan, bukan diulang dari nol](0084-melanjutkan-sesi-backlog.md) — **memulihkan substansi 0017** (yang ikut tercabut bersama 0024) di arsitektur sesi interaktif, melengkapi 0015/0002/0030/0019, **TIDAK menyentuh** 0037/0074 (SPEC-394): `startSpecSession` punya **tiga** keadaan, bukan dua — **live** (pane tmux hidup → re-attach), **resume** (`stage ≠ done` + `baseSha` ada + artefak masih ada → lanjutkan, `201 { id, resumed: true }`), **fresh** (perilaku lama). **Pane MATI bukan sesi**: `remain-on-exit on` menahannya hanya agar layar terakhirnya terbaca, dan mengembalikannya sebagai sesi membuat tombol "Lanjutkan" **diam** — UI sudah menghitung `!exited`, jadi tombol itu muncul persis saat pane mati, lalu satu-satunya jalan keluar (Tutup) justru **menghapus worktree-nya**. Dua bentuk resume: worktree `.worktrees/<id>` yang masih sah dipakai **apa adanya** (satu-satunya jalur yang TIDAK memanggil `addWorktree` — helper itu selalu merebut path dengan `remove --force` + `rmSync`, benar sebagai *reclaim*, fatal sebagai "lanjutkan"), atau — bila worktree hilang — dibangun ulang `--detach` di tip **`origin/hanoman/<id>` → `hanoman/<id>` → `Spec.headSha`**. Urutan itu mengikat: `origin/…` adalah ref yang `git push` di akhir sesi harus fast-forward, dan worktree yang lahir dari `branchFrom` membuat push itu **ditolak non-fast-forward** (diuji langsung ke git — sesi ulangan bahkan tak bisa menyimpan hasil ulangannya). `baseSha`/`headSha` **tak pernah ditulis ulang saat resume**; prompt `resumePrompt` menyebut baris fase yang **sudah tercatat** (berkas fase hidup di luar worktree → selamat dari penghapusan, dan agen tak bisa menurunkannya sendiri) dan **tak mengulang** klausa keputusan pasca-Audit begitu `Audit` tercatat. Server **tak pernah menulis** `$HANOMAN_PHASE_FILE`. `stage = done` tetap jalur SPEC-172. Premis 0024 yang membatalkan 0017 ("sesi tmux tak pernah terputus") **terlalu kuat**: benar untuk restart API, salah untuk mesin restart / agen keluar / operator menutup sesi. Gerbang pane-mati dipasang di **titik cekik `createSession`**, jadi ia menutup sekaligus jalur tanpa gerbang sendiri (sesi konflik `merge-<spec>` & `finishGraphOp`, konsol VPS `vpsc-<id>`); kelima route **project-level** (reverse·scaffold·prd·breakdown·cross-audit) ikut disempitkan ke `!exited` — **berpasangan wajib** dengan `ensureWorktree()` yang melewati `addWorktree` bila worktree masih sah, karena memperbaiki gerbangnya sendirian menukar "tombol diam" dengan **kehilangan dokumen belum-commit**; prompt flow itu cukup diberi satu kalimat `RESUMED_WORKTREE_NOTE` (bukan `resumePrompt` — deliverable-nya dokumen, fasenya tak punya artefak berkotak). `attach()` pada pane mati tetap sah. Tanpa skema/migration/endpoint baru
- [0083 — Retensi dokumen audit: artefak diagnosis berumur, bukan SoT abadi](0083-retensi-dokumen-audit.md) — **membatasi 0057**, terkait 0011/0018/0021/0023/0076 (SPEC-386): dokumen `research/audit-<spec>-<slug>.md` yang ditulis flow audit adalah **artefak diagnosis berumur**, bukan SoT abadi — ia hidup sampai eskalasinya diputuskan (0076) **dan** spec turunannya tuntas, lalu dihapus **berikut entri indexnya**. Syaratnya temuannya sudah punya jejak permanen (ADR, baris di doc SoT, atau kode ter-commit) dan **rujukan masuk ikut dibereskan** — doc permanen kerap menaut dokumen auditnya, dan di SPEC-386 ada empat (ADR-0062/0064/0081 + `frontend-implementation.md`) yang linknya mati bila dilewatkan. Alasannya ukuran: 27 laporan = 115 KB berkas + **43% index**, sementara index dibaca setiap sesi agen. **ADR tidak ikut berumur** (0021 utuh) — yang pindah dari index hanyalah narasinya, ke sub-index ini. Tak ada otomasi penghapus (0024); ledger 27 dokumen yang dihapus ada di dalam ADR-nya
- [0082 — Kontrak apply changefeed: record tertunda, kursor tak melompat, tarik ulang penuh](0082-kontrak-apply-changefeed-record-tertunda.md) — **memperluas 0045/0046**, **memulihkan janji 0068**, terkait 0043/0067/0062 (SPEC-382): feed berisi record **berelasi** (`ticketAttachment.ticketId` → FK `Ticket.id`), jadi "anak tiba sebelum induk" adalah keadaan **normal** — dan mesin lama menanganinya dengan dua cara yang sama-sama salah: jalur WS **menelan** kegagalan lalu frame berikutnya memajukan kursor **melewatinya** (baris hilang selamanya — inilah lampiran triase yang tak pernah sampai), jalur pull membiarkan exception merambat keluar `syncOnce` sehingga `setCursor` tak tercapai dan client **mandek total**. Kontrak barunya: record yang gagal **ditunda & dicoba ulang selama masih ada kemajuan**; sisa yatim sejati (induk sudah dihapus di hub — feed append-only tanpa tombstone) **dilewati dengan `console.warn`, bukan menahan kursor** (menahannya = livelock); jalur WS lewat satu pintu `applyFeedFrame()` yang menyalakan penanda `feedHole` sehingga **tak ada** frame yang boleh memajukan kursor melewati lubang, lalu `tick()` menambalnya lewat pull. Ditambah: feed memancarkan **INDUK sebelum ANAK** di sumber (`routes/help.ts`), `POST /api/sync/now { full:true }` (kursor → `0`, drain halaman demi halaman) sebagai satu-satunya jalan pulang bagi baris yang terlanjur dilompati, dan `acceptTicket` **memateralisasi byte** lampiran lebih dulu supaya path di direktif SPEC-286 nyata ada saat agen mem-`Read`-nya (auto-accept scheduler SPEC-297 jalan tanpa manusia yang pernah membuka gambarnya). Kontrak **entitas-agnostik**, **tanpa skema/migration**, bentuk wire `pull`/`push` utuh → hub lama ↔ client baru tetap saling paham
- [0081 — Default sesi konflik: blok `Setting.conflict` opt-in yang mewarisi saat mati](0081-default-sesi-konflik-opt-in.md) — **memperluas 0061/0074**, terkait 0031/0053/0054/0073/0080, **TIDAK membalik 0037** (SPEC-383): sesi penyelesai konflik rebase/merge boleh punya agen/model/effort sendiri lewat satu triple `{enabled,agent,model,effort}` di `zSetting` (kolom `Json` → **tanpa migration**, tanpa endpoint baru), dikonsumsi `conflictSessionDefaults()` yang **mendelegasikan penuh** ke `sessionAgentDefaults()` selama `enabled` mati — jadi instalasi yang ada tak bergeser satu argv pun. Berlaku di **ketiga** pintu konflik (backlog · git graph · PRD). Satu triple, bukan blok per-agen: menukar agen menukar model/effort sekalian (cermin `pickAgent` StartSessionModal). **Gotcha wajib:** `ensureCodexTrust` harus diturunkan dari agen **hasil helper**, bukan `Setting.agent` — dengan blok ini keduanya bisa berbeda, dan membaca yang salah mengulang bug SPEC-377 dalam bentuk baru. Sekalian: tab Settings "Model sesi" ditata ulang bersumbu agen (dua blok berjudul + badge "dipakai sesi baru"), karena blok claude dulu hanya berbunyi "Model"/"Effort" sementara judul "default global" tetap terpampang meski agen aktifnya codex
- [0080 — Scope verifikasi per sesi: klausa prompt + env, bukan hook deny](0080-scope-verifikasi-per-sesi.md) — **memperluas 0061/0073/0074** (pola properti-per-sesi), **TIDAK membalik 0037**, terkait 0002/0029/0072 (SPEC-376): `verifyScope` (`changed` default | `full`) menyuruh sesi menguji **berkas yang berubah saja** — `pnpm vitest --run --changed "$HANOMAN_BASE_SHA"` / `vitest related`, typecheck **per paket** (bukan `pnpm -r typecheck`), lint per berkas, build penuh & boot-server+curl hanya bila memang relevan — supaya RAM & CPU tersisa untuk sesi lain di mesin yang sama (ukuran nyata di repo ini: 258 berkas test, 6 proses `tsc`). Akarnya **lubang di kontrak prompt**: `runner/src/prompt.ts` tak pernah menyebut scope verifikasi, jadi agen jatuh ke DoD repo target + kebiasaan "jalankan semuanya". Knob `Setting.verifyScope` (kolom `Json` → **tanpa migration**) + override di `POST /terminal/sessions`; mewujud sebagai klausa prompt (**hanya flow ber-fase `Execute`** — flow dokumen tak punya test) + env `HANOMAN_BASE_SHA`/`HANOMAN_VERIFY_SCOPE`. **Mengarahkan, bukan memaksa:** tak ada hook deny (preseden 0073), dan klausanya memberi jalan keluar eksplisit untuk perubahan berdampak luas. **Gotcha wajib:** `vitest --changed` menyalakan `passWithNoTests` sehingga nol test **terlihat hijau**; `baseSha` WAJIB lewat env karena worktree lahir `--detach` (tak ada `main`, `HEAD~1` salah); env sesi adalah **prefix shell** di depan argv sehingga tak pernah tercetak `/bin/echo` — buktinya harus dibaca dari DALAM proses; dan untuk perubahan di modul INTI `--changed` memang mendekati suite penuh (terukur di spec ini sendiri: menyentuh `shared/src/{enums,entities,dto}.ts` → **217 berkas / 1589 test / 177 dtk**) — itu blast radius yang sebenarnya, bukan kegagalan; penghematan datang dari perubahan berdaun
- [0079 — Riwayat sesi terminal: store LOCAL-only + transkrip berkas, hook di dua titik cekik pty](0079-history-sesi-terminal-store-lokal-plus-transkrip.md) — **memperluas 0016/0047**, membuka pengecualian **terbatas** atas larangan transkrip di 0047, terkait 0002/0015/0028/0038/0065 (SPEC-362): `SessionHistory` LOCAL-only (PK uuid — `sessionId` sesi spec deterministik & berulang tiap reopen; tanpa FK karena sesi VPS ber-`projectId` sintetis), baris lahir saat sesi **LAHIR** sehingga sesi berjalan pun tercatat; `registerSessionHooks({onBirth,onDeath})` ditembak dari `createSession`/`killSession` sehingga `pty.ts` tetap **nol dependensi DB** dan 12 call site tak tersentuh; transkrip `capture-pane` **tanpa `-e`** di-capture SEBELUM `kill-session` (teks polos, cap 1 MiB menyimpan **ekor**) jadi berkas di `HANOMAN_TRANSCRIPT_DIR`, DB hanya pointer; `GET/DELETE /api/terminal/history*` mewarisi capability `sessions` karena di bawah prefix `/terminal`, `skip`/`take` DB **sah** karena tak ada overlay live (beda dari larangan 0038 untuk `GET /specs`); UI modal di Terminal (muat-lebih + baris penutup pola SPEC-351) + "Mulai lagi" ber-`restartableKind`; `reconcileHistory()` saat boot menutup baris zombie
- [0078 — Unduh dokumen: query `?download=` di endpoint dokumen + render PDF server-side](0078-unduh-dokumen-md-pdf.md) — **memperluas 0011/0018** (unduhan = nilai turunan dari filesystem, tak disimpan), terkait 0041/0028/0057/0076 (SPEC-361): PRD · spec · plan · audit · docs SoT · berkas IDE bisa diunduh `.md` mentah maupun `.pdf` dari **setiap** pratinjau (`SpecDocsModal` menutup Backlog **dan** Terminal sekaligus). **Tanpa endpoint baru** — query `?download=md|pdf` menempel di empat endpoint dokumen yang sudah ada (preseden `GET /projects/:id/archive`), nilai lain/absen → JSON lama utuh; **tanpa skema/migration**; auth tetap cookie same-origin lewat `<a download>`. PDF dirender server-side `marked.lexer` → `pdfkit` memakai parser **yang sama** dengan preview, jadi yang tercetak = yang tampil. **Gotcha wajib:** standard-14 font hanya WinAnsi dan pdfkit **tak melempar** untuk glyph di luarnya — ia mencetak mojibake senyap (`→` jadi `!'`, emoji jadi `Ø<ß‰`), jadi semua teks harus lewat `toWinAnsi()`
> **Diperluas SPEC-385** (tanpa ADR baru): cakupan `?download=md|pdf` ADR-0078 bertambah dari 4 endpoint dokumen menjadi **9** — lima endpoint review/diff (`/specs/:id/review/*`, `/terminal/sessions/:id/review/*`, `/projects/:id/file-diff`, `/projects/:id/commit/:sha/file`, `/projects/:id/compare/file`) ikut menerima query yang sama lewat `sendReviewDownload`, mengirim `ReviewFile.content` (isi sesudah perubahan) dan membalas **404** untuk berkas biner/terhapus. Pemicunya aksi preview `.md` baru di IDE & Review (`ds/DocPreviewModal.tsx`); rinciannya di [frontend-implementation](../frontend/frontend-implementation.md) & [api-contract](../architecture/api-contract.md).
- [0077 — Hapus branch tak terpakai: daftar ter-merge turunan + pagar proteksi per-branch](0077-hapus-branch-tak-terpakai-pagar-per-branch.md) — **memperluas 0055**, terkait 0018/0011/0002/0032/0037/0065 (SPEC-360): `GET /projects/:id/branches/unused` menurunkan daftar branch ter-merge langsung dari git (`git branch --merged`, base `?base=→main→master→branch aktif`, ref origin dibanding `origin/<base>`) — tanpa kolom DB, tanpa migration; lima kunci proteksi per-branch (`current`/`base`/`worktree`/`spec-open`/`session` — `session` terpisah karena sesi lahir `--detach` sehingga tak muncul di `git worktree list`) **ditegakkan ulang di jalur tulis** `POST /projects/:id/branches/delete`, sehingga hanya branch ter-merge & tak terkunci yang bisa dihapus dan scope (`local`/`remote`/`both`) menyempit per branch; eksekusi tetap lewat `runGitOp` `delete-branch` (SPEC-206) tanpa `--force`. Pagar ini **keselamatan data untuk satu endpoint bulk**, bukan guardrail eksekusi — ADR-0037 utuh. Tiga gotcha git terukur: baris `(no branch)` di worktree detached, `origin/HEAD` yang dipendekkan jadi bare `origin`, dan `--end-of-options` yang tak berlaku untuk argumen `--merged` (→ base di-resolve ke SHA dulu). — **Diamandemen SPEC-859 (2026-08-20), cakupan melebar & satu larangan dipersempit, keputusannya utuh:** panel Branches hanya pernah memanggil endpoint ini, jadi branch yang masih **aktif** tak terlihat sama sekali di dashboard dan empty state "Tak ada branch ter-merge" membuat repo ber-belasan branch tampak kosong. `?include=all` kini memancarkan SELURUH ref (`refs/heads` ∪ `refs/remotes/origin`); tanpa parameter itu himpunan barisnya tetap seperti dulu. `local`/`remote` ditajamkan jadi "ref itu ADA" dan merged-ness pindah ke `mergedLocal`/`mergedRemote` + turunannya `merged` (= tiap sisi yang ada sudah ter-merge) — tanpa itu badge scope berbohong untuk branch belum ter-merge; efeknya pada mode default hanya di kasus divergen (`x` ter-merge ke `main` lokal tapi `origin/x` belum ke `origin/main`), yang kini **hilang** dari daftar alih-alih muncul setengah-aman — lebih ketat, tak pernah lebih longgar. Dan larangan `-D` **dipersempit**: alasan aslinya ("semua kandidat sudah ter-merge") gugur begitu daftarnya memuat branch belum ter-merge, sementara `git branch -d` memang menolaknya. Gerbangnya `allowUnmerged` di body `POST …/branches/delete`, dikirim **hanya** oleh dialog konfirmasi risiko yang menyebut nama branch-nya, menyatakan commit-nya akan hilang, dan menuntut ketikan ulang (`requireText` ADR-0121); force dipasang **per sisi** (hanya lokal yang `!mergedLocal` — `push origin --delete` tak pernah menguji merged-ness). Lima kunci proteksi **menang atas** `allowUnmerged` dan validasi ulang nama tetap ada. `DeleteResult.forced` membuat penghapusan paksa berjejak; batas render 100 baris di klien adalah **bagian dari definisi "sedang tampak"**, jadi "Pilih semua yang boleh" tak pernah memuat baris yang tak dirender
- [0076 — Eskalasi audit dinamis: manifest rekomendasi di dokumen audit + tiga pintu (QA · brief · PRD)](0076-eskalasi-audit-dinamis-manifest-rekomendasi.md) — **memperluas 0057** (audit-only kini punya tiga tindak lanjut, bukan satu), memakai pola **0069** (prosa + blok json kanonik), terkait 0075/0059/0041/0018/0011 (SPEC-340): fase Laporan menulis blok ```json `{escalation:{target:"none|qa|brief|prd",reason,alternatives,prefill}}` di dokumen audit → `parseEscalation` defensif + `GET /api/specs/:id/escalation` menyajikannya sebagai **nilai turunan** (freshest-wins, tanpa kolom DB); `SpecDetail` (source `audit` **dan** `cross-audit`) menyorot target rekomendasi + alasannya tapi tetap menyediakan ketiganya (manusia terakhir memutuskan); kontinuitas: brief lanjutan audit memakai `fromAudit` **tanpa** fase `skipped` (beda sadar dari qa — audit memuat temuan, bukan bentuk solusi), sesi PRD menerima `branchFrom` (worktree dari branch audit) **dan** `fromAudit` (isi dokumen audit disematkan ke prompt). Tanpa migration
- [0074 — Codex sebagai mesin sesi: `Agent` per sesi, hook lewat `-c`, mode goal deterministik](0074-codex-sebagai-mesin-sesi.md) — **memperluas 0024/0061/0073**, terkait 0002/0016/0029/0037 (SPEC-338): `Setting.agent` + `Setting.codex` (tanpa migration), `agent` opsional di `POST /terminal/sessions`, `@hanoman_agent` di tmux, argv per agen di `runner/src/agent-cli.ts`; codex tak punya event `Notification` (marker keputusan pakai `Stop`+`UserPromptSubmit`) dan **mendiamkan hook `type="prompt"`** → mode goal jadi gate sh deterministik (phase file + checkbox plan, exit 2 = continuation) berpagar 25 penolakan; `ensureCodexTrust` membuka gerbang trust direktori (satu entri per project, worktree mewarisi root); indikator limit codex = badge & endpoint TERPISAH (`GET /api/limits/codex` + grup siar `codexLimits`), dibaca dari snapshot `rate_limits` di rollout sesi codex — nol jaringan, nol sentuhan token; label window diturunkan dari `window_minutes` karena `primary` bisa 5-jam ATAU mingguan
- [0073 — Mode goal sesi backlog: Stop hook bertipe `prompt` saat sesi lahir + keystroke `/goal`](0073-mode-goal-stop-hook-per-sesi.md) — **memperkuat 0035** (otonomi lintas-fase jadi mekanisme, bukan imbauan prompt), cermin runtime bagi 0029 (gate plan terceklist), pola 0061 (knob → argv saat lahir), **TIDAK membalik 0037** (SPEC-332): `guardSettings` menyisipkan `hooks.Stop=[{type:"prompt",prompt:<kondisi>}]` ke `--settings` (jaminan, deterministik) + `armGoalInTui` mengetik `/goal` ke pane (visibilitas TUI, best-effort — keduanya tak saling hapus karena `/goal` hanya membaca session hooks registry); kondisi default = DoD hanoman yang menuntut **bukti segar** di transkrip (evaluator hook tanpa tool & transkrip dipotong); knob `Setting.goal` + `goal`/`goalCondition` di `POST /terminal/sessions`, presedens override→template→default; cakupan sesi backlog, default mati, tanpa migration
- [0072 — Fondasi scheduler otonom: engine in-process, antrean durable, cap concurrency](0072-scheduler-fondasi-engine-antrean-durable-cap.md) — **membalik sebagian 0024**, memperluas 0015/0016/0025/0049, terkait 0045 (SPEC-294): engine sweep in-process (server.ts, timer `.unref`), tabel `SchedulerQueueItem` (LOCAL-only, `specId @unique` idempoten), governor cap=`maxConcurrent` dari `pty.listSessions`, Pause; knob `Setting.scheduler` + `Project.schedulerOptIn` (default mati); kontrak `registerSchedulerSource`/`enqueue` + `GET/PUT /api/scheduler/config` & `GET /api/scheduler/state`
- [0071 — Link ticket triase: deep-link backlog (hash SPA) + token bagikan status publik](0071-link-ticket-triase-deeplink-sharetoken.md) — **memperluas 0062**, terkait 0018/0019/0044/0066 (SPEC-293): deep-link `#spec=<id>` sekali-mount (bukan router umum), `publicStatus` di `shared`, kolom `Ticket.shareToken` (opaque) → route publik terima kunci pelapor ATAU shareToken; `GET /tickets/:id` kembalikan `publicStatusUrl`
- [0069 — Breakdown PRD → backlog paralel-independen (sesi breakdown + manifest + materialize)](0069-breakdown-prd-ke-backlog-paralel.md) — **memperluas 0041**, terkait 0015/0002/0032/0059 (SPEC-273): flow `breakdown` menulis `docs/prd/<slug>.breakdown.md` (prosa + blok json kanonik); `GET /projects/:id/breakdown` + `POST /specs/batch` materialize N spec independen; tanpa perubahan skema
- [0068 — Lampiran tiket masuk record-sync (metadata di feed, byte lazy-fetch)](0068-lampiran-tiket-masuk-record-sync.md) — **mencabut sebagian 0066**, memperluas 0045, terkait 0043/0067/0062 (SPEC-272): entitas `ticketAttachment` di `SYNCED` (metadata) + endpoint biner hub `GET /api/sync/attachments/:storageKey` (device-token) + `readUploadOrFetch` fetch-through+cache di client; arah hub→local
- [0067 — Sync self-healing: backfill feed + rekonsil konflik manual (LWW-default)](0067-sync-lww-reconciliation-manual.md) — **memperluas 0045**, terkait 0043/0046/0066/0008 (SPEC-270): `updatedAt @updatedAt` jam LWW, tabel `SyncConflict` + modal side-by-side, `backfillFeed` boot hub
- [0066 — Ticket masuk record-sync (publish asal-hub) + pemicu sync manual](0066-errors-tickets-masuk-record-sync-plus-pemicu-manual.md) — **memperluas 0045**, terkait 0043/0046/0062 (SPEC-268); **diamandemen 0092** — bagian `errorGroup` dicabut, keputusan tiket & pemicu manual tetap berlaku
- [0065 — AI agent capability: agent token + capability scope per-domain gating `/api`](0065-ai-agent-capability-agent-token.md) — **memperluas 0028**, cermin 0044, terkait 0037 (SPEC-257)
- [0064 — `Project.id` renameable lewat operasi rename khusus (cascade + merambat sync)](0064-project-id-renameable.md) — **mencabut sebagian SPEC-146**, memperluas 0045/0046/0062 (SPEC-255)
- [0062 — Help Center: model tiket + endpoint publik ber-scope-project + jembatan triase→backlog](0062-help-center-tiket-publik-triase.md) — **memperluas 0028/0033/0039** (SPEC-253)
- [0061 — Model & effort per sesi (picker saat Start), mencabut matrix per-fase](0061-model-effort-per-sesi-picker-start.md) — **mengamandemen 0058** (SPEC-252)
- [0059 — Kontinuitas branch take-to-backlog (PRD→brief, audit→QA) + skip-audit qa lanjutan audit](0059-kontinuitas-branch-take-to-backlog-dan-skip-audit.md) — **memperluas 0032/0040/0041/0057** (SPEC-244)
- [0058 — Model & effort per fase, lewat `/model`+`/effort` in-session](0058-model-effort-per-fase.md) — **mengamandemen 0024, sebagian menghidupkan 0003** (SPEC-238) — *mekanisme per-fase dicabut oleh 0061*
- [0057 — Audit-only sebagai source + flow (dokumen, tanpa perbaikan)](0057-audit-only-source-flow.md) — SPEC-237
- [0056 — Terminal biasa = shell mentah di repoDir project (bukan claude)](0056-terminal-shell-non-claude.md) — SPEC-236, pola ADR-0042 · **amandemen 2026-08-19**: `shellBin()` juga dipakai untuk `set-option -g default-shell` tmux (sebelum `new-session`, seperti `remain-on-exit` 0016), berlaku untuk SEMUA sesi termasuk claude — tanpa itu shell login user service (`/usr/sbin/nologin` di host yang dikeraskan) yang menentukan, dan setiap pane mati saat lahir
- [0055 — Git graph parity: taksonomi operasi + eksekusi berlapis](0055-git-graph-parity-op-taxonomy.md) — **memperluas 0034/0053** (SPEC-233)
- [0054 — Review + integrate ber-skop sesi untuk sesi project-level (PRD)](0054-review-integrate-ber-skop-sesi-untuk-prd.md) — **memperluas 0041**
- [0053 — Merge via git graph: deterministik di worktree isolasi, konflik → sesi claude (pola integrate)](0053-git-graph-merge-worktree-isolasi-sesi-claude.md)
- [0052 — Scaffold flow: project from-scratch dari ide → SoT penuh (git-init + startScaffoldPrompt)](0052-scaffold-flow-from-ide.md)
- [0051 — Kepatuhan VPS Fase 3: drift derived + Notification agregat, applicability app-layer advisory](0051-vps-fase3-drift-applicability.md)
- [0050 — Kepatuhan VPS: katalog 232 item di git + model state + scoring + remediasi dry-run](0050-vps-compliance-katalog-scoring.md)
- [0049 — Config runtime: store + registry (knob sync di Settings)](0049-config-runtime-store-registry.md)
- [0048 — Auto-update: deteksi versi read-only, tanpa self-mutation](0048-auto-update-deteksi-read-only.md)
- [0047 — Ringkasan hasil sesi = SessionResult append-only, whitelist, purge manual](0047-activity-log-session-result.md)
- [0046 — Kanal WebSocket sync terpisah, token-authed pada upgrade](0046-kanal-ws-sync-terpisah.md)
- [0045 — Sync record via SyncLog change-feed + version-stamp optimistic concurrency](0045-skema-sync-synclog-version-stamp.md)
- [0044 — Identitas mesin lewat device token per-device (hash-at-rest, revocable)](0044-device-token-machine-identity.md)
- [0043 — Sync server↔client = server-to-server, peran ditentukan konfigurasi](0043-sync-arsitektur-hub-client-server-to-server.md)
- [0042 — Open Console = ssh mentah di tmux hanoman lokal, bukan tmux remote](0042-vps-console-ssh-tmux-lokal.md)
- [0041 — PRD adalah dokumen + flow project-level, bukan entitas DB](0041-prd-sebagai-dokumen-flow-project-level.md)
- [0040 — Jalur cepat qa dielicit lewat prompt, diputuskan agen](0040-jalur-cepat-qa-dielicit-prompt.md) — **supersedes mekanisme 0020**
- [0039 — Data real-time dashboard lewat satu WebSocket siar, bukan polling klien](0039-realtime-lewat-websocket-siar.md)
- [0038 — Paginasi/filter daftar di response layer, overlay atas set penuh](0038-paginasi-di-response-layer.md)
- [0037 — Cabut guardrail deny perintah (PreToolUse) sepenuhnya](0037-cabut-guardrail-safety.md)
- [0034 — IDE Visual boleh memutasi working tree, digerbang sesi + force](0034-ide-mutasi-working-tree-utama.md)
- [0036 — Notifikasi human decision dari hook Claude](0036-notifikasi-human-decision.md)
- [0035 — Sesi menembus batas fase tanpa berhenti kecuali butuh keputusan manusia](0035-sesi-lanjut-fase-tanpa-berhenti-kecuali-keputusan.md)
- [0033 — Notifikasi saat backlog selesai](0033-notifikasi-backlog-selesai.md)
- [0032 — Branch adalah properti backlog item](0032-branch-adalah-properti-backlog-item.md)
- [0031 — Rebase & merge branch done spec dari dashboard](0031-rebase-merge-backlog.md)
- [0030 — `Spec` menyimpan baseSha/headSha; review done men-diff darinya](0030-spec-menyimpan-base-head-sha.md)
- [0029 — `Execute done` hanya sah bila plan terceklist penuh](0029-execute-done-butuh-plan-terceklist.md)
- [0028 — Auth: sesi opaque revocable di DB, bind 127.0.0.1 + reverse proxy TLS](0028-auth-sesi-opaque-di-db.md)
- [0027 — Stage boleh mundur atas perintah human eksplisit](0027-revert-stage-backward-only.md)
- [0026 — Reverse docs sebagai sesi interaktif project-level](0026-reverse-docs-sesi-interaktif-project-level.md)
- [0025 — Modul VPS: tabel sendiri, script deterministik, tanpa queue](0025-modul-vps-script-deterministik.md)
- [0024 — Sesi Claude Code interaktif menggantikan run](0024-sesi-interaktif-menggantikan-run.md) — **supersedes 0005/0012/0017/0022, sebagian 0010, melemahkan 0008**
- [0023 — Guardrail Source of Truth dicabut](0023-guardrail-sot-dicabut.md) — **supersedes 0001**
- [0022 — Agen bertanya, run berstatus `awaiting`](0022-pertanyaan-agen-berstatus-awaiting.md) — *superseded by 0024*
- [0021 — Nomor SPEC diklaim docs, bukan hanya database](0021-nomor-spec-diklaim-docs-bukan-hanya-database.md)
- [0020 — Fase perencanaan QA dipangkas oleh keputusan audit](0020-fase-perencanaan-qa-dipangkas-keputusan-audit.md) — *mekanisme superseded by 0040*
- [0019 — SHA disimpan, diff diturunkan](0019-sha-disimpan-diff-diturunkan.md)
- [0018 — Coverage diturunkan saat dibaca, bukan disimpan](0018-coverage-nilai-turunan.md)
- [0017 — Run terputus melanjutkan sesinya](0017-run-terputus-melanjutkan-sesinya.md) — *superseded by 0024*
- [0016 — Sesi terminal hidup di tmux](0016-sesi-terminal-hidup-di-tmux.md)
- [0015 — Satu backlog, satu sesi Claude](0015-one-session-per-backlog.md)
- [0014 — Sesi interaktif lewat PTY di proses API](0014-pty-terminal-di-proses-api.md)
- [0013 — SoT coverage scoped to docsDir](0013-sot-coverage-scoped-to-docsdir.md)
- [0012 — Biaya adalah estimasi, bukan guardrail](0012-cost-is-an-estimate-not-a-guardrail.md) — *superseded by 0024*
- [0011 — Docs adalah filesystem nyata, bukan salinan DB](0011-docs-realtime-filesystem.md)
- [0010 — Runner spawn `claude` CLI langsung, bukan Agent SDK](0010-runner-spawns-claude-cli.md) — *sebagian superseded by 0024 (gate PreToolUse tetap)*
- [0009 — Guardrail yang crash gagal keras](0009-guardrail-crash-fails-loud.md) — *historis per 0023*
- [0008 — Spec stage mirrors a real run](0008-stage-mirrors-run.md) — *diamandemen 0024/0027/0029*
- [0007 — Run.finishedAt untuk durasi nyata](0007-run-finished-at.md) — *de-facto obsolete (0024)*
- [0006 — GitHub App schema](0006-github-app-schema.md) — *de-facto obsolete (0024)*
- [0005 — Antrian durable (BullMQ/Redis) + worker](0005-durable-queue-and-worker.md) — *superseded by 0024*
- [0004 — Foundation schema deltas](0004-foundation-schema-deltas.md)
- [0003 — Pemilihan model per step](0003-per-step-model-selection.md) — *de-facto obsolete (0024)*
- [0002 — Isolasi run dengan git worktree](0002-git-worktree-isolation.md)
- [0001 — Docs sebagai Source of Truth](0001-docs-as-source-of-truth.md) — *superseded by 0023*
